export const COURSES = [
  {
    slug: "system-design",
    title: "System Design / Arquitectura de Sistemas",
    level: "experto",
    category: "design",
    techs: ["Load Balancing", "Sharding", "Alta Disponibilidad"],
    duration: "9 semanas",
    icon: "🧠",
    description: "Requisitos, escalabilidad horizontal, load balancing, caching, CDN, replicación, sharding y diseño de sistemas de alto tráfico.",
    objectives: ["Diseñar sistemas de alto tráfico", "Resolver entrevistas de system design"],
    requirements: ["Sistemas Distribuidos", "Arquitectura"],
    tags: ["system-design", "escalabilidad", "alta-disponibilidad", "distribuido"],
    modules: [
      {
        id: "modulo-01",
        n: 1,
        t: "Fundamentos",
        level: "experto",
        caption: "Las piezas con las que se construye cualquier sistema grande: requisitos, capacidad y datos.",
        desc: "Define el problema, estima la carga y elige los datos antes de dibujar la primera caja en un pizarrón.",
        subtitle: "Requisitos, estimaciones y datos: el arranque de todo diseño serio.",
        objectives: [
          "Entender qué es system design y sus bloques de construcción",
          "Recoger requisitos funcionales y no funcionales",
          "Hacer estimaciones de capacidad de memoria, tráfico y almacenamiento",
          "Modelar los datos según el tipo de carga",
        ],
        lessons: [
          { id: "que-es-system-design", title: "¿Qué es system design?", duration: "8 min", level: "experto", category: "concepto", cardText: "Mapa del mapa: qué se evalúa, qué se aprende y qué skills construye.", desc: "Introducción al diseño de sistemas de alto tráfico." },
          { id: "bloques-de-construccion", title: "Bloques de construcción", duration: "10 min", level: "experto", category: "concepto", cardText: "Servidores, cachés, colas, BBDD, CDN: el vocabulario común.", desc: "Los componentes fundamentales que aparecen en todo diseño." },
          { id: "requisitos-y-restricciones", title: "Requisitos y restricciones", duration: "9 min", level: "experto", category: "proceso", cardText: "Funcionales, no funcionales y qué pasa si saltan los límites.", desc: "Cómo capturar requisitos y restricciones antes de diseñar." },
          { id: "estimaciones-de-capacidad", title: "Estimaciones de capacidad", duration: "10 min", level: "experto", category: "numeros", cardText: "Números de servilleta: QPS, almacenamiento, ancho de banda.", desc: "Cálculos rápidos para dimensionar el sistema." },
          { id: "modelado-de-datos", title: "Modelado de datos", duration: "9 min", level: "experto", category: "datos", cardText: "Entidades, relaciones, cardinalidad y cómo leer la carga.", desc: "Diseña los datos con la arquitectura en mente." },
        ],
      },
      {
        id: "modulo-02",
        n: 2,
        t: "Patrones",
        level: "experto",
        caption: "Los patrones recurrentes cuando el tráfico crece y el monolito deja de alcanzar.",
        desc: "Patrones de capas, eventos, consistencia, caché y durabilidad que todo diseñador maneja igual que un idioma.",
        subtitle: "Capas, eventos, consistencia y caché: el vocabulario de las arquitecturas distribuidas.",
        objectives: [
          "Dividir el sistema en capas y servicios con responsabilidades claras",
          "Modelar flujos con colas y eventos asíncronos",
          "Entender consistencia y consenso entre nodos",
          "Aplicar patrones de caché y durabilidad",
          "Comparar monolito frente a arquitectura distribuida",
        ],
        lessons: [
          { id: "arquitectura-en-capas", title: "Arquitectura en capas", duration: "9 min", level: "experto", category: "patron", cardText: "Presentación, lógica, datos: separación que escala.", desc: "Estructura el sistema por capas con fronteras claras." },
          { id: "cola-y-eventos-patterns", title: "Patrones de colas y eventos", duration: "9 min", level: "experto", category: "eventos", cardText: "Colas, pub/sub, topics y el acoplamiento que se disuelve.", desc: "Desacopla productores y consumidores con mensajería." },
          { id: "consistencia-consenso", title: "Consistencia y consenso", duration: "10 min", level: "experto", category: "distribuido", cardText: "CAP, quórums y liderazgo cuando los nodos opinan.", desc: "Coordinación entre nodos: consenso y consistencia." },
          { id: "dedicla-y-caching-patterns", title: "Patrones de durabilidad y caché", duration: "9 min", level: "experto", category: "datos", cardText: "Write-through, cache-aside y la durabilidad de cada dato.", desc: "Patrones de persistencia y caché que resuelven carga.", },
          { id: "monolitica-vs-distribuida", title: "Monolito vs distribuida", duration: "10 min", level: "experto", category: "decision", cardText: "Cuándo un monolito gana y cuándo toca repartir el sistema.", desc: "Elige entre un monolito bien hecho y una arquitectura distribuida.", },
        ],
      },
      {
        id: "modulo-03",
        n: 3,
        t: "Diseño completo",
        level: "experto",
        caption: "Casos reales de principio a fin: búsqueda, chat, feed y URL shortener.",
        desc: "Recorre el diseño completo de sistemas conocidos y aprende a comunicar cada decisión.",
        subtitle: "Cuatro casos clásicos de entrevista resueltos paso a paso.",
        objectives: [
          "Aplicar el proceso completo a sistemas conocidos",
          "Resolver los casos de entrevista: búsqueda, chat, feed y shortener",
          "Presentar y comunicar el diseño con claridad",
        ],
        lessons: [
          { id: "diseno-de-una-url-shortener", title: "Caso: URL shortener", duration: "11 min", level: "experto", category: "caso", cardText: "IDs cortos, redundancia y consistencia para millones de URLs.", desc: "Diseño completo de un acortador de URLs.", },
          { id: "diseno-de-un-feed", title: "Caso: feed (newsfeed)", duration: "11 min", level: "experto", category: "caso", cardText: "Fanout push/pull y el ranking que no mata al backend.", desc: "Diseño de un newsfeed estilo redes sociales.", },
          { id: "diseno-de-un-chat", title: "Caso: sistema de chat", duration: "11 min", level: "experto", category: "caso", cardText: "WebSockets, presencia y el límite de las conexiones.", desc: "Diseño de un chat de 1 a 1 y de grupos.", },
          { id: "diseno-de-busqueda", title: "Caso: búsqueda", duration: "11 min", level: "experto", category: "caso", cardText: "Índices invertidos, tokenización y relevancia.", desc: "Diseño de un motor de búsqueda básico.", },
          { id: "presentar-tu-diseno", title: "Presentar y comunicar", duration: "10 min", level: "experto", category: "soft", cardText: "Estructura la historia del diseño y defiende trade-offs.", desc: "Comunica el diseño de forma clara y segura.", },
        ],
      },
      {
        id: "modulo-04",
        n: 4,
        t: "Escalabilidad y distribución",
        level: "experto",
        caption: "Cuando una máquina no alcanza: reparte la carga por máquinas, nodos y regiones.",
        desc: "Escalamiento vertical y horizontal, load balancing, sharding, replicación y backpressure para sostener el tráfico.",
        subtitle: "De una máquina a un clúster sin perder la cabeza ni la latencia.",
        objectives: [
          "Decidir entre escalar vertical u horizontalmente",
          "Distribuir el tráfico con load balancers en varias capas",
          "Particionar datos con sharding y entender sus límites",
          "Dar redundancia con replicación sin elegir CAP a ciegas",
          "Proteger el sistema con backpressure y límites de rendimiento",
        ],
        lessons: [
          {
            id: "escalamiento-horizontal-y-vertical",
            title: "Escalamiento vertical y horizontal",
            duration: "11 min",
            level: "experto",
            category: "escalado",
            cardText: "Máquinas más grandes vs más máquinas: coste, límite y complejidad.",
            desc: "Compara escalar hacia arriba frente a escalar hacia afuera.",
            body: {
              intro: [
                "El primer cuello de botella de casi todo sistema es una sola máquina que se queda corta. La pregunta no es si va a pasar: es qué vas a hacer cuando pase. Tienes dos palancas: una máquina más potente o más máquinas trabajando juntas.",
                "Este capítulo define ambas estrategias, sus números, sus límites físicos y cuándo cada una es la jugada correcta. Ni la vertical ni la horizontal son buenas o malas: son herramientas con tramos de validez distintos.",
              ],
              def: {
                par: "Escalamiento vertical (scale-up) es aumentar la capacidad de una sola máquina: más CPU, más RAM, discos más rápidos. Escalamiento horizontal (scale-out) es añadir más máquinas idénticas detrás de un mecanismo que reparte la carga.",
                porQue: "El vertical paga solo y se topa con el techo físico de la plataforma (tu nube no vende una máquina de 4 TB de RAM a precio razonable). El horizontal compite contra la complejidad: cada nueva máquina exige estado compartido, sincronización y tolerancia a fallos.",
                cuando: "Empieza vertical si tu carga es pequeña y predecible; muévete a horizontal cuando la vertical deja de alcanzar, necesitas redundancia o quieres absorber picos sin máquinas gigantes.",
              },
              concepts: [
                "La carga útil (utilización de CPU/RAM/IO) dicta si escalar vertical u horizontal resuelve el problema real.",
                "El techo vertical: una instancia concreta de una nube tiene un máximo de RAM, vCPU y throughput que no puedes superar.",
                "El costo relativo: 2 máquinas medianas suelen salir más baratas que 1 máquina enorme con la misma capacidad.",
                "La estatividad es la barrera: si el estado vive en la máquina, añadir máquinas no escala; hay que sacar el estado fuera (BBDD, Redis, objetos).",
                "Escalar horizontal sin redundancia es solo repartir carga: para tolerar fallos necesitas al menos 2 nodos por rol y reintentos.",
              ],
              basic: {
                p: "El cálculo de servilleta: si una máquina aguanta 500 RPS y recibes 2.000 RPS, necesitas 4+ nodos con margen de pico.",
                lang: "text",
                code: [
                  "// Dimensionado sencillo",
                  "caps_machine = 500      // RPS por máquina",
                  "pico = 2000             // RPS esperados en hora punta",
                  "margen = 1.5            // para picos y failover",
                  "nodos = ceil(pico / caps_machine * margen)",
                  "// -> 6 nodos en vez de 4 por el margen",
                ],
                out: "pico 2000 RPS → 6 nodos con margen 1.5x",
              },
              intermediate: {
                p: "Los autoscalers de Kubernetes hacen la decisión horizontal automática con métricas, pero solo si la aplicación es stateless o el estado tiene su propio mecanismo de escala.",
                caption: "estado deseado del clúster (Kubernetes)",
                lang: "yaml",
                code: [
                  "apiVersion: autoscaling/v2",
                  "kind: HorizontalPodAutoscaler",
                  "metadata:",
                  "  name: api",
                  "spec:",
                  "  scaleTargetRef:",
                  "    apiVersion: apps/v1",
                  "    kind: Deployment",
                  "    name: api-v1",
                  "  minReplicas: 3",
                  "  maxReplicas: 20",
                  "  metrics:",
                  "    - type: Resource",
                  "      resource:",
                  "        name: cpu",
                  "        target:",
                  "          type: Utilization",
                  "          averageUtilization: 65",
                ],
              },
              real: {
                p: "Una API de pagos arrancó en una máquina de 8 vCPU y aguantaba 300 TPS. Llegó el lanzamiento de una app y el tráfico se quintuplicó en dos horas. La jugada fue: subir el autoscaler a 15 réplicas stateless, mover la sesión y la caché a Redis, y dejar la capa de datos con su propio clúster.",
                ul: [
                  "Separar cada instancia de su estado: sesión fuera, ficheros fuera, caché fuera.",
                  "Subir la base de datos vertical hasta un punto y luego particionarla (sharding).",
                  "Reducir el margen cuando el autoescalado ya cubre los picos.",
                  "Capacitar el sistema para degradar (caché global) si una dependencia cae.",
                ],
                lang: "nginx",
                code: [
                  "upstream api_pool {",
                  "  least_conn;",
                  "  server api-01:8080 max_fails=3 fail_timeout=10s;",
                  "  server api-02:8080 max_fails=3 fail_timeout=10s;",
                  "  server api-03:8080 max_fails=3 fail_timeout=10s;",
                  "}",
                  "server {",
                  "  listen 443 ssl;",
                  "  location / { proxy_pass http://api_pool; }",
                  "}",
                ],
                nota: "El load balancer y el autoscaler van de la mano: el balancer reparte a los nodos sanos y el autoscaler decide cuántos hay.",
              },
              expl: [
                "El volumen de trabajo define la palanca: si falta CPU, escala fuerza; si falta IOPS, revisa el diseño de datos antes que la nube.",
                "El estado se convierte en el diseñador: un servicio sin estado escala con dos líneas de config; uno con estado necesita su infraestructura de datos.",
                "El margen de failover es obligatorio: si necesitas N nodos en pico, despliega N+1 para que un fallo no cargue el 200 % sobre uno.",
                "El techo vertical en la nube es real y caro: llegando a las 16-32 vCPU la curva de precio se dispara.",
                "La regresión de planificación es vieja: primero arregla el algoritmo, después la máquina. La vertical nunca arregla un O(n²).",
              ],
              errors: [
                "Comprar la máquina más grande antes de medir si el problema es CPU, memoria o IOPS.",
                "Escalar horizontal un servicio que guarda sesión en memoria y romper la sesión de cada usuario en cada reinicio.",
                "Dimensionar solo para el pico y pagar 10x en la base de datos durante el valle.",
              ],
              tips: [
                "Empieza vertical, mide, y escala horizontal solo cuando el techo vertical o la redundancia lo justifiquen.",
                "Diseña stateless desde el día uno: sesión en header/cookie firmada o en Redis, nunca en memoria de app.",
                "Automatiza el escalado con métricas de latencia y utilización, no solo de CPU.",
                "Documenta el dimensionado: qué máquinas, qué margen y a qué tráfico aguanta cada pico.",
              ],
              exercise: [
                "Calcula cuántas máquinas de 400 RPS necesitas para 12.000 RPS con margen 1.4.",
                "Lista qué estado hay que sacar de la máquina para escalar tu servicio favorito.",
                "Diseña el autoscaler y el load balancer para el servicio del punto 1 en dos capas.",
              ],
              summary: [
                "Vertical es simple y finita; horizontal es compleja y sin techo práctico.",
                "El estado es la barrera real del escalado horizontal: sácalo de la instancia.",
                "Mide primero y deja margen de failover en cada dimensión de carga.",
              ],
              lang: "text",
              exampleFile: "escalamiento",
              desc: "Compara escalar vertical y horizontal y elige la palanca correcta con números.",
            },
          },
          {
            id: "load-balancing",
            title: "Load balancing a escala",
            duration: "11 min",
            level: "experto",
            category: "distribucion",
            cardText: "Cómo repartir tráfico entre nodos sin que uno muera de éxito.",
            desc: "El load balancer como puerta de entrada: algoritmos, capas y salud.",
            body: {
              intro: [
                "El load balancer (LB) es el portero de toda arquitectura que escala horizontal: recibe cada petición y decide a qué nodo sano va. Si está mal configurado, cualquier pico se convierte en una cascada de 5xx.",
                "Aquí verás los algoritmos de reparto, las diferencias entre L4 y L7, y cómo la salud de los nodos (health checks) es la mitad del trabajo real.",
              ],
              def: {
                par: "Un load balancer distribuye las peticiones entrantes entre un pool de servidores según un algoritmo (round-robin, least-conn, hash de IP/URL) y retira del pool a los nodos que fallan los health checks.",
                porQue: "Sin balancer, un nodo recibe todo y el resto está ocioso; con un balancer mal configurado, los nodos sobrecargados reciben carga igual. El reparto correcto mejora latencia, disponibilidad y aprovechamiento.",
                cuando: "Úsalo siempre que haya más de un nodo de un rol, y en más de una capa: edge (DNS/L4), servicio (L7) y hasta dentro del cluster (service mesh).",
              },
              concepts: [
                "L4 balancea por TCP/UDP (IP y puerto) sin mirar el contenido; L7 mira HTTP: rutas, headers y cookies.",
                "Round-robin reparte en ciclo; least-conn envía a quien menos conexiones abiertas tiene; hash de IP mantiene afinidad.",
                "El health check decide quién está sano: TCP, HTTP /healthz o checks de dependencias reales.",
                "Sticky sessions fijan el nodo del cliente por cookie: útil para sesiones, rompe el balanceo natural.",
                "El LB es un punto único: hay que balancear el propio balancer (VIP + keepalived, DNS, o LB gestionado multi-AZ).",
              ],
              basic: {
                p: "El reparto elemental por puerto: todos los nodos escuchan el mismo puerto y el balancer decide el destino.",
                lang: "nginx",
                code: [
                  "upstream app {",
                  "  least_conn;",
                  "  server 10.0.0.11:8080;",
                  "  server 10.0.0.12:8080;",
                  "  server 10.0.0.13:8080;",
                  "}",
                  "server {",
                  "  listen 80;",
                  "  location /api {",
                  "    proxy_pass http://app;",
                  "    proxy_next_upstream error timeout http_502;",
                  "  }",
                  "}",
                ],
                out: "Reparto con least_conn y fallback al siguiente nodo en error.",
              },
              intermediate: {
                p: "Para rutas públicas, el L7 permite enrutar por path: /api a servicios, /static a CDN, y aplicar límites por cliente.",
                caption: "balanceo por ruta (nginx)",
                lang: "nginx",
                code: [
                  "location /api/  { proxy_pass http://servicios; }",
                  "location /static/ { proxy_pass https://cdn.example.com; }",
                  "location /healthz { return 200; }",
                  "location / {",
                  "  limit_req zone=por_cliente burst=20 nodelay;",
                  "  proxy_pass http://servicios;",
                  "}",
                ],
              },
              real: {
                p: "Un chat de 300k tareas al día balanceaba solo por round-robin y los nodos con sesiones largas (WebSocket) se morían de hambre, mientras el LB 'repartía' equitativamente conexiones de peso muy distinto. Se cambió a least_conn en la capa de sockets y las máquinas se nivelaron de 92 % a 55 % de CPU.",
                ul: [
                  "Poner timeout y max_fails en cada upstream para retirar nodos degradados.",
                  "Separar WebSocket y HTTP en pools porque su perfil de conexión es distinto.",
                  "Balancear por hash de cliente (IP o user_id) cuando la cache local por nodo lo justifique.",
                  "Monitorizar el reparto real: latencia y RPS por nodo, no solo del pool completo.",
                ],
                lang: "nginx",
                code: [
                  "upstream sockets {",
                  "  ip_hash;",
                  "  server 10.0.0.21:4000;",
                  "  server 10.0.0.22:4000;",
                  "}",
                  "location /ws/ {",
                  "  proxy_pass http://sockets;",
                  "  proxy_http_version 1.1;",
                  "  proxy_set_header Upgrade $http_upgrade;",
                  "  proxy_set_header Connection \"upgrade\";",
                  "  proxy_read_timeout 300s;",
                  "}",
                ],
                nota: "La afinidad por hash rompe el balanceo perfecto, pero para conexiones persistentes es el coste correcto.",
              },
              expl: [
                "El algoritmo se elige por el perfil de la conexión: peticiones cortas a least_conn no les importa; sesiones largas lo necesitan.",
                "El health check debe reflejar la salud real: un /healthz que no comprueba la BBDD deja entrar a nodos 'vivos' que fallan todo.",
                "El propio LB se escala: en la nube usa el gestionado (multi-AZ), on-premise con VIP y keepalived.",
                "Las conexiones rápidas de WebSocket hacen que round-robin mida mal la carga real del nodo.",
                "La telemetría por nodo destapa los desequilibrios que el promedio del pool esconde.",
              ],
              errors: [
                "Health check de TCP puro que no detecta que la app está en fail loop.",
                "Timeout cortísimo que escala cualquier lentitud del backend a porcentaje de error del front.",
                "Sticky sessions activadas sin necesidad, impidiendo el rebalanceo natural del pool.",
              ],
              tips: [
                "Diseña /healthz para que compruebe las dependencias críticas con timeout corto propio.",
                "Configura failover con proxy_next_upstream para errores 5xx y timeouts.",
                "Balancea el balancer: gestionado multi-AZ, o VIP con keepalived en dos nodos.",
                "Mide por nodo, no solo por pool: ahí viven los desequilibrios.",
              ],
              exercise: [
                "Diseña el balanceo de una API con 4 nodos y una regla de failover a 5xx.",
                "Elige algoritmo para chat (sesiones largas) y para una web estática.",
                "Escribe el health check de un servicio que depende de BBDD y caché.",
              ],
              summary: [
                "El LB reparte a nodos sanos según el perfil de conexión, no solo en ciclo.",
                "Los health checks reales son tan importantes como el algoritmo de reparto.",
                "Balancea el balancer y monitoriza el reparto por nodo.",
              ],
              lang: "nginx",
              exampleFile: "load-balancing",
              desc: "Reparte tráfico entre nodos con algoritmos y health checks correctos.",
            },
          },
          {
            id: "sharding-y-particionado",
            title: "Sharding y particionado",
            duration: "12 min",
            level: "experto",
            category: "datos",
            cardText: "Cortar la base de datos en pedazos que escalan, y aguantar sus dolores.",
            desc: "Particiona los datos por llave, hazte experto en su redistribución.",
            body: {
              intro: [
                "Cuando una base de datos deja de caber en una máquina o su escritura no da abasto, la jugada estrella del system design es el sharding: cortar los datos en pedazos (shards) y repartirlos entre varias máquinas.",
                "El truco suena simple y es brutal en la práctica: cada shard escribe sola, pero ahora el sistema entero necesita decidir dónde vive cada dato, y la redistribución de shards es donde se comete el 80 % de los errores.",
              ],
              def: {
                par: "Sharding es particionar una base de datos por una llave (shard key) de forma que cada registro caiga en un shard determinado por una función (hash, rango, directorio). Cada shard es una base independiente con sus propios recursos.",
                porQue: "Una sola instancia tiene un techo físico de IOPS y espacio; el sharding reparte escrituras, lecturas y almacenamiento entre N máquinas y escala casi de forma indefinida.",
                cuando: "Úsalo cuando la BBDD supera el tamaño o la tasa de escritura que una máquina gestiona con latencia sana, y después de haber agotado caché, índices y réplicas de lectura.",
              },
              concepts: [
                "Shard key: la columna que decide dónde vive un registro; debe repartir uniforme (evita llaves cardinales como status).",
                "Hash vs rango: hash reparte uniforme y pierde rangos de consulta; rango ganan consultas por rango pero crean shards calientes.",
                "Directorios (index table): un mapa llave→shard permite migrar datos sin que la app conozca el esquema.",
                "El hot shard: una llave popular concentra tráfico y convierte un shard en cuello de botella.",
                "La redistribución (re-sharding): mover llaves entre shards para re-equilibrar carga; los datos deben migrar con consistencia.",
              ],
              basic: {
                p: "Un sharding por hash de user_id mantiene las consultas de un usuario en una sola pieza y reparte el tráfico con uniformidad.",
                lang: "python",
                code: [
                  "SHARDS = 16",
                  "",
                  "def shard_for(user_id: str) -> int:",
                  "    # hash estable: puede cambiar al añadir shards",
                  "    return (hash(user_id) % SHARDS)",
                  "",
                  "def write(key: str, value: str) -> None:",
                  "    s = shard_for(key)",
                  "    conn(s).set(f\"{key}\", value)",
                  "",
                  "def read(key: str) -> str | None:",
                  "    s = shard_for(key)",
                  "    return conn(s).get(f\"{key}\")",
                ],
                out: "user_id → shard; mismo usuario siempre al mismo shard.",
              },
              intermediate: {
                p: "Para evitarte re-hash cuando crezcan los shards, usa hash consistente: la reasignación solo mueve una fracción de las llaves.",
                caption: "hash consistente",
                lang: "python",
                code: [
                  "def confirma(nuevo_shards, llaves):",
                  "    movidos = sum(1 for k in llaves",
                  "                  if shard_for(k, nuevo_shards) != shard_for(k, 16))",
                  "    return movidos",
                  "",
                  "def shard_for(key: str, n: int) -> int:",
                  "    # hash + nodo más cercano en el anillo",
                  "    return (mmh3.hash(key) % n)",
                  "",
                  "# de 16 a 20 shards: ~25% de llaves se mueven y",
                  "# con hash consistente truco del anillo: ~1/n",
                ],
              },
              real: {
                p: "Un sistema de mensajería almacena 400 GB mensuales; una sola réplica moría de IOPS en los picos. Se particionó la BBDD por org_id con 32 shards y un directorio: la app mira la tabla de particiones con caché TTL, la escritura se reparte 32x, y al añadir shard 33 solo se migran las llaves que el mapa asigna al nuevo.",
                ul: [
                  "La llave debe venir en cada petición: el cliente manda org_id o el gateway lo deriva del token.",
                  "Las consultas cruzadas (join entre shards) se evitan: se desnormalizan o se resuelven por aplicación.",
                  "Los hot shards reciben tráfico de rutina: monitoriza el uso por shard para detectarlos.",
                  "Las migraciones de re-sharding se ejecutan en segundo plano con doble escritura y validación.",
                ],
                lang: "sql",
                code: [
                  "-- directorio de particiones (index table)",
                  "CREATE TABLE particiones (",
                  "  llave TEXT PRIMARY KEY,",
                  "  shard_id INTEGER NOT NULL,",
                  "  updated_at TIMESTAMP DEFAULT now()",
                  ");",
                  "-- consulta típica con la llave",
                  "SELECT shard_id FROM particiones WHERE llave = :org_id;",
                ],
                nota: "El directorio evita re-hash globales y permite mover llaves calientes entre shards.",
              },
              expl: [
                "La shard key es la decisión más importante: una mala llave convierte el sharding en un problema peor que el original.",
                "Hash uniforme para escala, rango para consultas ordenadas: no se puede tener ambos sin capa extra.",
                "Las transacciones que cruzan shards o mueren o cargan un coordinador: diseña las llaves para mantener juntos los datos que se consultan juntos.",
                "El re-sharding vive en producción: por eso el directorio y el hash consistente son tus amigos.",
                "La recuperación de un shard concreto debe ser automática: réplicas por shard y backups por shard, no del conjunto.",
              ],
              errors: [
                "Elegir una shard key de baja cardinalidad: 'status' reparte todo el tráfico a 2 de 16 shards.",
                "Hacer joins entre shards y cargar la coordinación en la app en cada consulta.",
                "Poner el re-sharding en vías muertas hasta que el hot shard derribe el servicio.",
              ],
              tips: [
                "Elige una llave de alta cardinalidad y de queries frecuentes (user_id, org_id).",
                "Mantén un directorio y cachéalo con TTL corto para re-equilibrar sin corte.",
                "Backup y réplicas por shard para que un fallo aísle una pieza.",
                "Instrumenta el uso por shard: el calor se detecta antes con números.",
              ],
              exercise: [
                "Elige la shard key de un sistema de pedidos y justifica dónde viven los datos de un usuario.",
                "Simula pasar de 16 a 18 shards y cuenta cuántas llaves se mueven con hash consistente.",
                "Diseña el directorio de particiones con su caché TTL y su migración en caliente.",
              ],
              summary: [
                "El sharding reparte almacenamiento y escritura a cambio de elegir bien la llave.",
                "Hash para uniformidad, rango para ordenar: decide por las consultas.",
                "El re-sharding se hace con directorio y hash consistente, en segundo plano y por pieza.",
              ],
              lang: "python",
              exampleFile: "sharding",
              desc: "Particiona los datos por una llave que reparte uniforme y sobrevive al crecimiento.",
            },
          },
          {
            id: "replicacion-y-redundancia",
            title: "Replicación y redundancia",
            duration: "11 min",
            level: "experto",
            category: "datos",
            cardText: "Copias de datos que dan disponibilidad y descargan lecturas sin romper la verdad.",
            desc: "Replica datos entre nodos y aprende a leer de copias sin mentir.",
            body: {
              intro: [
                "Tener el dato en un solo sitio es el pecado original del system design: ese sitio es un punto de fallo. La replicación pone el mismo dato en varios nodos para sobrevivir a fallos y para descargar al nodo maestro de las lecturas.",
                "El precio es la coordinación: las copias no se sincronizan solas, y entre el momento en que escribes y el momento en que todas las copias lo ven hay una ventana de inconsistencia que debes decidir.",
              ],
              def: {
                par: "La replicación mantiene copias de los mismos datos en varios nodos: un primario acepta escrituras y los réplicas reciben los cambios (síncronos o asíncronos). Las réplicas pueden servir lecturas y ser promovidas si el primario cae.",
                porQue: "Sin réplicas, un fallo del disco o del nodo es una caída de servicio y posible pérdida de datos; con réplicas, la lectura escala y la promoción de un réplica acota la ventana de indisponibilidad.",
                cuando: "Replica siempre que la disponibilidad importe. Sincronizá lo crítico (reservas, saldos) y mantené asíncronas las copias de lectura secundarias.",
              },
              concepts: [
                "Primario/réplica (replication lag): la diferencia de estado entre el nodo que escribe y el que aún no ha aplicado el cambio.",
                "Síncrono vs asíncrono: el síncrono espera confirmación de la copia (menos pérdida, más latencia); el asíncrono no espera (rápido, con ventana de pérdida).",
                "Promoción: si el primario muere, una réplica asciende; para evitar la 'cerebro partido' (split-brain), dos primarios escribiendo a la vez es el desastre del consensus.",
                "Failover automático: detectar por quórum y consensus que el primario no responde y nombrar nuevo líder.",
                "Las lecturas desde réplica sirven datos viejos por naturaleza: elige por caso si la lectura puede tolerar lag.",
              ],
              basic: {
                p: "El flujo mínimo de una BBDD con réplica de lectura: la escritura va al primario, las lecturas se reparten entre las copias.",
                lang: "yaml",
                code: [
                  "postgresql:",
                  "  primary: pg-01",
                  "  replicas:",
                  "    - pg-02   # async",
                  "    - pg-03   # async",
                  "  failover:",
                  "    trigger: quorum 2/3",
                  "    promote: pg-02",
                  "  reads:",
                  "    pool: [pg-02, pg-03]",
                  "    primary_for: [escrituras, \"e\/s fuertes\"]",
                ],
                out: "Escrituras al primario; lecturas en pool de réplicas.",
              },
              intermediate: {
                p: "La promoción con quórum evita el split-brain: solo se convierte en primario quien convence a la mayoría de que el antiguo no responde.",
                caption: "consensus con 3 votantes (etcd)",
                lang: "bash",
                code: [
                  "# 3 nodos de consensus (etcd)",
                  "etcdctl endpoint health cluster --endpoints=\\\"http://etcd-1:2379,http://etcd-2:2379,http://etcd-3:2379\\\"",
                  "# resultado: 3 health",
                  "# el candidato a líder necesita 2 de 3 votos",
                  "etcdctl election new primary --election-timeouts=1000",
                ],
              },
              real: {
                p: "Un juego online quisquilloso: las partidas escribían al primario y los perfiles públicos se leían de réplicas. En un pico, el replication lag subió a 4 segundos y los usuarios veían 'puntos gastados' como si nunca hubieran existido. La solución fue dual: las lecturas de saldo (críticas) vuelven al primario con 'read-after-write', y las lecturas de perfil (tolerantes) se quedan en las réplicas.",
                ul: [
                  "Clasifica lecturas: críticas (read-after-write al primario) vs tolerantes a lag (réplicas).",
                  "Da a cada rol recursos distintos: replica de búsqueda pesada y replica rápida de lectura.",
                  "La promoción manual solo en emergencias; lo normal es failover automático con quórum.",
                  "Monitoriza el replication lag como se vigila un sensor: cada segundo de lag es una mentira visible.",
                ],
                lang: "python",
                code: [
                  "def get_balance(user_id: str) -> int:",
                  "    # lectura crítica, consistente",
                  "    return primary_client.get(f\"balance:{user_id}\")",
                  "",
                  "def get_profile(user_id: str) -> dict:",
                  "    # lectura tolerante a lag",
                  "    return replica_pool.get(f\"profile:{user_id}\", read_preference=\"secondary\")",
                ],
                nota: "El read-after-write: quien acaba de escribir exige leer lo escrito (su propia copia), aunque sea volviendo al primario.",
              },
              expl: [
                "La replicación no es backup: una borrada masiva o un corrompido se replica también. Necesitas backups, no solo copias.",
                "El lag no es un bug: es el coste asumido de la disponibilidad y del rendimiento de lecturas.",
                "El split-brain es el desastre: dos primarios aceptando escrituras corrompen el dataset; el quórum evita nombrar líderes a la vez.",
                "La promoción síncrona protege los últimos commits; la asíncrona pierde una ventana pequeña en el failover.",
                "El consensus (Raft/Paxos) es la decisión: quién es el líder, y la config que todos respetan.",
              ],
              errors: [
                "Asumir que la réplica tiene los datos y no monitorear el replication lag.",
                "Failover manual a medianoche sin quórum, dejando dos primarios escribiendo.",
                "Leer del réplica datos que el usuario acaba de escribir y mostrar una mentira.",
              ],
              tips: [
                "Separa lectura crítica (primario) de lectura tolerante (réplica) con reglas explícitas.",
                "Automatiza el failover con quórum y consenso; el manual solo de emergencia.",
                "Alerta cuando el lag supere N segundos, no cuando ya se vea en el producto.",
                "Copia de copias: backup snapshot + WAL también para las réplicas.",
              ],
              exercise: [
                "Clasifica las lecturas de tu app en críticas y tolerantes a lag.",
                "Diseña el failover de un clúster de 3 nodos sin split-brain.",
                "Escribe la regla read-after-write para saldo de una app de pagos.",
              ],
              summary: [
                "Las réplicas dan disponibilidad y descargan lecturas a cambio de lag.",
                "El quórum evita el split-brain en los failover automáticos.",
                "Replica no es backup, y cada lectura tiene que elegir su tolerancia al lag.",
              ],
              lang: "yaml",
              exampleFile: "replicacion",
              desc: "Mantén copias del dato para sobrevivir fallos sin que las copias mientan.",
            },
          },
          {
            id: "backpressure-y-limites-de-rendimiento",
            title: "Backpressure y límites de rendimiento",
            duration: "10 min",
            level: "experto",
            category: "resiliencia",
            cardText: "Cuando la cola se llena: el sistema debe decir 'no' con elegancia.",
            desc: "Limita lo que entra y degrada con control cuando el sistema va al límite.",
            body: {
              intro: [
                "Todo sistema tiene un techo de rendimiento, y el tráfico no respeta techos. Cuando la demanda supera la capacidad, pasa una de dos: el sistema dice 'no' elegante (degradación, rechazo rápido) o se atasca acumulando trabajo hasta caerse.",
                "La backpressure es la disciplina de no aceptar más de lo que puedes procesar: el truco es que el rechazo sea el comportamiento planeado, no el síntoma de un fallo.",
              ],
              def: {
                par: "La backpressure (contrapresión) hace que un consumidor lento limite al productor: autorregulación con control de flujo, límites de concurrencia o rechazo temprano cuando el buffer se llena.",
                porQue: "Si el productor lanza sin límite, las colas crecen sin tope, la memoria se llena y el sistema entero muere lejos del punto del problema. Rechazar temprano es años más barato que caerse más tarde.",
                cuando: "Aplica límites en toda frontera: rate limiting por cliente, límite de workers y colas acotadas, timeout en cada llamada externa y rechazo 503 temprano cuando el sistema satura.",
              },
              concepts: [
                "El buffer siempre se llena: la pregunta no es si, es qué pasa después (bloquea, descarta, rechaza con 429/503).",
                "Timeout en cada espera: sin él, una dependencia lenta congela workers que deberían atender a otros.",
                "Límite de concurrencia: cap de workers o semáforos para que las tareas no se multipliquen sin freno.",
                "Rate limiting: por cliente/módulo, en su propia capa, decide quién entra antes de gastar recursos.",
                "Rechazo temprano con señal explícita (429, 503, retry-after): el cliente sabe que debe esperar.",
              ],
              basic: {
                p: "Una cola acotada con descarte explícito: cuando está llena, la petición recibe un 503 claro en vez de acumularse.",
                lang: "python",
                code: [
                  "from collections import deque",
                  "",
                  "MAX = 1000",
                  "queue = deque(maxlen=MAX)",
                  "",
                  "def enqueue(task):",
                  "    if len(queue) >= MAX:",
                  "        raise TooBusy(\"503 · cola llena\", retry_after=2)",
                  "    queue.append(task)",
                  "",
                  "def worker():",
                  "    task = queue.popleft()",
                  "    process(task)",
                ],
                out: "Cola llena → 503 con retry-after, no OOM.",
              },
              intermediate: {
                p: "El patrón de confiabilidad en nubes: el cliente espera períodos de 'busy' y los respeta con backoff, mientras la cola acotada protege al servicio.",
                caption: "cliente con backoff",
                lang: "python",
                code: [
                  "import time",
                  "",
                  "for intent in range(5):",
                  "    try:",
                  "        return client.enqueue(task)",
                  "    except TooBusy as e:",
                  "        time.sleep(min(intent * 0.5, e.retry_after))",
                  "",
                  "# 5 intentos, respetando el retry-after del servidor",
                ],
              },
              real: {
                p: "Una API de imágenes (3k req/s) recibió una campaña que la duplicó en diez minutos. La BBDD de cola se saturó, las peticiones se acumularon y el backend cayó con OOM. La corrección: límite de concurrencia en los workers, cola acotada con descarte, rate limit por API-key y 503 con Retry-After. Los usuarios vieron error controlado un rato en vez de una caída total de 40 minutos.",
                ul: [
                  "Cola acotada + descarte explícito antes de que la memoria decida por ti.",
                  "Rate limit por cliente en la capa de entrada con bustsize controlado.",
                  "Workers con semáforo y timeout por tarea para que una lenta no ocupe la flota.",
                  "503 con Retry-After para que los clientes respeten la recuperación.",
                ],
                lang: "nginx",
                code: [
                  "limit_req_zone $binary_remote_addr zone=por_ip:10m rate=20r/s;",
                  "limit_req_zone $http_x_api_key   zone=por_key:10m rate=200r/s;",
                  "server {",
                  "  location /api/ {",
                  "    limit_req zone=por_key burst=50 nodelay;",
                  "    limit_req_status 429;",
                  "    add_header Retry-After 5 always;",
                  "    proxy_pass http://api_pool;",
                  "  }",
                  "}",
                ],
                nota: "El 429/503 con Retry-After convierte el rechazo en un mecanismo legible por los clientes.",
              },
              expl: [
                "La cola es un amortiguador, no un almacén: un tamaño grande esconde el colapso hasta que el tiempo de espera mata la experiencia.",
                "Rechazo temprano localiza el problema: la petición que no puede atenderse se cae en la frontera, no en la dependencia profunda.",
                "El rate limit protege a todo el sistema de un solo cliente transversal y del spam accidental.",
                "Sin timeout, un worker bloqueado eternamente es capacidad muerta: cada tarea tiene su presupuesto.",
                "El sistema degradado debe ser visible en métricas: tasa de 429/503 y longitud de la cola son tus sensores.",
              ],
              errors: [
                "Cola sin límite que llena la memoria y mata el proceso con OOM a las 3 de la mañana.",
                "Retries infinitos del cliente que multiplican la carga justo cuando el servidor quiere recuperarse.",
                "Sin rate limit, un cliente iterando acelerado come la flota de workers del resto.",
              ],
              tips: [
                "Dimensiona la cola por el peor caso de duración de pico, no por el promedio.",
                "Usa 429 para límites de tasa y 503 para capacidad; ambos con Retry-After.",
                "Los workers llevan timeout propio y límite de reintentos acotado.",
                "Alerta sobre longitud de cola y request rate, no esperes al OOM.",
              ],
              exercise: [
                "Diseña una cola acotada de notificaciones con rechazo 503.",
                "Configura rate limit por API key para una API de 5 clientes grandes.",
                "Escribe el retry con backoff de un cliente que respeta Retry-After.",
              ],
              summary: [
                "El sistema debe decir 'no' con 429/503 y Retry-After cuando satura.",
                "Cola acotada, workers con timeout y rate limit por cliente forman la red de seguridad.",
                "La degradación planeada es mejor que la caída accidental.",
              ],
              lang: "python",
              exampleFile: "backpressure",
              desc: "Protege la capacidad con límites, colas acotadas y rechazo temprano.",
            },
          },
        ],
      },
      {
        id: "modulo-05",
        n: 5,
        t: "Caching y datos a escala",
        level: "avanzado",
        caption: "El mismo dato calculado mil veces es trabajo desperdiciado: caché, CDN y elección de almacén.",
        desc: "Cachés en cada capa, CDN, invalidación y la decisión de qué base de datos guarda qué.",
        subtitle: "La latencia que se fabrica y la latencia que se ahorra.",
        objectives: [
          "Aplicar las estrategias de caché clásicas sin invalidar en falso",
          "Desplegar CDN para contenido inmutable y geográficamente repetido",
          "Resolver la invalidación de caché con TTL, versionados y eventos",
          "Elegir el almacén adecuado según la forma de los datos",
          "Migrar esquemas en producción sin cortes",
        ],
        lessons: [
          {
            id: "estrategias-de-cache",
            title: "Estrategias de caché",
            duration: "11 min",
            level: "avanzado",
            category: "caching",
            cardText: "Cache-aside, write-through, write-back: dónde vive la copia y quién la actualiza.",
            desc: "Los patrones de caché que deciden qué se sirve y cuándo se actualiza.",
            body: {
              intro: [
                "El mismo dato líder (leído de la BBDD) consultado mil veces seguidas es mil accesos a disco e IOPS que se pueden ahorrar. La caché pone una copia rápida entre el cliente y el dato original.",
                "La magia no es la caché, es la estrategia: dónde vives la copia, quién la puede invalidar y qué pasa cuando el dato cambia son diseño, no configuración.",
              ],
              def: {
                par: "Cache-aside (lazy): la app busca en caché, y si no está, lee del origen y la rellena. Write-through: cada escritura actualiza origen y caché a la vez. Write-back: la escritura va solo a la caché y se vacía al origen después.",
                porQue: "Cache-aside es simple y alucinante en lecturas; los write-through/write-back mueven la complejidad a la escritura pero eliminan lecturas frías tras cada actualización.",
                cuando: "Empieza con cache-aside y TTL; evoluciona a write-through cuando el relleno en caliente genere lecturas frías a BBDD (thundering herd), y evalúa write-back solo si toleras perder una ventana de escrituras en la caché.",
              },
              concepts: [
                "Hit/miss: la caché acierta o falla; el ratio de acierto es la métrica que manda.",
                "Thundering herd (manada): mil peticiones del mismo dato frío golpeando el origen a la vez; se arregla con single-flight o bloqueo de relleno.",
                "Write-through: la escritura asegura que la copia y el origen no divergen; útil para datos que se leen apenas se escriben.",
                "Write-back: baja la latencia de escritura y el golpe al origen, pero la pérdida de la caché es pérdida de los últimos writes.",
                "El TTL es la válvula: un TTL largo sirve datos viejos; corto, sobrecarga el origen. La duración depende del caso, no del gusto.",
              ],
              basic: {
                p: "Cache-aside con TTL: patrón estándar que resuelve el 80 % de los casos de lectura.",
                lang: "python",
                code: [
                  "CACHE = redis.StrictRedis()",
                  "",
                  "def get_profile(user_id):",
                  "    key = f\"profile:{user_id}\"",
                  "    val = CACHE.get(key)",
                  "    if val is not None:",
                  "        return json.loads(val)",
                  "    rows = db.fetch(\"SELECT * FROM profiles WHERE id=%s\", user_id)",
                  "    if not rows: return None",
                  "    CACHE.setex(key, TTL, json.dumps(rows[0]))",
                  "    return rows[0]",
                ],
                out: "Miss → BBDD, rellena con TTL; hit siguiente → directo de Redis.",
              },
              intermediate: {
                p: "La actualización consistente (write-through): la escritura actualiza el origen y luego (o en paralelo) la caché, evitando que la caché devuelva el valor anterior.",
                caption: "write-through",
                lang: "python",
                code: [
                  "def update_nickname(user_id, nick):",
                  "    db.execute(\"UPDATE profiles SET nickname=%s WHERE id=%s\", nick, user_id)",
                  "    # invalidar la copia en caché (evita lecturas viejas)",
                  "    CACHE.delete(f\"profile:{user_id}\")",
                  "    # o write-through: reescribir la copia al momento",
                  "    CACHE.setex(f\"profile:{user_id}\", TTL, json.dumps({\"nickname\": nick}))",
                ],
              },
              real: {
                p: "Una página de catálogo de 200k productos: con cache-aside y TTL de 60s, la BBDD bajó de 4.000 lecturas/s a 250, y el thundering herd del relanzamiento de producto a las 9 am se controló con single-flight (una sola petición de relleno por clave, las demás esperan esa). El p95 de la página bajó de 380 ms a 12 ms.",
                ul: [
                  "Single-flight por clave para no golpear el origen con la manada de un dato frío.",
                  "Expiraciones escalonadas: no hagas que todos los TTL expiren a la vez y el origen reciba el golpe completo.",
                  "Caché de respuesta completa (página/JSON) y caché de partes por separado.",
                  "Monitoriza hit-ratio por ruta y ajusta TTL por dato, no de forma global.",
                ],
                lang: "python",
                code: [
                  "import asyncio",
                  "",
                  "inflight = {}",
                  "",
                  "async def get_or_fetch(key):",
                  "    val = CACHE.get(key)",
                  "    if val is not None: return val",
                  "    fut = inflight.get(key)",
                  "    if fut is not None:",
                  "        return await fut  # espera el relleno en curso",
                  "    fut = asyncio.create_task(fill_from_db(key))",
                  "    inflight[key] = fut",
                  "    try:",
                  "        return await fut",
                  "    finally:",
                  "        inflight.pop(key, None)",
                ],
                nota: "El single-flight convierte N mises simultáneos en una sola ida al origen.",
              },
              expl: [
                "El ratio de acierto es una función de la localidad temporal: lo que se pide, se vuelve a pedir; cacha eso.",
                "Write-through simplifica la lectura pero duplica la escritura; úsalo con datos leídos enseguida de escribirse.",
                "Write-back es un trade-off de durabilidad: si la caché muere, se van las escrituras pendientes.",
                "El TTL corto no es una caché pobre, es coherencia configurable: elige por dato.",
                "La invalidación por evento (notificar a la caché cuando el origen cambia) conserva el acierto y evita ventanas sucias.",
              ],
              errors: [
                "Devolver de caché datos que cambiaron hace 10 minutos por TTL gigante y no invalidar.",
                "Rellenar la caché en el miss sin single-flight y transformar cada pico en golpes al origen.",
                "Poner todo en caché sin preguntar si el dato cambia y quién lee la versión nueva.",
              ],
              tips: [
                "Define el objetivo: latencia, carga del origen o coste, y mide el ratio que lo refleja.",
                "Invalidación por evento para datos que cambian a menudo y TTL para los que cambian poco.",
                "Caché de datos y de fragmentos: perfila la página al completo y sus piezas.",
                "Escalona expiraciones y usa single-flight en los rellenos críticos.",
              ],
              exercise: [
                "Implementa cache-aside con TTL y single-flight en tu lenguaje.",
                "Diseña write-through para el perfil de un usuario y escribe el orden de actualización.",
                "Elige TTL e invalidación para un feed de precios que cambia cada 5 segundos.",
              ],
              summary: [
                "Cache-aside con TTL y single-flight resuelve la mayoría de las lecturas.",
                "Write-through y write-back mueven la complejidad a la escritura y cambian la durabilidad.",
                "El TTL y la invalidación son diseño del caso, no una constante global.",
              ],
              lang: "python",
              exampleFile: "estrategias-de-cache",
              desc: "Elige la estrategia de caché por cómo se leen y escriben tus datos.",
            },
          },
          {
            id: "cdn-y-contenido-estatico",
            title: "CDN y contenido estático",
            duration: "10 min",
            level: "avanzado",
            category: "cdn",
            cardText: "Viaja una vez, sírvete mil veces: la copia estática en el borde.",
            desc: "Cómo distribuir contenido inmutable cerca del usuario con una CDN.",
            body: {
              intro: [
                "Un recurso estático (logo, JS, video) es el mismo byte que viaja miles de veces. La CDN (Content Delivery Network) lo copia en cientos de puntos de presencia (PoP) y el usuario descarga de la réplica más cercana.",
                "La regla de oro detrás de toda CDN es la misma del caching: el contenido debe ser identificable por su versión. Si el byte no cambia de nombre cuando cambia de contenido, la CDN te servirá la versión vieja.",
              ],
              def: {
                par: "Una CDN es una red de servidores perimetrales (edge) que cachean réplicas de contenido estático y lo sirven desde el PoP más cercano al usuario, cortando latencia y tráfico al origen.",
                porQue: "Cada milisegundo de descarga cuenta y cada TB servido desde el origen cuesta. Con la CDN no solo ganas velocidad: proteges el origen porque el pico de lectura lo absorbe el borde.",
                cuando: "Úsala para todo lo inmutable y público: JS/CSS/imágenes/video/APIs GET de lectura. No metas en ella datos privados por usuario sin firmar las URLs y cuidar la expiración.",
              },
              concepts: [
                "Cache veronada: una URL = una versión; los hashes en el filename hacen que el contenido nuevo tenga URL nueva.",
                "Cache-control y TTL del borde: max-age, immutable, y s-maxage si hay una red intermedia.",
                "Invalidación por purga o por versionado: purgar es factible; versionar es lo robusto.",
                "Signed URLs / tokens para contenido privado que la CDN sirve solo a quien autorizamos.",
                "Los orígenes sanos también importan: la CDN rellena de tu origen y la caché solo ayuda si el cópia está bien hecha.",
              ],
              basic: {
                p: "Cabeceras que le dicen al borde (y al navegador) cuánto tiempo conservar cada fichero.",
                lang: "yaml",
                code: [
                  "headers:",
                  "  /static/*:",
                  "    Cache-Control: \"public, max-age=31536000, immutable\"",
                  "  /api/*:",
                  "    Cache-Control: \"no-store\"",
                  "",
                  "# inmutable: el nombre del fichero lleva el hash",
                  "# app.8f3a2b.js y no managers.js cualquiera",
                ],
                out: "Estático con hash: 1 año de caché. API: nunca en caché.",
              },
              intermediate: {
                p: "Para contenido privado, la CDN debe poder exigir autorización: URLs firmadas con expiración.",
                caption: "URL firmada",
                lang: "bash",
                code: [
                  "# generar URL firmada (ejemplo S3 + CloudFront)",
                  "TOKEN=$(openssl dgst -sha256 -hmac \"$SECRET\" -binary <<< \"videos/leccion-7.mp4 1710000000\" | base64)",
                  "echo \"https://cdn.example.com/videos/leccion-7.mp4?Expires=1710000000&Signature=$TOKEN\"",
                ],
              },
              real: {
                p: "Una plataforma de cursos geolocalizada: la misma lección en video se pedía 300k veces al día, el origen ardía en ancho de banda y los alumnos de Europa sufrían 900 ms de carga. Con la CDN se sirvió el video desde PoPs locales (latencia 120 ms) y el origen pasó de servir bytes a servir solo la primera copia de cada video nuevo.",
                ul: [
                  "Hash el nombre de cada build: app.<hash>.js para que el versionado invalide solo.",
                  "Firma las URLs de cursos de pago y expira con la sesión.",
                  "Cachea también los GET de API de lecturas públicas (catálogo, precios) en el borde.",
                  "Mide la cobertura geográfica: la CDN gana según dónde están tus usuarios.",
                ],
                lang: "nginx",
                code: [
                  "location /static/ {",
                  "  # el filename incluye el hash del contenido",
                  "  expires 1y;",
                  "  add_header Cache-Control \"public, immutable\";",
                  "  proxy_pass https://origencdn.example.com;",
                  "}",
                  "location /api/catalog {",
                  "  proxy_cache_valid 200 60s;",
                  "  proxy_pass http://api;",
                  "}",
                ],
                nota: "Versionar el nombre es la invalidación que nunca falla: lo viejo muere solo.",
              },
              expl: [
                "El paralelismo de la CDN y el tiempo de llegada: el PoP más cercano elimina rondas de ida y vuelta largas.",
                "La coherencia importa más en dinámico que en estático: por eso la CDN se reserva para lo inmutable y las APIs públicas con TTL corto.",
                "El ahorro de coste llega de mover el tráfico del origen al borde: bytes que ya no cruzan tu infra.",
                "Las URLs firmadas extienden la CDN al contenido protegido: el control está en quién sabe firmar.",
                "El cold-start es el enemigo: un contenido popular recién publicado golpea el origen una vez por PoP; pre-cachear lo predecible evita la manada de bordes.",
              ],
              errors: [
                "Inmutabilidad prometida sin hash en el nombre: cambias el CSS y el usuario sigue viendo el viejo del cache.",
                "Cache-Control inmutable en ficheros que no llevan versión y que cambian en cada deploy.",
                "Firmar todo el contenido público por igual y complicar el borde sin necesidad.",
              ],
              tips: [
                "Versiona con hash los estáticos y decláralos immutable por un año.",
                "Usa no-store para cualquier respuesta con datos privados por usuario.",
                "Firma URLs solo en contenido protegido y rota el secreto.",
                "Pre-cachea los hitos predecibles (lanzamientos) para que el borde no golpee el origen de golpe.",
              ],
              exercise: [
                "Diseña el scheme de nombres con hash para un build de una SPA.",
                "Configura cabeceras de caché para estático, API pública y API privada.",
                "Genera y verifica una URL firmada con expiración para un video.",
              ],
              summary: [
                "La CDN corta latencia y protege el origen moviendo bytes cerca del usuario.",
                "Versionar el nombre es la invalidación robusta; la purga es la manual.",
                "Contenido privado: URL firmada y expiración, no caché pública.",
              ],
              lang: "yaml",
              exampleFile: "cdn-y-contenido-estatico",
              desc: "Distribuye estáticos y APIs públicas en el borde con versionado y firmas.",
            },
          },
          {
            id: "invalidacion-de-cache",
            title: "Invalidación y consistencia",
            duration: "10 min",
            level: "avanzado",
            category: "caching",
            cardText: "El dato cambió: cómo hacer que la caché se entere sin borrarla toda.",
            desc: "Mecanismos para que la copia en caché no sobreviva al dato real.",
            body: {
              intro: [
                "Hay dos tipos difíciles de problemas en computación: los de cache invalidation, y los que acaban en cache invalidation. La caché acierta porque vive cerca y rápido; pero cuando el dato real cambia, la copia esconde la verdad.",
                "Invalidar no es borrar: es decidir, por cada pieza, qué umbral de antigüedad aceptas, quién se entera del cambio y cuánto cuesta el aviso.",
              ],
              def: {
                par: "La invalidación de caché elimina o refresca una entrada cuando el dato original cambia. Se hace por TTL (expirado natural), por evento (notificación del origen) o por versionado (nueva clave por versión).",
                porQue: "Una caché sin invalidación sirve datos viejos en silencio: ux rota, saldos falsos, stock inventado. El reto es equilibrar el coste del aviso (mensajes, escrituras extra) con la frescura que el caso exige.",
                cuando: "Combina TTL como red de seguridad y eventos para los datos sensibles a la frescura (precio, estado, saldo). Versiona cuando la lectura supera muchísimo a la escritura.",
              },
              concepts: [
                "TTL como techo: la entrada muere al caducar aunque nadie la borre; barato y simple.",
                "Invalidación por evento: al escribir, el origen publica (Redis pub/sub, cola) y el/los consumidor de caché borra o refresca.",
                "Versionado del dato: la clave lleva la versión (profile:42:v3); cambios = versión nueva, lo viejo caduca solo por TTL.",
                "La ventana de inconsistencia es el intervalo entre el cambio y la invalidación; debe ser menor que lo que el negocio tolera.",
                "Invalidar la caché mientras se escribe el origen crea carreras: el orden y la transaccionalidad importan.",
              ],
              basic: {
                p: "Invalidación por evento con pub/sub: cada escritura avisa y la caché borra la clave afectada.",
                lang: "python",
                code: [
                  "import redis",
                  "",
                  "r = redis.StrictRedis()",
                  "",
                  "# en el servicio que escribe",
                  "def update_profile(user_id, data):",
                  "    db.update(\"profiles\", user_id, data)",
                  "    key = f\"profile:{user_id}\"",
                  "    r.delete(key)          # borrar ya",
                  "    r.publish(\"invalida\", key)  # o avisar",
                  "",
                  "# consumidor: un worker suscrito",
                  "ps = r.pubsub()",
                  "ps.subscribe(\"invalida\")",
                  "for msg in ps.listen():",
                  "    if msg[\"type\"] == \"message\":",
                  "        r.delete(msg[\"data\"].decode())",
                ],
                out: "Cada escritura borra la copia; la siguiente lectura rellena fresca.",
              },
              intermediate: {
                p: "Versionado por modificación global (generación): una versión por dataset evita iterar llave por llave.",
                caption: "invalidación por generación",
                lang: "python",
                code: [
                  "def current_gen():",
                  "    return int(r.get(\"catalog:gen\") or 0)",
                  "",
                  "def get(product_id):",
                  "    gen = current_gen()",
                  "    key = f\"catalogv{gen}:{product_id}\"",
                  "    val = r.get(key)",
                  "    if val is not None: return json.loads(val)",
                  "    rows = db.fetch(\"SELECT * FROM catalog WHERE id=%s\", product_id)",
                  "    r.setex(key, TTL, json.dumps(rows[0]))",
                  "    return rows[0]",
                  "",
                  "def bump_generation():",
                  "    r.incr(\"catalog:gen\")  # todo lo viejo caduca con su TTL",
                ],
              },
              real: {
                p: "El inventario de una tienda con caché de 60s mostraba stock antiguo: al comprar, la página seguía vendiendo el producto agotado. Se pasó el inventario a invalidación por evento (al confirmar el pedido se borra la clave stock:SKU y la próxima lectura repinta desde origen) y las ventas fantasma desaparecieron, manteniendo la optimización de las lecturas.",
                ul: [
                  "Invalidación por evento para lo que cambia por acción de usuario (stock, saldo, estado).",
                  "TTL corto como red de seguridad para catch-all de fallos de aviso.",
                  "El orden escritura-origen→aviso debe ser atómico o el aviso puede llegar antes.",
                  "Prueba la consistencia con cargas: una invalidación que falla silenciosa es una caché sucia permanente.",
                ],
                lang: "python",
                code: [
                  "def confirm_order(sku, qty):",
                  "    # 1) transacción del origen",
                  "    with db.transaction():",
                  "        db.execute(\"UPDATE stock SET units=units-%s WHERE sku=%s\", qty, sku)",
                  "        # 2) invalidar la copia (después del commit)",
                  "        r.delete(f\"stock:{sku}\")",
                  "",
                  "def get_stock(sku):",
                  "    v = r.get(f\"stock:{sku}\")",
                  "    if v is not None: return int(v)",
                  "    n = db.fetch(\"SELECT units FROM stock WHERE sku=%s\", sku)[0][0]",
                  "    r.setex(f\"stock:{sku}\", 5, n)  # TTL corto de seguridad",
                  "    return n",
                ],
                nota: "Invalidar después del commit para que una lectura no vea el origen a medias.",
              },
              expl: [
                "El TTL solo es suficiente cuando la frescura tolera el retraso máximo (tu caso define el TTL).",
                "La invalidación por evento obtiene frescura casi inmediata a cambio de acoplar el origen al sistema de caché.",
                "Versionar es barato para datasets que cambian en ráfagas (catálogos por release) y caro de coordinar si cambia mucho.",
                "El manejo de carreras: invalidar antes de escribir deja un hueco donde la lectura vieja repinta la copia vieja; el orden y la serialización son diseño.",
                "La observabilidad de invalidación: cuenta avisos fallidos y edades de entradas para detectar caché sucia.",
              ],
              errors: [
                "TTL de 24h para un stock que cambia cada minuto, sin eventos.",
                "Invalidar por evento SIN red de seguridad TTL: un aviso perdido = dato viejo para siempre.",
                "Borrar la caché completa en cada cambio (rechazo de todos) y crear un thundering herd global.",
              ],
              tips: [
                "Design: TTL como red, eventos bajo demanda, versionado para ráfagas.",
                "Ordena: origen commit → aviso → (opcional) repintado anticipado.",
                "No borres todo por un cambio pequeño: borra/actualiza las llaves afectadas.",
                "Mide la edad de los datos servidos: la 'cache age' es tu sensor de coherencia.",
              ],
              exercise: [
                "Diseña la invalidación de stock con evento y TTL de seguridad.",
                "Implementa versionado por generación para un catálogo que se actualiza en lanzamientos.",
                "Escribe el diagrama de carreras: qué pasa si invalidás antes del commit.",
              ],
              summary: [
                "El TTL es el techo; los eventos traen frescura; el versionado resuelve ráfagas.",
                "Invalidar después del commit y con red de seguridad TTL.",
                "La edad de la caché medida es la prueba de que tu coherencia funciona.",
              ],
              lang: "python",
              exampleFile: "invalidacion-de-cache",
              desc: "Haz que la copia en caché se entere del cambio en el origen y en el tiempo justo.",
            },
          },
          {
            id: "eleccion-de-base-de-datos",
            title: "Cómo elegir base de datos",
            duration: "11 min",
            level: "avanzado",
            category: "datos",
            cardText: "SQL, NoSQL o especializada: la forma de tus datos decide el motor.",
            desc: "Un mapa para elegir entre los mundos de almacenamiento según tu carga.",
            body: {
              intro: [
                "Elegir base de datos es elegir la pelea de mañana: cada motor brilla en un perfil de carga y sangra en el contrario. El error clásico es elegir por moda o por 'ya lo conozco' y pagar la deuda en el pico.",
                "La clave es hacer preguntas de carga: ¿Relaciones complejas o documentos? ¿Lecturas de agregación? ¿Escrituras masivas y apuntando a un solo tamaño? Las respuestas descartan motores con elegancia.",
              ],
              def: {
                par: "La elección de motor se basa en la forma de los datos (relacional, documento, clave-valor, columna, búsqueda), el tipo de acceso (joins, punto, rango, agregación) y la escala de escritura/lectura.",
                porQue: "Un motor relacional modela transparencia y consistencia, pero se complica en escala de escritura; un documental es flexible y cómodo pero castiga joins; el clave-valor es la bala para cachés pero casi sin consultas.",
                cuando: "Usa SQL como default y baja a motores especializados cuando la forma o la escala lo exige: Redis para caché/sesiones, Elasticsearch para búsqueda, columnares para análisis, vectoriales para ML.",
              },
              concepts: [
                "Relacional (Postgres, MySQL): consistencia, joins, transacciones ACID. El default correcto.",
                "Documento (Mongo, DynamoDB): flexibilidad de esquema y escala horizontal; joins fuera de casa.",
                "Clave-valor (Redis): latencias < 1 ms, perfecto como capa de caché y sesiones; no para consultas complejas.",
                "Columna (BigQuery, ClickHouse): análisis y agregaciones sobre tablas enormes.",
                "Abstractora: miden lo mismo (CAP, consistencia, escala); lo que cambia es qué sacrificas por defecto.",
              ],
              basic: {
                p: "La decisión en servilleta: si la carga es transaccional y con relaciones → SQL; si es millones de lecturas por clave → Redis; si es agregación de logs → columnar.",
                lang: "text",
                code: [
                  "CASO                    | MOTOR SENSATO      ",
                  "pedidos + inventario     | PostgreSQL (ACID)  ",
                  "sesiones y contadores    | Redis / Memcached  ",
                  "catálogo flexible        | DynamoDB/Mongo     ",
                  "logs y métricas          | ClickHouse/BigQuery",
                  "búsqueda full-text       | Elasticsearch      ",
                  "grafos                   | Neo4j              ",
                ],
                out: "La forma del dato y del acceso elige el motor.",
              },
              intermediate: {
                p: "Los motores conviven: la clave es separar responsabilidades y duplicar lo necesario (la especialización cuesta consistencia).",
                caption: "persistencia políglota",
                lang: "yaml",
                code: [
                  "almacenes:",
                  "  postgres:   # fuente de verdad",
                  "    modelos: [pedidos, usuarios, inventario]",
                  "  redis:      # sesión + caché de catálogo",
                  "    TTL: true",
                  "  search:     # réplica optimizada",
                  "    engine: Elasticsearch",
                  "    sincroniza: eventos(catálogo)",
                  "  analytics:",
                  "    engine: ClickHouse",
                  "    cargador: export nightly",
                ],
              },
              real: {
                p: "Un marketplace migró su buscador de PostgreSQL a Elasticsearch porque el 'LIKE %texto%' sobre 40 millones de filas tardaba 900 ms. La fuente de verdad siguió en Postgres; cada cambio de producto publica un evento que Elasticsearch consume; las búsquedas pasaron a 15 ms y el coordinador de facetas se resolvió en el motor de búsqueda.",
                ul: [
                  "Mantén una sola fuente de verdad y deja que los especializados sean réplicas dirigidas por eventos.",
                  "El precio de la réplica es consistencia eventual: mide cuánto tarda en verse un cambio.",
                  "Delimita responsabilidades: búsqueda al buscador, sesión al KV, análisis al columnar.",
                  "Revisa la elección cuando el cambio de volumen acerque un motor a su punto de dolor.",
                ],
                lang: "bash",
                code: [
                  "# productor: cada alta publica al topic del catálogo",
                  "kafka-topics.sh --create --topic catalog-events --partitions 8",
                  "# consumidor: Elasticsearch se suscribe y reindexa",
                  "kafka-console-consumer.sh --topic catalog-events \\",
                  "  --from-beginning | indexer --into search",
                ],
                nota: "La réplica especializada vale por resolver una lectura concreta; no por duplicar toda la verdad.",
              },
              expl: [
                "El SQL no está viejo: es el estándar de consistencia y relaciones; elijo algo más solo cuando hay una razón de forma o escala.",
                "DynamoDB/Mongo ganan en flexibilidad y escala horizontal, y pierden en consultas ad-hoc y joins.",
                "Redis es el queso de la arquitectura: rápido, y se abusa de él en cosas que deberían estar en el origen.",
                "Los analíticos columnar leen megabytes en vez de filas: la elección para dashboards sobre datos gordos.",
                "La elección no termina: el volumen crece y la lectura cambia; el diseño debe permitir migrar gradualmente.",
              ],
              errors: [
                "Elegir un motor NoSQL por su fama y vivir un año de joins hechos por la app.",
                "Poner datos transaccionales (saldos) en un documental sin transacciones reales.",
                "Mantener 4 motores para lo mismo sin una fuente de verdad, contradiciéndose hoy."
              ],
              tips: [
                "El default defensible es un SQL maduro (Postgres): sal de él con motivo.",
                "Formaliza la fuente de verdad y cada réplica especializada con su mecanismo de sincronía.",
                "Prueba la elección contra tu ratio de escritura/lectura y tu latencia objetivo.",
                "Mide la consistencia eventual de cada réplica: el lag es una deuda conocida.",
              ],
              exercise: [
                "Elige motores para: pedidos, sesiones, búsqueda de productos y reportes mensuales.",
                "Describe el evento que sincroniza el catálogo entre Postgres y Elasticsearch.",
                "Argumenta cuándo prefieres Postgres antes que DynamoDB para tu app actual.",
              ],
              summary: [
                "La forma del dato y del acceso, no la moda, elige el motor.",
                "SQL como default; especializados como réplicas de responsabilidad única.",
                "La elección se revisa cuando el volumen o la lectura cambian de perfil.",
              ],
              lang: "text",
              exampleFile: "eleccion-base-datos",
              desc: "Un mapa para elegir el motor correcto sin casarte con una moda.",
            },
          },
          {
            id: "migraciones-y-evolucion-de-esquemas",
            title: "Migraciones y evolución del esquema",
            duration: "10 min",
            level: "avanzado",
            category: "datos",
            cardText: "Cambiar la tabla en producción sin cortar el servicio ni perder filas.",
            desc: "Estrategias para evolucionar el esquema y migrar datos con la app yendo.",
            body: {
              intro: [
                "Los esquemas viven: añades columnas, cambias tipos, mueves tablas a otro motor. Hacerlo en caliente, con el servicio sirviendo, es una de las tareas más delicadas del system design.",
                "La idea central: cada migración debe ser un cambio hacia adelante y reversible, desplegada en pasos donde app y BBDD siempre siguen compatiendo.",
              ],
              def: {
                par: "Evolucionar el esquema es modificar la estructura de datos en producción con la aplicación ejecutándose, usando migraciones versionadas, expansión/contracción (expand-contract) y backfills controlados.",
                porQue: "Un ALTER gigante tarda, bloquea filas y, si rompes, el rollback es otro desastre. Dividir el cambio en pasos compatibles hace que cada deploy sea seguro y que el fallo sea solo un paso atrás.",
                cuando: "Aplica expand-contract para todo cambio de estructura con datos enormes, backfills por lotes con límite de escritura y feature flags para apagar la vieja vía cuando la nueva domina.",
              },
              concepts: [
                "Expand-contract (3 fases): añadir (expand), hacer doble escritura/lectura, y eliminar (contract) la pieza vieja.",
                "Migraciones versionadas: cada cambio es un fichero con número de orden, aplicado en serie y registrado.",
                "Backfill por lotes: actualizar los datos históricos en pedazos con pausa, no en un UPDATE gigante.",
                "Lock mínimo: los ALTER en Postgres modernos (ADD COLUMN por defecto, etc.) evitan el bloqueo; los específicos (changing type) no.",
                "La compatibilidad de esquemas: app nueva debe poder leer/escribir BBDD vieja y viceversa en la ventana del deploy.",
              ],
              basic: {
                p: "El patrón de migración versionada mínimo: ordenado, idempotente y registrado en la BBDD.",
                lang: "sql",
                code: [
                  "-- 004_nuevo_campo.sql",
                  "BEGIN;",
                  "  ALTER TABLE orders",
                  "    ADD COLUMN IF NOT EXISTS paid_at",
                  "    TIMESTAMP NULL;",
                  "-- ídem: idempotente, no bloquea con NULL por defecto",
                  "CREATE INDEX IF NOT EXISTS ix_orders_paid",
                  "  ON orders (paid_at);",
                  "COMMIT;",
                ],
                out: "Añadir columna nullable no pide reescribir la tabla.",
              },
              intermediate: {
                p: "Expand/contract con doble escritura para renombrar una tabla sin parar el servicio.",
                caption: "expand-contract",
                lang: "sql",
                code: [
                  "-- FASE 1 · expand",
                  "CREATE TABLE orders_v2 (id BIGSERIAL PRIMARY KEY, code TEXT, ...);",
                  "-- la app escribe en v1 y v2 a la vez",
                  "",
                  "-- FASE 2 · doble escritura + backfill",
                  "INSERT INTO orders_v2 SELECT ... FROM orders;",
                  "-- FASE 3 · leer de v2 (feature flag 95-100%)",
                  "-- FASE 4 · contract: drop de la vieja",
                ],
              },
              real: {
                p: "Un sistema de facturación debía renombrar `amount` a `total_amount` con signo: la FACTURACIÓN A2 gigante llevaría horas y bloquearía la tabla en venta alta. El plan: columna nueva nullable, backfill por lotes de 10k filas cada 5s, feature flag en la app para leer la nueva, y 3 días después, contracción de la vieja. Ningún bloqueo si la app nunca leyó las dos columnas modernas.",
                ul: [
                  "Añade la columna nueva nullable y con índice diferido (después del backfill).",
                  "Backfill de a poco, con un rate que no estorbe el tráfico productivo.",
                  "Levanta primero la lectura/escritura de la app con feature flag y despliega por porcentaje.",
                  "Contrae solo cuando la vieja no se lea ni se escriba desde hace N días.",
                ],
                lang: "yaml",
                code: [
                  "migracion:",
                  "  fase: \"expand\"",
                  "  backfill:",
                  "    lote: 10000",
                  "    pausa: 5s",
                  "    condicion: \"paid_at IS NULL\"",
                  "  cutover:",
                  "    flag: \"usar_v2\"",
                  "    rollout: [5, 50, 100] %",
                  "  contraccion:",
                  "    espera_dias: 2",
                ],
                nota: "El feature flag convierte el cutover en un interruptor, no en una ventana límite.",
              },
              expl: [
                "Cada paso de una migración debe dejar BBDD y app funcionando en el estado anterior o en el nuevo: nunca en el vacío.",
                "Un backfill sin límite es un pico de I/O que compite con tu producción.",
                "Las migraciones se prueban contra una copia con volumen real, no contra 100 filas de dev.",
                "El lock de las operaciones DDL es el asesino callado: conocer qué ALTER bloquea es parte del diseño.",
                "Los rollbacks modernos se hacen con la misma herramienta: 'rollback' de la migración, no un ALTER manual.",
              ],
              errors: [
                "Una migración no idempotente que al repetirse en staging rompe y falla el pipeline.",
                "Backfill gigante en una sola transacción que bloquea la tabla y congela el SaaS.",
                "Deploy de la app que lee la columna nueva antes de que exista: 500 por esquema adelantado.",
              ],
              tips: [
                "Versiona y registra cada migración; que sea idempotente y sin locks.",
                "Backfill a pedazos con pausa y monitoreo de I/O del origen.",
                "Cutover con feature flag y despliegue por porcentaje.",
                "Contrae la pieza vieja en otra fecha, con métricas de uso de por medio.",
              ],
              exercise: [
                "Diseña el expand-contract para añadir un tipo de pago nuevo a una tabla de 80M filas.",
                "Escribe una migración idempotente con ADD COLUMN y su índice diferido.",
                "Planea un backfill por lotes con límite de I/O y timeline de cutover.",
              ],
              summary: [
                "Evolucionar el esquema es expandir, escribir las dos, migrar a pedazos y contraer.",
                "Migrantes versionadas, idempotentes y sin bloqueos de tabla.",
                "El cutover con feature flag convierte la migración en interruptor, no en salto al vacío.",
              ],
              lang: "sql",
              exampleFile: "migraciones-esquema",
              desc: "Cambia el esquema en caliente con expand-contract, backfill por lotes y flags.",
            },
          },
        ],
      },
      {
        id: "modulo-06",
        n: 6,
        t: "Resiliencia y consistencia",
        level: "experto",
        caption: "El sistema caerá: diseño para que una falla no sea toda la falla.",
        desc: "Fallos en cascada, circuit breakers, retries, idempotencia y la consistencia eventual como herramienta.",
        subtitle: "La resiliencia no evita los fallos: limita su alcance.",
        objectives: [
          "Romper las cascadas de fallos con circuit breakers y límites",
          "Diseñar retries y backoff que no multipliquen la carga",
          "Hacer las operaciones idempotentes y deduplicables",
          "Aprovechar la consistencia eventual sin sufrir la eventualidad",
        ],
        lessons: [
          {
            id: "fallos-en-cascada",
            title: "Fallos en cascada",
            duration: "10 min",
            level: "experto",
            category: "resiliencia",
            cardText: "Una dependencia lenta en cadena: cómo un fallo se come a todo el sistema.",
            desc: "Reconoce y corta las cascadas de fallo antes de que toquen al usuario.",
            body: {
              intro: [
                "El fallo aislado es manejable: una copia, una BBDD, un host se caen y el resto sigue. El problema es cuando la caída viaja: un componente lento hace esperar al siguiente, que acumula trabajo, se esfuerza el doble y al final también cae.",
                "Las cascadas de fallos casi nunca empiezan en el sistema entero: empiezan en una dependencia del 1 % y terminan en el 100 % de disponibilidad. Cortar esa propagación es resiliencia.",
              ],
              def: {
                par: "Un fallo en cascada ocurre cuando la falla de un componente aumenta la carga o la recurso de otros hasta que estos también fallan, propagando la indisponibilidad a través de dependencias en cadena.",
                porQue: "Los sistemas distribuidos conectan antes el todo que el modo individual: un back que tarda rellena workers, sube las colas, y el frente termina con timeouts que repiten la petición, multiplicando la carga del back caído.",
                cuando: "Prepara límites en todas las fronteras: peticiones por segundo, timeouts por dependencia, semáforos de concurrencia y 'fail fast' cuando una dependencia cruza su latencia crítica.",
              },
              concepts: [
                "El efecto manada: una dependencia revive y todos sus clientes (que esperaban) le pegan a la vez.",
                "Cascada por retries: tiempo de espera + reintentos en cada cliente multiplica la carga del origen.",
                "Semáforo per-dependencia: limita la concurrencia hacia cada recurso, no global.",
                "Colapso del multiproceso: procesos nuevos se inician, hacen teclado a la dependencia caída y consumen recursos muriendo en loop.",
                "Aislamiento por timeout y por límite: la dependencia lenta se degrada, no se propaga.",
              ],
              basic: {
                p: "El patrón básico de aislamiento: todo accesso a una dependencia pasa por un límite de tiempo y de concurrencia.",
                lang: "javascript",
                code: [
                  "const TIMEOUT = 500; // ms",
                  "const MAX_CONCURRENCY = 20;",
                  "",
                  "async function callWithIsolation(path) {",
                  "  if (inflight >= MAX_CONCURRENCY) throw new CircuitOpen();",
                  "  inflight++;",
                  "  try {",
                  "    return await fetch(path, { signal: AbortSignal.timeout(TIMEOUT) });",
                  "  } finally {",
                  "    inflight--;",
                  "  }",
                  "}",
                ],
                out: "Más de 20 en vuelo o más de 500 ms → fail fast.",
              },
              intermediate: {
                p: "El límite por semáforo evita el colapso del pool: no dejes que una dependencia rara llegue a consumir toda la flota de workers.",
                lang: "javascript",
                code: [
                  "class Semaphore {",
                  "  constructor(max) { this.max = max; this.used = 0; this.wait = []; }",
                  "  async acquire() {",
                  "    if (this.used < this.max) { this.used++; return; }",
                  "    await new Promise((r) => this.wait.push(r));",
                  "  }",
                  "  release() {",
                  "    this.used--;",
                  "    const next = this.wait.shift();",
                  "    if (next) { this.used++; next(); }",
                  "  }",
                  "}",
                  "",
                  "const s = new Semaphore(10);",
                  "await s.acquire();",
                  "try { await callExternal(); } finally { s.release(); }",
                ],
              },
              real: {
                p: "Un servicio de notificaciones dependía de 3 proveedores. Uno empezó a responder a los 2 s (eran esperados 300 ms). Los workers, con timeout de 10 s, se llenaron esperando a ese proveedor; las notificaciones del resto esperaban detrás; y el email (sano) se enviaba con 40 s de retraso. Con timeout de 800 ms y semáforo de 5 para ese proveedor, su lentitud pasó de tumbar todo a solo 'ese canal degradado', y llegó la notificación de que el proveedor caía, no la caída general.",
                ul: [
                  "Timeout corto por dependencia: el presupuesto de tiempo es por llamada externa, no global.",
                  "Semáforo per-dependencia: un proveedor no roba la capacidad de otro.",
                  "Canal del proveedor degradado: el usuario entiende 'retrasado', no 'caído'.",
                  "Al ascensor de la dependencia, sube límites gradualmente para no recibir la manada de golpe.",
                ],
                lang: "yaml",
                code: [
                  "dependencias:",
                  "  sms:",
                  "    timeout_ms: 800",
                  "    concurrencia: 5",
                  "    circuit: { fallos: 10, ventana: 30s }",
                  "  email:",
                  "    timeout_ms: 1500",
                  "    concurrencia: 50",
                  "  push:",
                  "    timeout_ms: 600",
                  "    concurrencia: 8",
                ],
                nota: "Cada dependencia tiene su presupuesto: aislamiento es config, no altruismo.",
              },
              expl: [
                "La cascada viaja por dos rutas: esperas que saturan recursos y reintentos que multiplican carga.",
                "El fail fast con límite corto convierte la lentitud en rechazo temprano y en métrica.",
                "El aislamiento per-dependencia es la diferencia entre 'un proveedor degradado' y 'todo caído'.",
                "La reactivación vuela por el ventanal: al volver, el tráfico acumulado pega de golpe; la subida gradual la absorbe.",
                "La zombi es peor que el caído: un host que responde lento y arde CPU sin morirse es el peor caso de fallo en cascada.",
              ],
              errors: [
                "Timeout global de 10 s que convierte una dependencia lenta en un vertedero de workers.",
                "Retries sin límite que multiplican la carga justo cuando el origen quiere recuperarse.",
                "Sin semáforo per-dependencia, el canal minoritario come la capacidad del que sostiene el servicio.",
              ],
              tips: [
                "Fija timeout por llamada externa y semáforo por dependencia.",
                "Reintentos con backoff y jitter, y con tope total corto.",
                "Sube límites gradualmente al recuperar una dependencia.",
                "Monitorea la cascada: aumento de cola + aumento de latencia = inicio de caída.",
              ],
              exercise: [
                "Diseña los límites (timeout, concurrencia, retries) de una API que llama a 3 terceros.",
                "Describe cómo se propaga un fallo del tercero de SMS en tu sistema y dónde cortas.",
                "Escribe el plan de reactivación gradual tras recuperar una dependencia.",
              ],
              summary: [
                "El fallo de una dependencia debe aislarse con timeouts y límites por canal.",
                "Los reintentos sin tope multiplican la cascada.",
                "La subida gradual de límites al recuperar evita la manada.",
              ],
              lang: "javascript",
              exampleFile: "fallos-en-cascada",
              desc: "Aísla cada dependencia para que un fallo no viaje por el sistema.",
            },
          },
          {
            id: "circuit-breakers-y-timeouts",
            title: "Circuit breakers y timeouts",
            duration: "11 min",
            level: "experto",
            category: "resiliencia",
            cardText: "Corte el grifo antes del colapso y vuelva a abrir el agua cuando sane.",
            desc: "El interruptor de circuitos y el presupuesto de tiempo por dependencia.",
            body: {
              intro: [
                "El circuit breaker es el fusible del sistema: cuando una dependencia falla el suficiente rato, el cliente deja de llamarla directamente y responde desde un fallback o con error rápido, dándole a la dependencia espacio para recuperarse.",
                "Su compañero inseparable es el timeout: el breaker decide cuándo cortar, pero necesita el timeout para saber cuándo 'ya es demasiado tarde'.",
              ],
              def: {
                par: "El circuit breaker vigila las llamadas a una dependencia en una ventana; si los fallos superan un umbral, abre el circuito y deja de llamarla (fail fast), para cerrarlo de nuevo cuando una sonda (half-open) confirma que recuperó la salud.",
                porQue: "Llamar a una dependencia caída es gastar capacidad para recibir un error esperado; el breaker convierte eso en una respuesta inmediata y en un respiro para la dependencia.",
                cuando: "Úsalo en toda frontera crítica y no trivial: cada proveedor externo, BBDD, servicio interno. El break não sustituye el timeout: lo complementa.",
              },
              concepts: [
                "Estados del circuito: cerrado (deja pasar), abierto (falla rápido), half-open (sonda de recuperación).",
                "Umbral de apertura: % de fallos o conteo en una ventana de tiempo (ej. > 50 % en 30 s).",
                "Timeout es el presupuesto de cada llamada; breaker es la política sobre la acumulación de fallos.",
                "Fallback: respuesta de respaldo cuando el circuito está abierto (caché vieja, opción limitada).",
                "El half-open limita los ensayos a una sonda para no repetir la manada al reactivar.",
              ],
              basic: {
                p: "El esquema funcional de un breaker: registra errores y decide si el grifo se abre.",
                lang: "python",
                code: [
                  "class CircuitBreaker:",
                  "    def __init__(self, fail_limit=5, window=30):",
                  "        self.fail_limit = fail_limit",
                  "        self.failures = 0",
                  "        self.window_end = window",
                  "",
                  "    def allow(self):",
                  "        if self.failures >= self.fail_limit:",
                  "            raise Opened()   # fail fast",
                  "        return True",
                  "",
                  "    def on_success(self):",
                  "        self.failures = 0",
                  "",
                  "    def on_failure(self):",
                  "        self.failures += 1",
                ],
              },
              intermediate: {
                p: "La envoltura completa con half-open: cuando el circuito está abierto, una sola sonda decide el cierre.",
                lang: "python",
                code: [
                  "async def guarded(call):",
                  "    state = breaker.state()",
                  "    if state == \"ABIERTA\":",
                  "        if not breaker.allow_probe():",
                  "            return fallback()",
                  "    try:",
                  "        r = await asyncio.wait_for(call(), timeout=0.5)",
                  "        breaker.on_success(); return r",
                  "    except Exception:",
                  "        breaker.on_failure(); return fallback()",
                  "",
                  "def fallback():",
                  "    cache = cache_get(\"precio:ultimo\")",
                  "    return cache or {\"precio\": \"no disponible\"}",
                ],
              },
              real: {
                p: "Una pasarela de pagos falló el 30 % de las veces durante 20 minutos por una caída del 3DS. Sin breaker, el 30 % de las compras pagaba el costo de esperar 8 s y fallar (churn + timeouts). Con breaker (umbral 20 % en 30 s), el tráfico se redirigió al fallback de 'espacio de pago redirigido más tarde' y el 70 % estable siguió aprobando. Y al volver la dependencia, el half-open confirmó la salud antes de reabrir del todo.",
                ul: [
                  "Elige el umbral por el ratio de fallos de la dependencia, no a dedo.",
                  "El fallback con caché vieja convierte un error en una promesa degradada.",
                  "El half-open hace los ensayos con una sola llamada, no con una avalancha.",
                  "Log siempre la apertura: el on/off del breaker es un dato de la operación.",
                ],
                lang: "python",
                code: [
                  "breaker = CircuitBreaker(fail_limit=10, window_seconds=30)",
                  "",
                  "def charge(order):",
                  "    if not breaker.allow():",
                  "        queue_for_retry(order)   # fallback",
                  "        return {\"status\": \"pendiente_retry\"}",
                  "    try:",
                  "        r = gateway.charge(order, timeout=2)",
                  "        breaker.on_success(); return r",
                  "    except GatewayError:",
                  "        breaker.on_failure()",
                  "        queue_for_retry(order)",
                  "        return {\"status\": \"pendiente_retry\"}",
                ],
                nota: "El fallback con cola transforma el error puntual en una promesa de reintento seguro.",
              },
              expl: [
                "El timeout responde '¿cuánto espero?'; el breaker responde '¿cuántos fallos aguanto?'. Sin el primero, el segundo no tiene señal.",
                "El half-open es la clave del recovery: uno sondea, y solo si sana, todos vuelven.",
                "El breaker protege al consumidor de gastar recursos y a la dependencia de recibir basura; los dos lados ganan.",
                "Un umbral demasiado bajo abre el circuito con ruido de fondo; demasiado alto, la caída ya fue larga.",
                "El fallback degrada la experiencia, no la cancela: la promesa de reintento o los datos viejos están en el diseño.",
              ],
              errors: [
                "Umbral tan alto que el breaker abre cuando el daño ya está hecho.",
                "Sin half-open: al reabrir, todo el tráfico pega a la dependencia recién muerta.",
                "Timeout infinito por todos lados: la dependencia 'espera' y el circuito nunca ve el fallo a tiempo.",
              ],
              tips: [
                "Timeouts cortos y breaker con ventana y umbral medidos.",
                "Haz el fallback explícito (caché, cola) y registra cuándo se usa.",
                "Half-open con una sola sonda y cierre gradual.",
                "Métricas: aperturas del breaker por dependencia y % de fallback usado.",
              ],
              exercise: [
                "Configura un breaker para un tercero con umbral y ventana razonables.",
                "Diseña el fallback de tu endpoint de pagos cuando el gateway está abierto.",
                "Explica por qué el half-open evita la manada al recuperar.",
              ],
              summary: [
                "El breaker corta las llamadas a una dependencia que falla y hace fail fast.",
                "El timeout mide la espera; el breaker decide aguantar o cortar.",
                "El fallback con caché o cola y el half-open convierten el recovery en una promesa.",
              ],
              lang: "python",
              exampleFile: "circuit-breakers",
              desc: "Interrumpe las llamadas a dependencias fallidas y recupéralas por sonda.",
            },
          },
          {
            id: "retries-y-backoff",
            title: "Retries y backoff exponencial",
            duration: "10 min",
            level: "experto",
            category: "resiliencia",
            cardText: "Reintentar está bien; multiplicar la carga, no. El arte del backoff.",
            desc: "Reintentos con espera exponencial y jitter que no golpean al origen.",
            body: {
              intro: [
                "Los errores transitorios existen: una red que se cae 100 ms, un nodo que reinicia. No reintentar es perder trabajo innecesariamente; reintentar mal es fabricar una cascada.",
                "La regla es el backoff exponencial: cada intento espera más que el anterior, y el jitter (aleatoriedad) evita que todos los clientes reintenten a la vez, en sincronía fatal.",
              ],
              def: {
                par: "El retry con backoff exponencial reintenta una operación fallida con espera creciente entre intentos (1s, 2s, 4s, 8s) y añade un jitter aleatorio, para que la retirada de reintentos reparta su golpe en el tiempo en vez de sincronizarse.",
                porQue: "Reintentar rápido y sincronizado convierte un error transitorio en una tormenta: todos vuelven a la vez y la capacidad de recuperación se ahoga. El backoff le da al origen ventana para sanar.",
                cuando: "Reintenta errores transitorios (5xx, timeouts, connection reset), jamás 4xx (error del cliente), y con tope total de tiempo o de intentos.",
              },
              concepts: [
                "El 5xx es reintentable; el 4xx no: reintentar un 400 con el mismo payload es doblar la apuesta a la que ya perdiste.",
                "Backoff exponencial: 2^n como techo de espera con tope máximo (ej. max 30-60 s).",
                "Jitter: uniforme [0, delay] o full jitter; evita la sincronía de reintentos.",
                "Idempotencia como requisito del retry: si reintentas, el doble efecto debe ser imposible (ver lección de idempotencia).",
                "El tope de intentos totales limita cuánto 'cuesta' cada retry en tiempo y en carga.",
              ],
              basic: {
                p: "El patrón de backoff con tope y aleatoriedad: espera creciente, golpe repartido.",
                lang: "python",
                code: [
                  "import random, time",
                  "",
                  "MAX_DELAY = 16.0",
                  "MAX_ATTEMPTS = 5",
                  "",
                  "for attempt in range(MAX_ATTEMPTS):",
                  "    try:",
                  "        return call()",
                  "    except TransientError:",
                  "        if attempt == MAX_ATTEMPTS - 1: raise",
                  "        delay = min(2 ** attempt, MAX_DELAY)",
                  "        delay *= random.uniform(0.5, 1.0)  # jitter",
                  "        time.sleep(delay)",
                  "",
                  "# intentos: t+0, t+1s, t+2s, t+4s, t+8s aprox (± jitter)",
                ],
                out: "5 intentos con espera creciente y repartida.",
              },
              intermediate: {
                p: "En un entorno con varios clientes, el jitter es obligatorio: sin él, todos los clientes que fallaron a la vez reintentan también a la vez.",
                lang: "python",
                code: [
                  "def full_jitter(base, cap):",
                  "    return random.uniform(0, min(base * 2 ** attempt, cap))",
                  "",
                  "# frente a exponencial puro, el full-jitter",
                  "# reparte los reintentos en el tiempo y reduce",
                  "# la mediana del tiempo de éxito tras un pico.",
                ],
              },
              real: {
                p: "Una importación nocturna fallaba a las 18:00 por el 'reinicio de BBDD' que coincidía justo con su reintento. El script reintentaba a los 5 s exactos, pegada al reinicio del origen. Con backoff máximo 60 s + jitter, el import reintentó en 3-23 s aleatorios, la mayoría tras el reinicio, y la tasa de éxito saltó del 40 % al 98 %. El propio origen, además, dejó de recibir 30 golpes idénticos por reintento.",
                ul: [
                  "Cada reintento lleva jitter: es lo que hace estable la recuperación colectiva.",
                  "Tope de intentos: 3-5 son suficientes; más allá, mejor avisa al operador.",
                  "Añade el tentativo: la transición de transitorio a permanente la decide un umbral, no el miedo.",
                  "Idempotencia el efecto que reintentas, o del retry de hoy nace la corrupción de mañana.",
                ],
                lang: "python",
                code: [
                  "def export_with_retry(payload):",
                  "    for attempt in range(4):",
                  "        try:",
                  "            return post(\"/import\", json=payload)",
                  "        except (TimeoutError,) as e:",
                  "            d = random.uniform(0, min(60, 2 ** attempt))",
                  "            wait(d)",
                  "    raise PersistentFailure",
                  "",
                  "# 4 intentos, retry-after respetado si viene",
                ],
                nota: "El jitter convierte el backoff en una coreografía colectiva, no en una tropa en marcha.",
              },
              expl: [
                "El tiempo de éxito tras un fallo transitorio baja con el backoff porque las ventanas de reentrada se reparten.",
                "Sin cap y sin jitter, el backoff se satura o se sincroniza y pierde su función.",
                "Los 4xx no se reintentan: corrigen el payload, no la espera.",
                "La idempotencia y el retry son pareja: reintentar sin idempotencia duplica efectos.",
                "La telemetría del retry: cuántos automáticos sobrevivieron vs cuántos terminaron en alerta.",
              ],
              errors: [
                "Reintento inmediato y fijo (0.5 s) que acompaña la caída del origen paso a paso.",
                "Sin jitter, una cascada de clientes que reintenta sincronizada hace el golpe triple.",
                "Reintentar un 401/400 eternamente y ensuciar los logs por intentos sin esperanza.",
              ],
              tips: [
                "Backoff exponencial con cap y jitter; reintentos máximos 3-5.",
                "Reintenta solo transitorios y respeta los Retry-After que el servidor mande.",
                "Garantiza idempotencia en lo que reintentes.",
                "Registra y alerta por el retry permanente; el intento no es la solución, es el síntoma.",
              ],
              exercise: [
                "Implementa backoff con jitter y cap en tu lenguaje favorito.",
                "Decide qué códigos de estado reintentas en una llamada externa.",
                "Pon idempotencia a una re-carga de pedido que ahora reintentas.",
              ],
              summary: [
                "Reintenta lo transitorio, con backoff, cap y jitter.",
                "El jitter separa los reintentos en el tiempo y permite recuperar sin tormenta.",
                "La idempotencia es el requisito de todo reintento.",
              ],
              lang: "python",
              exampleFile: "retries-backoff",
              desc: "Reintenta con espera creciente y aleatoria sin machacar al origen.",
            },
          },
          {
            id: "idempotencia-y-deduplicacion",
            title: "Idempotencia y deduplicación",
            duration: "11 min",
            level: "experto",
            category: "aplicacion",
            cardText: "Repetir la operación debe dar el mismo resultado: el poder del retry.",
            desc: "Diseña operaciones seguras de repetir y elimina los dobles efectos.",
            body: {
              intro: [
                "Una petición puede repetirse sin quererlo: un timeout que volvió, un cliente que reintenta, una cola que entrega dos veces. Si el mismo pedido se cobra dos veces, la arquitectura está fallando, no el cliente.",
                "La idempotencia hace que repetir la operación produzca el mismo resultado que hacerla una vez. Es la condición de todo sistema que reintenta, reenvía o entrega al menos una vez.",
              ],
              def: {
                par: "Una operación es idempotente si aplicarla varias veces produce el mismo efecto que una sola: el servidor detecta que ya vio esta petición (por idempotency key) y responde el resultado original sin repetir el efecto.",
                porQue: "El mundo real reenvía: timeouts, retries y colas entregan más de una vez. Si el efecto del lado del servidor no es deduplicable, la segunda copia crea el error (doble pago, doble reserva).",
                cuando: "Haz idempotentes las mutaciones que el cliente puede repetir: pagos, reservas, envíos de email, incrementos. La clave suele ser un idempotency-key generada por el cliente para cada intención.",
              },
              concepts: [
                "Idempotency key: un id único de intención que el cliente manda y el servidor recuerda.",
                "Almacén de claves con TTL: la tabla que conserva la respuesta original mientras dura la ventana de retry.",
                "La carrera del doble: dos copias simultáneas; se gana con constraint único en la clave + transacción.",
                "La deduplicación en cola: cuando un consumidor recibe el mismo mensaje dos veces, el dedup por evento-id lo protege.",
                "GET/PUT/DELETE son idempotentes por naturaleza; POST no lo es sin clave.",
              ],
              basic: {
                p: "El patrón con clave de idempotencia: guarda la intención antes del efecto y responde lo guardado si vuelve.",
                lang: "sql",
                code: [
                  "CREATE TABLE idem_keys (",
                  "  key_id TEXT PRIMARY KEY,",
                  "  response JSONB,",
                  "  created_at TIMESTAMP DEFAULT now()",
                  ");",
                  "",
                  "BEGIN;",
                  "INSERT INTO idem_keys (key_id) VALUES (:key)",
                  "ON CONFLICT DO NOTHING;",
                  "-- si la fila ya estaba, es repetida",
                  "SELECT (xmax = 0) AS es_nueva FROM idem_keys WHERE key_id=:key;",
                  "COMMIT;",
                ],
                out: "El UNIQUE de la clave convierte la carrera en un solo ganador.",
              },
              intermediate: {
                p: "En cola, la deduplicación protege del doble consumo: evento-id visto → ignora.",
                lang: "python",
                code: [
                  "def handler(event):",
                  "    # dedup con SET NX de Redis (atómico)",
                  "    ok = redis.set(f\"seen:{event.id}\", \"1\", ex=3600, nx=True)",
                  "    if not ok:",
                  "        return  # ya lo procesé",
                  "    apply_effect(event)",
                ],
              },
              real: {
                p: "Un checkout sin idempotencia duplicó 1 de cada 3.000 compras durante un pico de timeouts del gateway: el cliente reintentaba, el gateway procesaba la segunda también, y el error descubierto a la mañana era triple: 3 cobros. Con idempotency-key (UUID del pedido) en el header, Redis `SETNX` de 24 h y constrait UNIQUE, la segunda llegada responde el resultado de la primera sin cobrar de nuevo. Cero duplicados desde entonces, incluida la carrera de dos copias simultáneas.",
                ul: [
                  "La clave la genera el cliente y es estable por intención (el UUID del pedido, no un timestamp).",
                  "Guarda la respuesta original para que el reintento la reciba idéntica.",
                  "El constraint único + transacción resuelve la carrera de llegadas paralelas.",
                  "TTL de la clave: la ventana de retry del cliente, más un margen de operación.",
                ],
                lang: "python",
                code: [
                  "def charge(order, idem_key):",
                  "    ok = redis.set(f\"idem:{idem_key}\", \"running\", ex=86400, nx=True)",
                  "    if not ok:",
                  "        return redis.get(f\"idem:{idem_key}:resp\")  # repetida",
                  "    try:",
                  "        resp = gateway.charge(order)",
                  "    except Exception:",
                  "        redis.delete(f\"idem:{idem_key}\")",
                  "        raise",
                  "    redis.set(f\"idem:{idem_key}:resp\", resp, ex=86400)",
                  "    return resp",
                ],
                nota: "La clave con TTL y el SETNX convierten 'repetida' en un lookup, no en un segundo efecto.",
              },
              expl: [
                "La idempotencia transforma el retry de peligro en herramienta: el reintento ya solo reparte el resultado.",
                "El almacén de claves debe sobrevivir reinicios: Redis persistente o BBDD real, no memoria de proceso.",
                "La carrera requiere atomicidad: dos copias del mismo evento deben competir en un solo inserto.",
                "El TTL define la ventana de credulidad: pasado el TTL, el mismo key revuelve y podría repetir el efecto.",
                "La deduplicación de cola y la idempotencia del endpoint son dos capas del mismo principio: detectar lo ya visto.",
              ],
              errors: [
                "Generar la clave por timestamp en el servidor: la segunda copia recibe clave distinta y ya no deduplica.",
                "Almacén de claves en memoria del proceso: reinicia y pierdes la memoria de lo procesado.",
                "Sin constraint único, dos copias simultáneas entran las dos y el doble efecto sobrevive.",
              ],
              tips: [
                "Idempotency-key estable por intención, generada por el cliente.",
                "Constraint único + transacción para la carrera; guarda la respuesta para el reintento.",
                "TTL con margen de la ventana de retry; Redis persistente o BBDD.",
                "Añade la clave en colas: dedup por event-id con SETNX.",
              ],
              exercise: [
                "Diseña el endpoint /charge idempotente con su tabla de claves.",
                "Protege un consumidor de cola contra la doble entrega con dedup.",
                "Elige el TTL y el tamaño del almacén de claves para 10k pedidos/día.",
              ],
              summary: [
                "La idempotencia hace que repetir equivalga a hacer una vez.",
                "Clave estable + constraint + respuesta guardada = deduplicación real.",
                "El retry es seguro solo si la operación es idempotente.",
              ],
              lang: "python",
              exampleFile: "idempotencia",
              desc: "Haz que repetir una operación no repita el efecto.",
            },
          },
          {
            id: "consistencia-eventual",
            title: "Consistencia eventual y conflictos",
            duration: "11 min",
            level: "experto",
            category: "distribuido",
            cardText: "Los datos eventualmente coinciden; el diseño decide cuándo y con qué lente.",
            desc: "Aprovecha la consistencia eventual sin que la eventualidad duela.",
            body: {
              intro: [
                "En un sistema distribuido, al rendir o al proteger la disponibilidad, la respuesta puede venir de una copia que aún no vio el último cambio. Ese intervalo es la consistencia eventual: el dato terminó coincidiendo si esperas.",
                "El trabajo no es negar la eventualidad: es decidir qué lecturas la toleran, cómo se resuelven los conflictos y cuánto puede durar la ventana antes de que el negocio se queje.",
              ],
              def: {
                par: "La consistencia eventual garantiza que, si no hay nuevas escrituras, todas las réplicas convergen al mismo valor con el tiempo. Entre escribir y converger existe una ventana en la que las lecturas pueden ver valores distintos.",
                porQue: "Ejecutar disponibilidad y rapidez con réplicas asíncronas significa responder desde lo que tienes, no esperar a lo que llegará. A cambio, guardas la promesa de que eventualmente será el valor definitivo.",
                cuando: "Usa eventualidad para lecturas de bajo riesgo (feed, perfiles, métricas) y mantén fuerza en las que el negocio castiga (saldos, reservas). El estado del arte es clasificar la fuerza por operación, no por sistema.",
              },
              concepts: [
                "Fuerte vs eventual: la lectura fuerte vuelve al origen (o espera quórum); la eventual lee de la réplica que sea.",
                "La ventana de divergencia: duración máxima de la discrepancia entre escribir y ver.",
                "Conflicto y resolución: dos usuarios escriben los mismos datos a la vez; LWW (last write wins) o resolución por regla de negocio.",
                "Vector clocks / versionado: etiquetar versiones para detectar y resolver conflictos, no solo sobreescribir.",
                "API: los motores exponen sus garantías (Dynamo eventual default, fuerte opt-in; Cassandra LWW).",
              ],
              basic: {
                p: "El patrón mínimo que convierte una lectura en 'fuerte cuando importa': opción por operación.",
                lang: "python",
                code: [
                  "def read_balance(user_id):",
                  "    # Fuerte: espera el quórum o vuelve al primario",
                  "    return dynamo.get(key=user_id, consistency=\"strong\")",
                  "",
                  "def read_profile(user_id):",
                  "    # Eventual: cualquier réplica, rápido y barato",
                  "    return dynamo.get(key=user_id, consistency=\"eventual\")",
                ],
                out: "La fuerza se elige por caso, no por motor global.",
              },
              intermediate: {
                p: "La resolución de conflictos con reglas de negocio evita el 'último que escribe, gana' destructivo.",
                caption: "resolución por versión",
                lang: "python",
                code: [
                  "def merge(remote, local):",
                  "    if remote.version > local.version:",
                  "        return remote            # el más reciente",
                  "    if remote.version == local.version:",
                  "        return negociar(remote, local)  # regla de negocio",
                  "    return local",
                  "",
                  "# los discount del carrito usan una estructura",
                  "# de versión: cada item lleva su etiqueta",
                  "def negociar(a, b):",
                  "    # suma de ítems + id del operador: sin destruir",
                  "    return union_por_regla(a, b)",
                ],
              },
              real: {
                p: "Un carrito compartido (dos pestañas de la misma cuenta) tenía LWW y perdía productos: la pestaña más lenta sobreescribía lo que la rápida había añadido. Se pasó a un modelo de 'opus conflictual': cada ítem lleva versión y el merge es la unión por regla (suma de ítems + dedup por sku). El resultado: convergencia sin perder la mitad del carrito, y la fuerza fuerte solo en el checkout, donde la divergencia sería cobrada.",
                ul: [
                  "Fuerza por operación: fuerte en el camino del dinero, eventual en el de la lectura.",
                  "Resolución con regla de negocio para conflictos del dominio, no LWW a ciegas.",
                  "Etiqueta cada pieza con versión; el merge se disloca en la pieza, no en todo el documento.",
                  "Mide la ventana de divergencia de las lecturas tolerantes: si crece, revisa la replica lag.",
                ],
                lang: "yaml",
                code: [
                  "lecturas:",
                  "  /cart (checkout): fuerte   # road del dinero",
                  "  /cart (vista):    eventual  # lateral del usuario",
                  "  /profile:         eventual",
                  "  /balance:         fuerte",
                  "merges:",
                  "  cart: union_por_sku",
                  "  profile: last_write_wins",
                ],
                nota: "El mismo dato puede leerse fuerte y eventual según la operación que lo consume.",
              },
              expl: [
                "La eventualidad no es un error: es el costo elegido para disponibilidad y rapidez de lectura.",
                "La clasificación de fuerza por operación es el diseño: las reglas de negocio dirán quién aguanta divergencia.",
                "El conflicto necesita un modelo: LWW es barato y destructivo; versionar resuelve mejor el dominio.",
                "La ventana real de divergencia se mide (lag de réplicas), no se asume.",
                "Lo eventual y lo fuerte conviven: las dos lecturas del mismo dato en servicios distintos es normal y correcto.",
              ],
              errors: [
                "LWW en un carrito compartido: la pestaña lenta borra lo que la rápida puso.",
                "Lectura eventual del saldo justo después de un pago: el usuario ve el pago 'no aplicado'.",
                "Resolver conflictos con la 'última escritura' sin leer si hay versiones ocultas del otro nodo.",
              ],
              tips: [
                "Clasifica cada operación como fuerte o eventual con regla del dominio.",
                "Para conflictos reales, versiona las piezas y define la regla de merge.",
                "Mide el lag de réplicas y alerta cuando la ventana se hace visible para el usuario.",
                "Siempre documenta: qué garantía tiene cada lectura de tu API.",
              ],
              exercise: [
                "Clasifica las lecturas de una tienda: catálogo, carrito, stock y saldo.",
                "Diseña el merge de un carrito compartido sin LWW destructivo.",
                "Explica la diferencia práctica entre eventual por defecto y fuerte opt-in.",
              ],
              summary: [
                "La consistencia eventual es el precio que eliges para rapidez y disponibilidad.",
                "Fuerza por operación: fuerte en el dinero, eventual en lo demás.",
                "Conflicto: versión + regla de negocio, no LWW a ciegas.",
              ],
              lang: "python",
              exampleFile: "consistencia-eventual",
              desc: "Diseña lecturas fuertes y eventuales con la lente del negocio.",
            },
          },
        ],
      },
      {
        id: "modulo-07",
        n: 7,
        t: "Operación y entrevista de diseño",
        level: "experto",
        caption: "Del pizarrón a producción y de producción a una oferta: cierra el ciclo.",
        desc: "Observabilidad a escala, capacidad, despliegues y la metodología completa de la entrevista de system design.",
        subtitle: "Lo que pasa después del diagrama: monitorizar, dimensionar y comunicar.",
        objectives: [
          "Observar un sistema distribuido con logs, métricas y trazas unidas",
          "Planificar capacidad y entender el gasto por componente",
          "Desplegar con feature flags y reversiones quirúrgicas",
          "Ejecutar la entrevista de system design con método y claridad",
          "Tomar y defender decisiones con trade-offs explícitos",
        ],
        lessons: [
          {
            id: "observabilidad-a-escala",
            title: "Observabilidad a escala",
            duration: "11 min",
            level: "experto",
            category: "operacion",
            cardText: "Logs, métricas y traces: las tres señales que ponen luz en lo distribuido.",
            desc: "Diseña la observabilidad para que la operación no sea un callejón.",
            body: {
              intro: [
                "En un sistema de una máquina, miras los logs y adivinas. En uno distribuido, un fallo de usuario viaja por 8 servicios, 3 colas y 2 cachés: la telegrafía de un solo componente no cuenta la historia.",
                "La observabilidad se construye con tres señales que deben cruzarse: métricas para los síntomas, logs para la evidencia y trazas para el recorrido. Y un código de correlación para unirlas.",
              ],
              def: {
                par: "La observabilidad permite responder 'qué está pasando' con las preguntas que no planificaste: métricas (estado y tendencias), logs (eventos y errores) y trazas (el camino de una petición entre servicios), unidas por un identificador común.",
                porQue: "La operación distribuida cae no donde falla el byte sino donde cuenta la latencia: sin trazas no ves cuál de los 8 servicios va lento; sin métricas no ves la tendencia antes del pico; sin logs, no ves el porqué.",
                cuando: "Diseña la observabilidad al construir el servicio: cada petición lleva su request-id, cada componente emite métricas RED/USE, y los logs son estructurados y con contexto.",
              },
              concepts: [
                "RED/USE: para servicios, Rate/errors/Duration; para recursos, Utilization/Saturation/Errors.",
                "Traza distribuida (OpenTelemetry): cada petición consigue un trace-id que se propaga por headers.",
                "Logs estructurados (JSON) con contexto: sin texto libre, con claves buscables.",
                "Muestreo adaptativo: trazas completas en errores y una muestra en el pico para no pagar el almacenaje de todo.",
                "Service level objectives (SLO): el presupuesto de disponibilidad en números que el negocio acepta.",
              ],
              basic: {
                p: "Las tres señales se unen con el correlator: el SAME trace-id en logs de todos los servicios.",
                lang: "yaml",
                code: [
                  "telemetria:",
                  "  metricas:",
                  "    - http_request_duration_ms{método, ruta, status}",
                  "    - http_errors_total{ruta}",
                  "    - queue_depth{colas}",
                  "  logs:",
                  "    formato: JSON",
                  "    claves: [ts, level, trace_id, service, msg]",
                  "  traces:",
                  "    propagacion: [traceparent, x-request-id]",
                  "    muestreo:", 
                  "      errores: 100%",
                  "      ok: 5%",
                ],
                out: "Una petición → trace-id único en métricas, logs y traces.",
              },
              intermediate: {
                p: "El SLO se vigila con el burn rate: cuando el error consume el presupuesto demasiado rápido, se escala la alerta.",
                lang: "yaml",
                code: [
                  "slo:",
                  "  objetivo: 99.9%",
                  "  ventana: 30d",
                  "  burn_rates:",
                  "    - página: 14.4x  # cuádruple del presupuesto",
                  "      alerta: page",
                  "    - página: 3x",
                  "      alerta: warning",
                ],
              },
              real: {
                p: "Una plataforma de reservas sufría 'caídas fantasma': usuarios veían errores pero los dashboards pedían perdón. Cada equipo miraba lo suyo sin correlacionar. Se impuso un request-id en una cabecera, se enviaron trazas OpenTelemetry al 100 % en errores y al 5 % en ok, y los logs pasaron a JSON con trace-id. El siguiente incidente se resolvió en 11 minutos (era el timeout del proveedor #2 rebotando por 3 caminos), contra las 3 horas del anterior.",
                ul: [
                  "Correlación con trace-id: es lo que convierte 8 servicios en una sola historia.",
                  "Logs JSON y estructurados: la búsqueda del porqué no es grep por texto libre.",
                  "Muestreo adaptativo: luz en el error, poco coste en la normalidad.",
                  "SLO con burn rate: la alerta llega antes del presupuesto agotado.",
                ],
                lang: "bash",
                code: [
                  "# localizar el cuello de botella por traza",
                  "curl 'http://otel/query' -d '{\"trace_id\":\"ab12\",\"service\":\"checkout\"}'",
                  "# salida: el 78% del tiempo se pasa en gateway-pagos",
                  "# ahora el log con el mismo trace_id cuenta el porqué",
                ],
                nota: "La observabilidad no es un panel: es la capacidad de responder a preguntas nuevas.",
              },
              expl: [
                "Las métricas cuentan qué se gasta; los logs, por qué; las trazas, dónde. Las tres juntas resuelven el caso.",
                "Sin correlación, cada equipo culpa al otro: el trace-id es el idioma común del incidente.",
                "El muestreo separa el coste del valor: errores al 100 %, normalidad muestreada.",
                "El SLO en números hace que la discusión de disponibilidad sea de producto, no de ego.",
                "La on-call sin telemetría es magia: la pregunta '¿qué pasó?' se responde con datos, no con memoria.",
              ],
              errors: [
                "Métricas por servicio y sin trazas: sabes que murió, no dónde.",
                "Logs de texto libre sin trace-id: imposible unir las piezas del incidente.",
                "Muestreo que descarta los errores justo en el pico donde más importan.",
              ],
              tips: [
                "Propaga el trace-id por defecto en cada cliente HTTP y en cada cola.",
                "Emite RED/USE en cada componente nuevo como parte del Definition of Done.",
                "Logs JSON con contexto; nunca texto libre de campos buscables.",
                "Define SLO por caso de negocio y alerta por burn rate, no por umbral de aficionado.",
              ],
              exercise: [
                "Diseña la telemetría de un nuevo microservicio: métricas, logs y trazas.",
                "Escribe la definición de un SLO 99.9 % con su burn rate.",
                "Traza el recorrido de una petición de checkout y dónde buscarías la lentitud.",
              ],
              summary: [
                "Métricas para síntomas, logs para el porqué, trazas para el dónde.",
                "El trace-id une los tres y convierte un sistema distribuido en una historia.",
                "El SLO y el muestreo adaptativo hacen la observabilidad sostenible.",
              ],
              lang: "yaml",
              exampleFile: "observabilidad",
              desc: "Construye la observabilidad para que cada fallo distribuido cuente su historia.",
            },
          },
          {
            id: "planificacion-de-capacidad",
            title: "Planificación de capacidad",
            duration: "10 min",
            level: "experto",
            category: "operacion",
            cardText: "Cuánto sistema necesitas antes del pico, y qué pasa si no llegas.",
            desc: "Dimensiona nodos, almacenamiento y coste con números antes del lanzamiento.",
            body: {
              intro: [
                "La capacidad no se adivina: se calcula. Cuántos nodos, cuánto almacenamiento, cuántas IOPS y cuánto coste al mes son preguntas con respuesta numérica a partir de una estimación de tráfico.",
                "El plan de capacidad es el puente entre la arquitectura y el presupuesto: cada decisión de diseño tiene un coste mensual y un gasto de pico.",
              ],
              def: {
                par: "La planificación de capacidad traduce requisitos de negocio (usuarios, peticiones, datos) en recursos (nodos, memoria, disco, ancho de banda) con margen para picos, crecimiento y fallos, y estima su coste.",
                porQue: "Sin dimensionado, o sobre-compras (coste) o sub-compras (caída en el pico esperado): el dimensionado por proyecto y por mes convierte el gasto en decisión, no en sorpresa.",
                cuando: "Hazla antes de lanzar, en cada release grande y con la telemetría real del último pico; se revisa mensualmente con los números que ya tienes.",
              },
              concepts: [
                "Del negocio al número: usuaria/día → peticiones/segundo pico → CPU/RAM/IOPS.",
                "El margen de pico: pico de QPS × factor de cliente (tiempo máximo), y el margen de failover (N+1).",
                "Almacenamiento: tamaño de registro × volumen × retención, creciendo en el tiempo.",
                "El coste es dato: nube sale por vCPU/hora y por GB; el diseño se traduce a €/mes.",
                "El over-provisioning controlado: compra el 20 % de colchón para picos, no el 2x.",
              ],
              basic: {
                p: "Del tráfico a nodos con margen: el cálculo de servilleta del día uno.",
                lang: "text",
                code: [
                  "peticiones/día = 2.000.000",
                  "QPS promedio   = 2M / 86.400 ≈ 23 RPS",
                  "QPS pico       = 23 × 5 ≈ 115 RPS (factor 5)",
                  "",
                  "capacidad nodo = 400 RPS (medida)",
                  "nodos          = 115 / 400 = 0.29 → 1 (con margen)",
                  "nodos reales   = 2 (redundancia e improvistos)",
                  "memoria app    = 2 × 512 MB = 1 GB",
                ],
                out: "2 nodos para un servicio de 23 RPS promedio.",
              },
              intermediate: {
                p: "La hoja de capacidad mensual con crecimiento compuesto convierte el plan en revisable.",
                lang: "text",
                code: [
                  "mes  | RPS pico | nodos (400rps) | BBDD GB | €/mes",
                  "-----+----------+----------------+---------+-------",
                  "1    |     115  |  2             |  45     | 380",
                  "3    |     190  |  3             |  90     | 610",
                  "6    |     340  |  4             | 180     | 990",
                  "12   |     620  |  5             | 360     | 1500",
                  "",
                  "crecimiento 20%/trimestre → comprar por trimestre,",
                  "autoescalar para picos, revender al bajar.",
                ],
              },
              real: {
                p: "Una app de eventos lanzaba por dates famosas: el primer pico murió en 20 minutos porque el equipo dimensionó para el promedio (50 RPS) y llegó un 12x. El plan siguiente: QPS esperado × factor 5 de pico, nodos N+1, caché por catálogo y bases con réplica de lectura; costó 30 % más de presupuesto y absorbió sin un solo 5xx un evento de 14x. El histograma real del evento validó el factor.",
                ul: [
                  "Diseña el pico: pregúntate '¿y si este evento duplica?', no dimensiones el promedio.",
                  "Valida los supuestos con la telemetría real del último evento y ajusta el factor.",
                  "Autoescalado para picos y capacidad mínima para el valle: no compres el pico siempre.",
                  "Revisa trimestralmente: el crecimiento de datos es exponencial, no lineal.",
                ],
                lang: "yaml",
                code: [
                  "capacidad:",
                  "  servicios:",
                  "    api: { grupos: 3, autoescalado: [3, 30], margen: N+1 }",
                  "    workers: { grupos: 2, autoescalado: [4, 40] }",
                  "  datos:",
                  "    postgres: { almacenamiento_gb_proyectado_12m: 360 }",
                  "  pico:",
                  "    factor: 5x",
                  "    prueba: load test en shadow",
                ],
                nota: "El autoescalado compra el pico mientras dura; la capacidad mínima paga el valle.",
              },
              expl: [
                "La capacidad se calcula desde el negocio: el QPS y el tamaño de registro son números reales, no arquitectura pura.",
                "El margen protege del pico, y el N+1 protege del fallo: las dos incertidumbres que cuestan caras.",
                "El coste mensual es una columna del diseño: comparar caché vs BBDD, réplica vs columna, se hace en €/mes.",
                "El crecimiento de datos se subestima siempre: los registros se acumulan y las BBDD se llenan.",
                "La planificación se valida con la telemetría: si el pico real no coincide con el factor, ajustas el modelo.",
              ],
              errors: [
                "Dimensionar el promedio y morir en el pico: la caída en la hora punta es la más visible.",
                "Comprar el pico siempre (pico 2x constante) y pagar 4x de coste mensual.",
                "Olvidar el crecimiento de datos: la BBDD llena a los 3 meses y la migración es urgente.",
              ],
              tips: [
                "Traduce negocio → QPS → nodos → € con una hoja y revisión trimestral.",
                "Factor de pico y N+1 siempre en el cálculo.",
                "Autoescalado para picos; compra mínimos para el valle.",
                "Valida los factores con los histogramas reales de cada evento.",
              ],
              exercise: [
                "Calcula nodos y BBDD para 5M de peticiones/día con factor de pico 4 y nodo de 500 RPS.",
                "Haz la hoja 12 meses de una app que crece 25 %/trimestre.",
                "Diseña el plan de capacidad para el lanzamiento de una fecha con 14x esperado.",
              ],
              summary: [
                "La capacidad se calcula desde el negocio y se revisa con la telemetría real.",
                "El margen de pico y el N+1 son la parte cara y necesaria del plan.",
                "El coste mensual es una columna del diseño, no una sorpresa final.",
              ],
              lang: "text",
              exampleFile: "planificacion-capacidad",
              desc: "Dimensiona nodos, almacenamiento y coste con fórmula y margen.",
            },
          },
          {
            id: "feature-flags-y-despliegues",
            title: "Feature flags y despliegues",
            duration: "10 min",
            level: "experto",
            category: "operacion",
            cardText: "Publica la mitad de la función y destrázalo cuando se detecta el problema.",
            desc: "Despliega con flags, canary y rollback quirúrgico sin cortes.",
            body: {
              intro: [
                "El peor camino de un feature a producción es 'se despliega todo hoy'. Si algo falla, la reversión es un segundo deploy angustioso y el daño ya tocó a todos.",
                "Las técnicas modernas separan desplegar (publicar el código) de exponer (activar la función): feature flags, canary releases y rollbacks quirúrgicos convierten el lanzamiento en una serie de palancas.",
              ],
              def: {
                par: "Los feature flags activan o desactivan una función en runtime sin nuevo deploy: rollout por porcentaje, por segmento y apagado instantáneo. El canary lanza la versión a un grupo pequeño y escala si la telemetría sana.",
                porQue: "Si la función vive tras una palanca, el problema se apaga en segundos sin desplegar nada, y el rollout se mide antes de que alcance al 100 %.",
                cuando: "Usa flags para features grandes y reversibles, canary para releases de servicios y feature gates para habilitar por usuario/plan. Nunca para 'caché' del código roto: la palanca debe apagar algo que funciona.",
              },
              concepts: [
                "Feature flag: condición runtime que decide si la función se ejecuta (por % o por segmento).",
                "Canary: lanzar el 5-10 % del tráfico a la nueva versión y escalar con la señal de error y latencia.",
                "Rollback quirúrgico: volver atrás solo el servicio problemático, sin tocar al resto.",
                "Apagado de emergencia (kill switch): palanca global para retirar una función al instante.",
                "Dead code y limpieza: un flag eterno es deuda; el código de los dos caminos vive en producción.",
              ],
              basic: {
                p: "La condición mínima de un flag: la función se enciende por porcentaje de usuarios.",
                lang: "yaml",
                code: [
                  "features:",
                  "  checkout-v2:",
                  "    enabled: true",
                  "    rollout: 20%",
                  "    segmentos: [plan_premium, sin_migración]",
                  "  chat-emoji:",
                  "    enabled: false",
                ],
                out: "El 20 % prueba checkout-v2; el resto usa la v1.",
              },
              intermediate: {
                p: "El evaluador en la app decide por usuario de forma estable para que el usuario no cambie de vía a cada petición.",
                lang: "javascript",
                code: [
                  "function userInRollout(userId, pct) {",
                  "  const h = hash(userId) % 100;",
                  "  return h < pct;",
                  "}",
                  "",
                  "const useV2 = flag(\"checkout-v2\") &&",
                  "              userInRollout(user.id, 20) &&",
                  "              userHas(ctx, \"plan_premium\");",
                ],
              },
              real: {
                p: "El 'checkout-v2' de una tienda salió con canary 10 %; a los 20 minutos, la telemetría mostró 4x de timeouts en la autorización para el segmento con tarjetas extranjeras. Con el flag, se desactivó en segundos (sin rollback completo), se corrigió la lógica del provider en una hora y se relanzó con 100 % al día siguiente. El coste del incidente: 20 minutos al 10 % de los usuarios.",
                ul: [
                  "Lanza canary con grupo pequeño y telemetría comparada (error_rate_v2 vs v1).",
                  "Flag por funcionalidad con apagado instantáneo; no destruyas el flag con el primero que entre.",
                  "Compara con la baseline: el canary sin SLO de control no te cuenta si sufre.",
                  "Limpia flags viejos: el código de los dos caminos es deuda que se paga en mantenimiento.",
                ],
                lang: "yaml",
                code: [
                  "deploy:",
                  "  canary:",
                  "    porcentaje: 10%",
                  "    ventana: 30m",
                  "    criterios_ok:",
                  "      - error_rate <= 1.2x baseline",
                  "      - p95_latencia <= 1.1x baseline",
                  "    escalar: 10 -> 25 -> 50 -> 100%",
                  "  kill_switch:",
                  "    feature: checkout-v2",
                  "    activado: \"si error_rate > 5x\"",
                ],
                nota: "El canary con criterios numéricos decide solo; el flag decide al instante.",
              },
              expl: [
                "Separar deploy de exposición es la libertad terrible: el código ya está, decide cuándo lo activas.",
                "El rollout por porcentaje con telemetría comparada te deja revertir sin drama de despliegue.",
                "La reversión de infraestructura (azulejo de AWS regresión) sigue siendo deploy; el flag te cubre la función, no el recurso.",
                "El kill switch tiene que existir de verdad: probado y con telemetría que lo encienda por si sola.",
                "Los flags viejos son coste: mientras vivan, dos caminos de lógica se mantienen.",
              ],
              errors: [
                "Publicar todo de golpe sin flag: la 'reversión rápida' se convierte en un segundo deploy en pánico.",
                "Canary que sube al 100 % sin criterios: la ventana pequeña engaña y el grosor llega de golpe.",
                "Flag eterno sin limpiar: la deuda de los dos caminos se paga en cada cambio.",
              ],
              tips: [
                "Cada función grande nace tras un flag: activar por %, segmento y con kill switch.",
                "Canary con telemetría comparada y criterios numéricos de subida.",
                "Alerta automática que apague el flag si el error rate cruza el umbral.",
                "Widget de limpieza: cada release revisa flags que ya pueden morir.",
              ],
              exercise: [
                "Diseña el flag de una función nueva con rollout por segmento y kill switch.",
                "Define los criterios de subida de un canary de 10 % a 100 %.",
                "Enumera qué flags de tu sistema viven y podrían retirarse.",
              ],
              summary: [
                "Despliegues seguros: desplegar es publicar, exponer es una palanca.",
                "Canary con criterios y flags con kill switch cubren el lanzamiento.",
                "El flag sin limpiar es deuda; el rollback quirúrgico es el rey.",
              ],
              lang: "yaml",
              exampleFile: "feature-flags",
              desc: "Lanza features reversibles con flags, canary y telemetría comparada.",
            },
          },
          {
            id: "entrevista-de-system-design",
            title: "La entrevista paso a paso",
            duration: "12 min",
            level: "experto",
            category: "entrevista",
            cardText: "De requisitos a pizarra: la metodología de 45 minutos que funciona.",
            desc: "Ejecuta una entrevista de system design con método, estructura y calma.",
            body: {
              intro: [
                "La entrevista de system design mide cómo piensas bajo presión y con poca información. La gente que 'se sabe todos los casos' falla por lo mismo: sin método, 45 minutos no alcanzan y el pizarrón se llena de cajas sin orden.",
                "La metodología es un reloj: clarificar (5m), diseñar alto nivel (10m), profundizar según el interviewer (20m), cerrar con trade-offs y proyección (10m). Cada minuto tiene trabajo propio.",
              ],
              def: {
                par: "Una entrevista de system design es una conversación sobre la arquitectura de un sistema dado: requisitos, escala, componentes, datos y trade-offs, evaluada por claridad, alcance y la capacidad de tomar decisiones con la información disponible.",
                porQue: "El evaluador no busca 'la arquitectura perfecta' (no existe): busca tu proceso: cómo deshaces la ambigüedad, cómo priorizas y cómo justificas cada decisión con sus costes.",
                cuando: "Aplica esta estructura a todo caso: el reloj es el mismo para un chat, un feed o un buscador; varían las piezas, no el método.",
              },
              concepts: [
                "Clarifica primero: funcionales + no funcionales, escala objetivo y qué es lo importante del caso.",
                "Estima a servilleta: QPS, almacenamiento, y usalos para decidir componentes grandes.",
                "El diagrama de cajas con responsabilidad: propón el alto nivel y espera el enfoque del interviewer.",
                "Round 2 de profundidad: la burbuja que el interviewer marca es la que aguanta detalles (datos, caching, fallos).",
                "El cierre: trade-offs, cómo escala a 10x y qué sacrificarías.",
              ],
              basic: {
                p: "El patrón de los 45 minutos, con su presupuesto por fase.",
                lang: "text",
                code: [
                  "0-5m   Clarificar requisitos y escala",
                  "       - funcionales (qué hace el sistema)",
                  "       - no funcionales (latencia, consistencia)",
                  "       - números: usuarios, QPS, tamaño",
                  "",
                  "5-15m  Alto nivel: cajas y flujo, sin detalle",
                  "       - cliente → LB → API → servicios → datos",
                  "       - colas para lo asíncrono (mail, jobs)",
                  "",
                  "15-35m La burbuja que el interviewer marca",
                  "       - datos: esquema, caché, replicación",
                  "       - comportamiento: cold, fallos, timer",
                  "",
                  "35-45m Trade-offs y proyección",
                  "       - qué sacrificas y por qué",
                  "       - qué pasaría si el tráfico x10",
                ],
                out: "45 min con reloj por fase, no pizarra sin norte.",
              },
              intermediate: {
                p: "Las preguntas que siempre debes hacerte en el momento de los datos y la escala.",
                lang: "text",
                code: [
                  "DATOS",
                  "  ¿Quién escribe y quién lee?",
                  "  ¿Cuántos registros por acción? ¿Persistencia?",
                  "  ¿Puede una lectura tolerar lag? (fuerte vs eventual)",
                  "  ¿Hay joins que la BBDD no puede hacer? (denormalización)",
                  "",
                  "ESCALA",
                  "  ¿Esto se lee mucho más de lo que se escribe? → caché",
                  "  ¿La escritura supera una bd? → sharding",
                  "  ¿El pico manda? → autoscaling y cola",
                  "  ¿La latencia importa por geografía? → CDN",
                ],
              },
              real: {
                p: "El caso típico que se evalúa: 'diseña un servicio de URLs cortas'. El candidato que sacó nota dibujó: clarifica redirección 302 y analytics (2m), estima 60M URLs/mes → 25 QPS (pico 100), y propuso: API de alta, BBDD con tabla urls (hash único), caché Redis, redirector 302 sin BBDD en el hot path; para los analytics, proceso asíncrono con cola. En la burbuja 'persistencia', defendió hash de 7 chars, 62^n cardinalidad y el anti-colisión. Cerró con trade-off: redirección fuerte de BBDD (fallback), consistencia eventual en contadores.",
                ul: [
                  "Habla poco y preguntas: la entrevista puntúa tu proceso, no tu monólogo.",
                  "Redondea los números: 60M/mes ≈ 23 RPS; no necesitas exactitud de calculadora.",
                  "Nombra las decisiones: 'aquí elijo caché con fallback a BBDD porque la lectura domina y tolero lag'.",
                  "La burbuja que te marcan es tu señal: profundiza ahí, no donde a ti te apetece.",
                ],
                lang: "bash",
                code: [
                  "# estimación del caso 'url shortener'",
                  "urls/mes  = 60M",
                  "QPS const = 60M / (30*86400) ≈ 23",
                  "pico      = 23 * 5 ≈ 115",
                  "# almacenamiento",
                  "registro/gen   = 400 B",
                  "1 año          = 60M * 12 * 400 B ≈ 288 GB",
                  "# direcciones de 7 chars (62^n)",
                  "62^7 ≈ 3.5 billones >> 720M requeridas",
                ],
                nota: "Estimación redondeada y con margen: la precisión no es el objetivo, el orden de magnitud sí.",
              },
              expl: [
                "La clarificación define el alcance: dos casos 'distintos' del mismo enunciado dependen de ti.",
                "El alto nivel primero sin detalle: las cajas con responsabilidad y los datos con flechas.",
                "La profundidad se gasta en la burbuja correcta: el interviewer la marca, y ahí es donde los detalles valen.",
                "Los trade-offs son el extracto: mostrar qué elegirías y qué sacrificias es lo que distingue al senior.",
                "La proyección 10x (normalmente '¿cómo escala a 10x?') se responde con caché→sharding→regiones en orden.",
              ],
              errors: [
                "Empezar a dibujar sin clarificar: el caso se pierde porque no sabías qué se pregunta.",
                "Profundizar en la caché cuando el interviewer marca la cola de fallos.",
                "Monólogo interminable: la entrevista es conversación, no conferencia.",
                "No cerrar con trade-offs: terminas la historia sin el morral que el evaluador buscaba.",
              ],
              tips: [
                "Usa el reloj: 5-10-20-10 y deja que el interviewer guíe la burbuja.",
                "Números redondeados y con su margen; nada de calculadora.",
                "Nombra cada decisión con su coste ('elijo X por Y, y el precio es Z').",
                "Cierra siempre con '¿qué pasa si x10?' y la respuesta en pasos.",
              ],
              exercise: [
                "Practica 'diseña X' con tu cronómetro 5-10-20-10 y un caso que no conozcas.",
                "Graba 3 minutos: cuánto hablaste tú y cuántas preguntaste.",
                "Responde '¿cómo escala a 10x?' para un feed con mas 20x de usuarios.",
              ],
              summary: [
                "La entrevista es proceso: clarificar, alto nivel, burbuja del interviewer, trade-offs.",
                "Los números redondeados y las decisiones nombradas la ganan.",
                "El cierre 10x y los trade-offs son el sello del senior.",
              ],
              lang: "text",
              exampleFile: "entrevista-system-design",
              desc: "Ejecuta el caso completo con reloj, estructura y decisiones nombradas.",
            },
          },
          {
            id: "trade-offs-y-decisiones",
            title: "Trade-offs y decisiones de diseño",
            duration: "10 min",
            level: "experto",
            category: "entrevista",
            cardText: "No hay arquitectura perfecta: la marca del senior es elegir y justificar.",
            desc: "Toma decisiones de diseño con sus costes y defiende el porqué.",
            body: {
              intro: [
                "Todo diseño es una serie de renuncias: la consistencia cuesta latencia, la caché cuesta frescura, la resiliencia cuesta complejidad. El que diseña bien no es el que evita los conflictos: es el que los elige conscientemente.",
                "La habilidad de seniority es nombrar el trade-off: 'aquí gano X, y el precio es Y, y acepto Y porque el caso (o el presupuesto de fallo) lo permite'. Sin esa frase, una arquitectura es un montón de cajas.",
              ],
              def: {
                par: "Un trade-off de diseño es la elección consciente entre dos o más fuerzas en conflicto (latencia, consistencia, coste, complejidad), aceptando el coste de una para ganar la otra, siempre justificado por el caso de negocio.",
                porQue: "Las arquitecturas no tienen respuestas absolutas, tienen respuestas adecuadas: la misma decisión (réplica asíncrona, write-back, sharding) es brillante en un caso y un desastre en otro. Elegir y defender es la diferencia entre 'dibujó algo' y 'diseñó'.",
                cuando: "Aplica el marco en cada decisión de la entrevista y de la arquitectura real: enuncua las opciones, elige, y diqué sacrificas y por qué.",
              },
              concepts: [
                "El eje CAP: consistencia vs disponibilidad en particiones; se elige con el negocio.",
                "El eje latencia/coste: la caché y los réplicas suben el coste y bajan la latencia; cada aceleración se paga.",
                "Durabilidad vs rendimiento (write-back): más rápido = más riesgo de perder la última escritura.",
                "Simplicidad vs flexibilidad: el microservicio flexible es caro de operar; el monolito simple se equivoca poco.",
                "El presupuesto de fallo (SLO/budget) es la moneda que convierte trade-offs en números.",
              ],
              basic: {
                p: "El formato de una decisión bien tomada: opción, elección, coste, y por qué el caso lo acepta.",
                lang: "text",
                code: [
                  "DECISIÓN: replicar la BBDD con lectura de réplica",
                  "",
                  "GANO   → latencia de lectura (caso 80% lectura)",
                  "PAGO   → consistencia eventual (lag de réplicas)",
                  "",
                  "POR QUÉ SÍ → el perfil de la lectura es 90% perfil/",
                  "                catálogo y tolera 2 s de lag",
                  "POR QUÉ NO (alternativa) → fuerte siempre = réplica",
                  "                síncrona o primario = más coste y",
                  "                latencia para un caso que no lo exige",
                ],
                out: "Gano frescura suficiente, pago una ventana soportable.",
              },
              intermediate: {
                p: "El trade-off de la caché de catálogo con escrita de catálogo: rápida y poco fresca se decide con el 'caso de frescura'.",
                lang: "yaml",
                code: [
                  "fuerzas:",
                  "  frescura: precio/catálogo cambiando cada hora",
                  "  latencia: lectura 99% del tráfico",
                  "",
                  "opciones:",
                  "  sin_caché:           frescura perfecta, latencia alta",
                  "  caché_ttl_60s:       latencia baja, frescura ≤ 60s",
                  "  caché_evento:        latencia baja, frescura ~0s, más complejo",
                  "",
                  "decisión: caché_ttl_60s + evento para el catálogo crítico",
                  "coste:  ventana ≤ 1 min en la web de precios",
                  "acepta: el caso no bloquea compra por 1 min de staleness",
                ],
              },
              real: {
                p: "Un servicio de pedidos eligió réplica asíncrona (con 'débito por falsa' de 2 s) contra síncrona para la lectura de estado. En producción, el 1 % de usuarios vio el pedido 'sin pagar' 2 s después de pagar. El equipo consideró exigir fuerte siempre (más coste y los picos de lectura concentrados morían). La decisión final fue híbrida: lectura fuerte para el estado del pedido del propio usuario (read-after-write), eventual para los catálogos y agregados. El trade-off se convirtió en dos reglas en vez de una.",
                ul: [
                  "Nombra las fuerzas en conflicto antes de elegir: sin fuerzas, no hay trade-off.",
                  "Traduce el coste a números de negocio: '2 s de lag' o '30 % más de coste'.",
                  "Pega la regla al caso: la lectura del propio dato (fuerte), el agregado tercero (eventual).",
                  "Revisa el trade-off cuando las fuerzas cambien: el tráfico mueve la decisión.",
                ],
                lang: "yaml",
                code: [
                  "decision: 'lectura de pedidos'",
                  "fuerzas: [consistencia real, coste_bbdd, latencia_p95]",
                  "regla:",
                  "  - si es el propio usuario dueño → strong (read-after-write)",
                  "  - si es catálogo o feed             → eventual + TTL",
                  "coste_aceptado:",
                  "  - catálogo: soporta 60s de staleness",
                  "  - pedido propio: 0 (fuerte)",
                  "revisión: trimestral con histogramas de lag y coste",
                ],
                nota: "Los trade-offs también tienen reglas: la decisión no es 'uno u otro', es 'cuándo cada uno'.",
              },
              expl: [
                "Las fuerzas se enuncian primero; sin conflicto nombrado, la elección no es trade-off, es inercia.",
                "El caso de negocio es el árbitro: la vigencia del coste la pone el dominio, no la opinión.",
                "La línea de complejidad hay que pagarla al subir: cada microservicio extra es una operación que mantener.",
                "El presupuesto de fallo convierte 'tal vez aceptable' en 'aceptable según el SLO': la moneda del trade-off.",
                "El trade-off se revisa: las fuerzas cambian con el tráfico, y la decisión de ayer puede estar hoy anclada a datos viejos.",
              ],
              errors: [
                "Elegir caché sin nombrar la frescura que pierdes ni verificar que el caso la tolera.",
                "Microservicios por moda sin pagar la operación: cada caja es un SLA que sostener.",
                "Fórmula seguridad en decisiones: 'se hizo así' sin el porqué ni su coste.",
              ],
              tips: [
                "Siempre enuncia: GANO / PAGO / POR QUÉ el caso lo acepta.",
                "Traduce el coste a números del dominio (ms, %, $) y al SLO.",
                "Si dudás entre tres opciones, la barata-simple-suficiente suele ganar.",
                "Revisa la decisión cuando el tráfico cambie de orden de magnitud.",
              ],
              exercise: [
                "Toma una decisión del sistema: enuncia GANO/PAGO/caso en 3 líneas.",
                "Compara caché con evento vs TTL para tu catálogo y calcula el coste de cada vía.",
                "Discute: ¿cuándo justificas un microservicio más sobre el monolito?",
              ],
              summary: [
                "Toda arquitectura es una serie de elecciones con costes aceptados.",
                "Nombrar las fuerzas y traducir el coste al negocio es el oficio.",
                "Los trade-offs se revisan con el tráfico y se convierten en reglas.",
              ],
              lang: "text",
              exampleFile: "trade-offs",
              desc: "Toma decisiones de diseño nombrando ganancias, costes y el caso que las acepta.",
            },
          },
        ],
      },
    ],
  },
];