// _curriculum_python.mjs
// Currículum del curso de Python para Backend.
// Exporta COURSES con todos los módulos y lecciones.
// Las lecciones existentes en disco se listan SIN body (no se regeneran);
// las nuevas llevan body completo (11 secciones estándar + metadatos).

export const COURSES = [
  {
    slug: "python",
    title: "Python para Backend",
    modules: [
      {
        id: "modulo-01",
        n: 1,
        t: "Introducción a Python",
        level: "basico",
        caption: "Los primeros pasos: instalar, ejecutar y leer/escribir datos.",
        desc: "Instala Python, usa el intérprete y la consola interactiva, y domina variables, tipos, entrada y salida de datos y comentarios.",
        subtitle: "Los primeros pasos: instalar, ejecutar y leer/escribir datos.",
        objectives: [
          "Instalar Python y ejecutar tu primer script",
          "Usar la consola interactiva y el intérprete",
          "Manejar variables, constantes y tipos de datos",
          "Trabajar con entrada y salida de datos",
          "Documentar el código con comentarios y docstrings",
        ],
        lessons: [
          {
            id: "instalando-python-y-el-interprete",
            title: "Instalando Python y el intérprete",
            duration: "8 min",
            level: "basico",
            category: "fundamentos",
            cardText: "Instala Python en tu sistema, entiende qué es el intérprete y verifica la instalación con un comando.",
            desc: "Instala Python en tu sistema y comprueba que el intérprete funciona correctamente.",
          },
          {
            id: "primer-script-y-consola-interactiva",
            title: "Tu primer script y la consola interactiva",
            duration: "8 min",
            level: "basico",
            category: "fundamentos",
            cardText: "Crea tu primer archivo .py y explora la consola interactiva (REPL) para probar código al vuelo.",
            desc: "Crea tu primer archivo .py y explora el REPL de Python.",
          },
          {
            id: "variables-constantes-y-tipos",
            title: "Variables, constantes y tipos de datos",
            duration: "10 min",
            level: "basico",
            category: "fundamentos",
            cardText: "Aprende int, float, str y bool, y cómo funciona la asignación con tipado dinámico.",
            desc: "Variables, constantes y los tipos de datos básicos de Python.",
          },
          {
            id: "entrada-y-salida-de-datos",
            title: "Entrada y salida de datos",
            duration: "8 min",
            level: "basico",
            category: "fundamentos",
            cardText: "Recibe datos del usuario con input() y muéstralos con print() y f-strings.",
            desc: "Recibe y muestra datos con input(), print() y f-strings.",
          },
          {
            id: "comentarios-y-docstrings",
            title: "Comentarios y docstrings",
            duration: "8 min",
            level: "basico",
            category: "fundamentos",
            cardText: "Documenta tu código con comentarios de una línea y docstrings profesionales.",
            desc: "Documenta tu código con comentarios y docstrings.",
          },
        ],
      },
      {
        id: "modulo-02",
        n: 2,
        t: "Control de flujo y funciones",
        level: "basico",
        caption: "Que tu código tome decisiones y se repita por ti.",
        desc: "Condicionales, bucles y funciones: las piezas que convierten un script lineal en lógica de backend real.",
        subtitle: "Condicionales, bucles y funciones para escribir lógica reutilizable.",
        objectives: [
          "Tomar decisiones con if, elif y else",
          "Repetir trabajo con bucles for y while",
          "Definir funciones con parámetros y retorno",
          "Usar argumentos posicionales y por palabra clave",
          "Combinar estas piezas para validar datos de backend",
        ],
        lessons: [
          {
            id: "condicionales-if-elif-else",
            title: "Condicionales: if, elif y else",
            duration: "9 min",
            level: "basico",
            category: "control de flujo",
            cardText: "Toma decisiones en tu código con if, elif y else usando comparaciones y operadores lógicos.",
            desc: "Aprende if, elif y else en Python para tomar decisiones en tu backend.",
            body: {
              intro: [
                "Las APIs y servicios reciben datos que cambian según la petición. Tu código necesita decidir qué hacer frente a cada caso: ¿el usuario tiene permisos? ¿el campo viene vacío? ¿el status es 200 o 404? Eso se resuelve con condicionales.",
                "En Python las condiciones se escriben con if, elif y else, y el bloque de código que se ejecuta se marca por indentación.",
              ],
              def: {
                par: "Un condicional evalúa una expresión booleana y ejecuta un bloque de código solo si el resultado es verdadero. elif encadena condiciones alternativas y else captura el caso restante.",
                porQue: "Todo backend toma decisiones: autorización, validaciones, enrutado lógico y control de errores. Sin condicionales el código se ejecutaría siempre en línea recta.",
                cuando: "Usa si verificas una condición de entrada, un estado de usuario, el valor de una variable de configuración o el código de respuesta de otra API.",
              },
              concepts: [
                "if evalúa una condición y ejecuta el bloque si es True.",
                "elif permite probar condiciones adicionales cuando la anterior falla.",
                "else ejecuta el bloque final si ninguna condición se cumplió.",
                "La indentación (4 espacios) delimita el bloque de cada rama.",
                "Operadores: ==, !=, <, >, <=, >= y los lógicos and, or, not.",
              ],
              basic: {
                p: "Vamos a clasificar un código de estado HTTP con una cadena de condiciones.",
                code: [
                  "status = 200",
                  "if status >= 200 and status < 300:  # el bloque va indentado",
                  '    print("Respuesta OK, procesando cuerpo...")',
                  "elif status == 404:",
                  '    print("Recurso no encontrado")',
                  "elif status >= 500:",
                  '    print("Error del servidor, reintenta en 5 segundos")',
                  "else:",
                  '    print("Respuesta inesperada")',
                ],
                out: "Respuesta OK, procesando cuerpo...",
                lang: "python",
              },
              intermediate: {
                p: "Combinando comparaciones con operadores lógicos para validar varios campos de una petición.",
                code: [
                  "usuario = {\"rol\": \"editor\", \"activo\": True, \"intentos\": 2}",
                  'if usuario["rol"] == "admin" and usuario["activo"]:',
                  '    print("Acceso completo")',
                  'if usuario["rol"] == "editor" or usuario["intentos"] < 3:',
                  '    print("Acceso parcial concedido")',
                  "else:",
                  '    print("Cuenta bloqueada")',
                ],
                caption: "Validación de múltiples condiciones con and y or",
              },
              real: {
                p: "En un backend real sueles decidir qué respuesta devolver. Este patrón de clasificación es común en clientes HTTP y validadores de peticiones.",
                ul: [
                  "Devuelve pronto: evalúa primero los casos más urgentes.",
                  "Evita condiciones gigantes; refactoriza a funciones claras.",
                  "Usa constantes nombradas para que códigos como 404 sean legibles.",
                ],
                code: [
                  "def clasificar_respuesta(status, error_mensaje):",
                  "    if status is None:",
                  '        return "error", "Sin respuesta del servidor"',
                  "    if 200 <= status < 300:",
                  '        return "ok", "Peticion completada"',
                  "    if status == 404:",
                  '        return "error", "El recurso solicitado no existe"',
                  "    if status >= 500:",
                  '        return "error", f"Fallo interno del servidor: {error_mensaje}"',
                  '    return "desconocido", "Estado no contemplado"',
                  "",
                  'tipo, mensaje = clasificar_respuesta(503, "timeout en la base de datos")',
                  'print(tipo, "-", mensaje)',
                ],
                nota: "En FastAPI o Express usarás este tipo de lógica en middlewares y validadores antes de tocar la base de datos.",
              },
              expl: [
                "La condición de un if siempre se reduce a un booleano: variables como 0, \"\" y None se consideran falsas.",
                "elif ahorra anidar if dentro de else, dejando el código plano y legible.",
                "Evalúa arriba los casos más probables o los más baratos de comprobar.",
                "Un dict o match puede sustituir cadenas largas de elif cuando comparas un mismo valor.",
                "La indentación inconsistente rompe el programa con IndentationError.",
              ],
              errors: [
                "SyntaxError: expected ':' cuando olvidas los dos puntos al final de la condición.",
                "IndentationError al mezclar espacios y tabulaciones.",
                "Confundir = (asignación) con == (comparación).",
              ],
              tips: [
                "Escribe condiciones legibles: 200 <= status < 300 en lugar de status >= 200 and status < 300.",
                "Evita else innecesarios con guard clauses (return anticipado).",
                "Usa constantes con nombre para códigos mágicos: HTTP_OK = 200.",
                "Comprueba primero None o vacío para no reventar al acceder a atributos.",
              ],
              exercise: [
                "Escribe una función nivel_acceso(rol) que devuelva el nivel según admin, editor o visitante.",
                "Clasifica las edades de un diccionario de usuarios en mayor o menor usando condicionales.",
                "Agrega el caso de estado 3xx a clasificar_respuesta.",
              ],
              summary: [
                "if/elif/else deciden qué bloque se ejecuta según una condición booleana.",
                "Los operadores de comparación y lógicos componen condiciones poderosas.",
                "Las guard clauses y las constantes nombradas hacen el código de backend más claro.",
              ],
              lang: "python",
              exampleFile: "condicionales",
              desc: "Aprende if, elif y else en Python para tomar decisiones en tu backend.",
            },
          },
          {
            id: "bucles-for-y-while",
            title: "Bucles: for y while",
            duration: "10 min",
            level: "basico",
            category: "control de flujo",
            cardText: "Repite tareas con for y while: recorre listas, filtra datos y procesa registros.",
            desc: "Repite tareas con bucles for y while en Python.",
            body: {
              intro: [
                "Los bucles te permiten repetir una operación sobre muchos datos: recorrer una lista de pedidos, leer líneas de un log o construir una respuesta agregada.",
                "Python ofrece dos: for, perfecto para recorrer colecciones, y while, para repetir mientras se cumpla una condición.",
              ],
              def: {
                par: "for itera sobre cada elemento de una secuencia (lista, tupla, string, dict). while repite el bloque mientras su condición siga siendo verdadera.",
                porQue: "El backend procesa colecciones constantemente: usuarios en memoria, filas de una tabla, eventos de un log. Un bucle bien escrito sustituye cientos de líneas repetidas.",
                cuando: "Usa for para recorrer algo conocido; usa while cuando no sabes cuántas iteraciones harán falta: colas, reintentos, encuestas.",
              },
              concepts: [
                "range(n) genera números de 0 a n-1 para iterar.",
                "enumerate(coleccion) entrega índice y elemento a la vez.",
                "break corta el bucle; continue salta a la siguiente iteración.",
                "Una comprensión de lista es un for condensado que devuelve una lista nueva.",
                "Los diccionarios se recorren con .items() para obtener clave y valor.",
              ],
              basic: {
                p: "Recorrer una lista de usuarios y construir una lista filtrada con comprensión.",
                code: [
                  "usuarios = [\"ana\", \"luis\", \"carla\"]",
                  "for i, u in enumerate(usuarios, start=1):",
                  '    print(f"{i}. {u}")',
                  "",
                  "pares = [n for n in range(10) if n % 2 == 0]",
                  "print(pares)",
                ],
                out: "1. ana\n2. luis\n3. carla\n[0, 2, 4, 6, 8]",
                lang: "python",
              },
              intermediate: {
                p: "Filtrar registros mientras recorremos un dataset con for y acumular totales.",
                caption: "Procesamiento y filtrado de datos en un solo paso",
                code: [
                  "pedidos = [",
                  '    {"id": 1, "total": 120.00, "estado": "entregado"},',
                  '    {"id": 2, "total": 350.50, "estado": "pendiente"},',
                  '    {"id": 3, "total": 90.99, "estado": "entregado"},',
                  "]",
                  "total_pendiente = 0",
                  "entregados = []",
                  "for pedido in pedidos:",
                  '    if pedido["estado"] == "entregado":',
                  '        entregados.append(pedido["total"])',
                  "    else:",
                  '        total_pendiente += pedido["total"]',
                  "print(\"Entregados:\", entregados)",
                  "print(\"Pendiente de cobro:\", total_pendiente)",
                ],
              },
              real: {
                p: "Un caso típico en backend: leer un log línea a línea y quedarte solo con los errores críticos.",
                ul: [
                  "Procesa con streams en lugar de cargar todo a memoria.",
                  "Usa break para detenerte al encontrar lo buscado.",
                  "Combina con análisis de cadenas para extraer campos.",
                ],
                code: [
                  'with open("servidor.log", "r", encoding="utf-8") as f:',
                  "    errores = []",
                  "    while linea := f.readline():",
                  '        if "ERROR" in linea and "BBDD" not in linea:',
                  "            errores.append(linea.strip())",
                  "        if len(errores) >= 10:",
                  "            break",
                  'print(f"Se encontraron {len(errores)} errores críticos")',
                  "for e in errores:",
                  "    print(e)",
                ],
                nota: "El operador := (walrus) asigna y evalúa en la misma línea; es una joya moderna de Python 3.8+.",
              },
              expl: [
                "for recorre elementos; range solo produce números para controlar recorridos numéricos.",
                "Los bucles se detienen con break y se saltan elementos con continue.",
                "Un else tras un for se ejecuta si el bucle terminó sin break; útil en búsquedas.",
                "Las comprensiones de lista son más rápidas y legibles para filtrar y transformar.",
              ],
              errors: [
                "Olvidar la indentación del cuerpo del bucle.",
                "Incrementar manualmente una variable en un for en vez de recorrer la colección.",
                "Crear un while infinito al no cambiar la condición dentro del bloque.",
              ],
              tips: [
                "Prefiere for sobre while cuando recorres colecciones conocidas.",
                "Usa enumerate en lugar de indexar con range(len(...)).",
                "Procesa archivos con with y línea a línea para no agotar memoria.",
                "Nombra el iterador de forma significativa: for pedido in pedidos.",
              ],
              exercise: [
                "Suma los totales de una lista de dicts con un for.",
                "Genera la lista de ids impares con una comprensión.",
                "Lee un archivo y cuenta las líneas vacías con un while.",
              ],
              summary: [
                "for recorre colecciones y while repite hasta cumplir una condición.",
                "break y continue controlan el flujo de la iteración.",
                "Las comprensiones condensan la transformación y el filtrado.",
              ],
              lang: "python",
              exampleFile: "bucles",
              desc: "Repite tareas con bucles for y while en Python.",
            },
          },
          {
            id: "funciones-def-y-retorno",
            title: "Funciones: def, parámetros y retorno",
            duration: "10 min",
            level: "basico",
            category: "funciones",
            cardText: "Define funciones con def, parámetros y retorno para reutilizar tu lógica de backend.",
            desc: "Define y usa funciones en Python para reutilizar tu lógica de backend.",
            body: {
              intro: [
                "Un script de una sola pieza se vuelve imposible de mantener. Las funciones te permiten aislar una tarea, darle un nombre y reutilizarla.",
                "En backend, cada validación, transformación o consulta suele vivir en una función pequeña con responsabilidad única.",
              ],
              def: {
                par: "Una función es un bloque de código reutilizable definido con def, que recibe parámetros, ejecuta su lógica y devuelve un resultado con return.",
                porQue: "Mantienen el código DRY (no te repitas), facilitan testear piezas aisladas y hacen el código autodocumentado.",
                cuando: "Usa funciones para toda operación repetida: normalizar texto, validar email, parsear fechas o construir tramas HTTP.",
              },
              concepts: [
                "def nombre(param1, param2): inicia la definición y el bloque va indentado.",
                "return devuelve un valor y termina la función; sin return se retorna None.",
                "Los parámetros pueden llevar valores por defecto.",
                "Se puede devolver varios valores en una tupla y desempaquetarla al llamar.",
                "Los docstrings documentan qué hace la función.",
              ],
              basic: {
                p: "Nuestra primera función reutilizable: validar un email con un esquema simple.",
                code: [
                  "def es_email_valido(email):",
                  '    return "@" in email and "." in email[email.index("@"):]',
                  "",
                  'correo = "ana.ejemplo@mail.com"',
                  "if es_email_valido(correo):",
                  '    print("Correo valido para registrar usuario")',
                  "else:",
                  '    print("Correo rechazado")',
                ],
                out: "Correo valido para registrar usuario",
                lang: "python",
              },
              intermediate: {
                p: "Funciones con retorno múltiple y valores por defecto para parsear datos de petición.",
                caption: "Retorno de tuplas y parámetros con valor por defecto",
                code: [
                  'def dividir_pedido(total, proveedor="local"):',
                  "    subtotal = total",
                  "    impuesto = round(subtotal * 0.16, 2)",
                  "    return subtotal, impuesto, proveedor",
                  "",
                  "sub, imp, prov = dividir_pedido(1000)",
                  'print(f"Subtotal: {sub}, impuesto: {imp}, proveedor: {prov}")',
                ],
              },
              real: {
                p: "Construyendo una función con capa de validación antes de devolver datos a la API.",
                ul: [
                  "Una función, una responsabilidad.",
                  "Valida y devuelve errores claros en lugar de excepciones mudas.",
                  "Registra logs en los puntos de entrada.",
                ],
                code: [
                  "def normalizar_usuario(usuario):",
                  '    if not usuario.get("email"):',
                  '        return {"ok": False, "error": "El email es obligatorio"}',
                  "    usuario_normalizado = {",
                  '        "nombre": usuario.get("nombre", "").strip().title(),',
                  '        "email": usuario.get("email").lower().strip(),',
                  "    }",
                  '    return {"ok": True, "usuario": usuario_normalizado}',
                  "",
                  'resultado = normalizar_usuario({"email": " LUIS@EJEMPLO.COM ", "nombre": "luis"})',
                  "print(resultado)",
                ],
                nota: "Este patrón de devolver un dict con ok y error es muy usado en handlers de FastAPI.",
              },
              expl: [
                "return detiene la ejecución: el código posterior no corre, útil para guard clauses.",
                "Si una función no tiene return explícito, devuelve None.",
                "Un parámetro con default permite llamadas más cortas y compatibles.",
                "Los docstrings con triple doble comilla documentan y activan el autocompletado en los IDE.",
              ],
              errors: [
                "NameError por llamar una función antes de definirla o fuera de su scope.",
                "TypeError cuando pasas argumentos de más o de menos.",
                "Devolver None y hacer operaciones sobre él sin comprobarlo.",
              ],
              tips: [
                "Dale nombres verbo-objeto: validar_usuario, generar_token.",
                "Prefiere retornos tempranos para validaciones.",
                "Documenta cada función con un docstring conciso.",
                "Mantén las funciones pequeñas; si pasa de 20 líneas, divídela.",
              ],
              exercise: [
                "Crea una función calcular_iva(total, tasa=0.16) que devuelva el iva y el total.",
                "Escribe validar_edad(edad) que devuelva un mensaje según rangos.",
                "Agrega docstring a todas tus funciones del ejercicio anterior.",
              ],
              summary: [
                "def define funciones reutilizables con parámetros y retorno.",
                "return detiene la ejecución y entrega el resultado.",
                "Los valores por defecto y los docstrings hacen funciones robustas.",
              ],
              lang: "python",
              exampleFile: "funciones",
              desc: "Define y usa funciones en Python.",
            },
          },
          {
            id: "argumentos-y-kwargs",
            title: "Argumentos posicionales y keyword",
            duration: "9 min",
            level: "basico",
            category: "funciones",
            cardText: "Pasa argumentos posicionales y por nombre, y domina *args y **kwargs.",
            desc: "Domina argumentos posicionales, keyword, *args y **kwargs en Python.",
            body: {
              intro: [
                "Python ofrece una flexibilidad enorme a la hora de pasar datos a una función: posicionalmente, por nombre, o agrupados en listas y diccionarios.",
                "Entender argumentos y kwargs es clave para escribir helpers de backend que reciban configuraciones variables sin romper llamadas.",
              ],
              def: {
                par: "Los argumentos posicionales se asignan por orden; los keyword se pasan con su nombre. *args recoge los extra posicionales en una tupla y **kwargs los extra por nombre en un dict.",
                porQue: "Las firmas flexibles permiten utilidades y librerías compartidas, evitando miles de parámetros de más y llamadas frágiles.",
                cuando: "Usa *args y **kwargs en wrappers, decoradores, logging y funciones de configuración que reciben opciones variables.",
              },
              concepts: [
                "Llamadas como f(1, 2, 3) son posicionales; f(b=2) son keyword.",
                "*args agrupa los posicionales extra en una tupla.",
                "**kwargs agrupa los pares clave/valor extra en un dict.",
                "Se puede desempaquetar listas con *lista y dicts con **dict al llamar.",
                "El orden de la firma es: posicionales, *args, keyword-only, **kwargs.",
              ],
              basic: {
                p: "Un logger que acepta una cantidad variable de datos extra y los agrupa.",
                code: [
                  "def registrar(nivel, *args, **kwargs):",
                  '    contexto = " ".join(str(a) for a in args)',
                  '    print(f"[{nivel.upper()}] {contexto}")',
                  "    if kwargs:",
                  "        print(\"   ->\", kwargs)",
                  "",
                  'registrar("info", "conexion", "ok")',
                  'registrar("error", "timeout", host="db-01", puerto=5432)',
                ],
                out: "[INFO] conexion ok\n   -> {}\n[ERROR] timeout\n   -> {'host': 'db-01', 'puerto': 5432}",
                lang: "python",
              },
              intermediate: {
                p: "Desempaquetado de argumentos desde listas y diccionarios al llamar a una función.",
                caption: "Asterisco para desempaquetar en la llamada",
                code: [
                  "def conectar(host, puerto, ssl):",
                  '    print(f"Conectando a {host}:{puerto} ssl={ssl}")',
                  "",
                  'conf = {"host": "localhost", "puerto": 5432, "ssl": True}',
                  "conectar(**conf)",
                  "",
                  'nodos = ["192.168.1.10", 8080, False]',
                  "conectar(*nodos)",
                ],
              },
              real: {
                p: "Un wrapper que registra errores y añade contexto sin romper las firmas de cada función.",
                ul: [
                  "Reutiliza firmas complejas sin reescribirlas.",
                  "Añade logging sin tocar cada llamada.",
                  "Los kwargs hacen explícita la configuración.",
                ],
                code: [
                  "def con_log(nombre_servicio):",
                  "    def envoltura(func):",
                  "        def interna(*args, **kwargs):",
                  "            try:",
                  "                resultado = func(*args, **kwargs)",
                  "                return resultado",
                  "            except Exception as error:",
                  '                print(f"[{nombre_servicio}] fallo: {error}")',
                  "                raise",
                  "        return interna",
                  "    return envoltura",
                  "",
                  '@con_log("auth-api")',
                  "def obtener_usuario(usuario_id):",
                  '    if usuario_id != 1:',
                  '        raise ValueError("usuario no encontrado")',
                  '    return {"id": 1, "nombre": "Ana"}',
                  "",
                  "print(obtener_usuario(1))",
                ],
                nota: "Este es estructuralmente un decorador: reutiliza la firma original de obtener_usuario pase lo que pase.",
              },
              expl: [
                "*args y **kwargs solo dictan el empaquetado; el nombre puede ser cualquiera (convención args/kwargs).",
                "Los keyword-only arguments van después de * y obligan a pasar el dato por nombre.",
                "El orden correcto de la firma evita TypeError en tiempo de ejecución.",
                "El desempaquetado hace las llamadas mucho más expresivas.",
              ],
              errors: [
                "TypeError por mezclar posicionales después de keyword en la llamada.",
                "TypeError si olvidas el doble asterisco delante del dict.",
                "Confundir *args (tupla) con **kwargs (dict) al iterar.",
              ],
              tips: [
                "No apliques **kwargs cuando la firma sea fija; la firma explícita autodocumenta el código.",
                "Aplica *args y **kwargs solo en wrappers y utilidades compartidas.",
                "Itera kwargs con .items() para más legibilidad.",
                "Mantén valores por defecto en los parámetros fijos, no en kwargs sueltos.",
              ],
              exercise: [
                "Crea conectarse(**parametros) que imprima host y puerto de un dict.",
                "Escribe un wrapper multiplicar(*numeros) que devuelva el producto.",
                "Crea un decorador de logging que añada un timestamp.",
              ],
              summary: [
                "Los posicionales se asignan por orden; los keyword por nombre.",
                "*args empaqueta posicionales extra y **kwargs pares clave-valor.",
                "El desempaquetado (*lista, **dict) llena llamadas dinámicas.",
              ],
              lang: "python",
              exampleFile: "argumentos",
              desc: "Argumentos posicionales, keyword, *args y **kwargs.",
            },
          },
        ],
      },
      {
        id: "modulo-03",
        n: 3,
        t: "Estructuras de datos",
        level: "intermedio",
        caption: "Las colecciones que ordenan, agrupan y buscan tus datos.",
        desc: "Listas, strings, tuplas, dicts y conjuntos: las estructuras con las que operas los datos que llegan y salen de tu backend.",
        subtitle: "Domina listas, strings, tuplas, dicts y conjuntos para modelar y transformar datos.",
        objectives: [
          "Dominar listas: slicing, ordenación y copias",
          "Trabajar con strings y sus métodos de limpieza",
          "Empaquetar datos con tuplas y desempaquetado",
          "Recorrer y buscar en dicts y conjuntos",
          "Transformar colecciones con comprensiones",
        ],
        lessons: [
          {
            id: "dicts-y-conjuntos",
            title: "Dicts y conjuntos",
            duration: "10 min",
            level: "intermedio",
            category: "dicts",
            cardText: "Modela datos con diccionarios y conjuntos: claves, valores, membresías y operaciones de conjuntos.",
            desc: "Diccionarios y conjuntos para modelar y buscar datos.",
          },
          {
            id: "listas-en-profundidad",
            title: "Listas en profundidad",
            duration: "10 min",
            level: "intermedio",
            category: "listas",
            cardText: "Slicing, ordenación, copias y comprensiones para manipular listas como un profesional.",
            desc: "Slicing, ordenación, copias y comprensiones de listas.",
            body: {
              intro: [
                "Las listas son la colección que más usas en backend: listas de resultados de una consulta, de pedidos, de ids para una API.",
                "En esta lección vas más allá de append y for: slicing, ordenación con claves, copias seguras y comprensiones potentes.",
              ],
              def: {
                par: "Una lista es una secuencia ordenada y mutable de elementos que se indexa desde 0 y se manipula con métodos y operadores.",
                porQue: "Casi todos los datasets que se procesan llegan como listas; dominarlas es procesar datos a velocidad real.",
                cuando: "Usa listas para resultados de consultas, colas de trabajo, historiales y agrupaciones en memoria.",
              },
              concepts: [
                "Los índices empiezan en 0; los negativos cuentan desde el final.",
                "El slicing lista[inicio:fin:paso] devuelve una copia parcial.",
                "sorted() ordena sin mutar; .sort() ordena en el lugar.",
                "append, extend, insert, remove y pop editan la lista.",
                "Las comprensiones [exp for x in it if cond] construyen listas nuevas.",
              ],
              basic: {
                p: "Slicing y operaciones básicas sobre una lista de nombres.",
                code: [
                  "nombres = [\"ana\", \"luis\", \"carla\", \"pedro\"]",
                  "print(nombres[0])        # primer elemento",
                  "print(nombres[-1])       # ultimo elemento",
                  "print(nombres[1:3])      # slice del indice 1 al 2",
                  "print(nombres[::2])      # de dos en dos",
                  'nombres.append("sofia")',
                  "nombres.sort()",
                  "print(nombres)",
                ],
                out: "ana\npedro\n['luis', 'carla']\n['ana', 'carla']\n['ana', 'carla', 'luis', 'pedro', 'sofia']",
                lang: "python",
              },
              intermediate: {
                p: "Ordenar un listado de usuarios por un campo y filtrar con comprensión.",
                caption: "Ordenación con key y filtrado con comprensión de lista",
                code: [
                  "usuarios = [",
                  '    {"nombre": "Ana", "puntos": 12},',
                  '    {"nombre": "Luis", "puntos": 30},',
                  '    {"nombre": "Carla", "puntos": 7},',
                  "]",
                  'ordenados = sorted(usuarios, key=lambda u: u["puntos"], reverse=True)',
                  'top = [u["nombre"] for u in ordenados if u["puntos"] >= 10]',
                  "print(top)",
                ],
              },
              real: {
                p: "Paginación en memoria de una lista grande de resultados antes de devolverla en una API.",
                ul: [
                  "Un slice para paginar de (pagina-1)*limite a pagina*limite.",
                  "Una comprensión para mapear o filtrar los items.",
                  "El dict de respuesta incluye total, pagina y limite.",
                ],
                code: [
                  "def paginar(datos, pagina=1, limite=10):",
                  "    inicio = (pagina - 1) * limite",
                  "    fin = inicio + limite",
                  "    pagina_datos = datos[inicio:fin]",
                  "    return {",
                  '        "total": len(datos),',
                  '        "pagina": pagina,',
                  '        "limite": limite,',
                  '        "items": pagina_datos,',
                  "    }",
                  "",
                  "resultados = list(range(1, 26))",
                  "print(paginar(resultados, pagina=2, limite=5))",
                ],
                nota: "En producción la paginación suele caer en la base de datos (LIMIT/OFFSET), pero este patrón vale para listas en memoria y cachés.",
              },
              expl: [
                "El slicing no modifica la lista original: crea una copia.",
                ".sort() altera el estado original; sorted() lo respeta.",
                "Las comprensiones son más rápidas y legibles que un for + append.",
                "key recibe una función; lambda permite claves rápidas sin definir función aparte.",
              ],
              errors: [
                "IndexError al acceder a un índice fuera de rango.",
                "Modificar una lista mientras se itera provoca saltos; itera sobre una copia.",
                "Ordenar una lista mixta de tipos lanza TypeError en la comparación.",
              ],
              tips: [
                "Usa sorted() cuando quieras mantener la lista original intacta.",
                "Define claves con nombres en lugar de lambdas si se repiten.",
                "Usa .copy() o [:] para evitar aliasing involuntario.",
                "Para colas FIFO usa collections.deque, no list.pop(0).",
              ],
              exercise: [
                "Toma una lista de temperaturas y quédate con las mayores a 20 con una comprensión.",
                "Ordena una lista de dicts de productos por precio descendente.",
                "Implementa paginar() y prueba bordes como pagina=0.",
              ],
              summary: [
                "Slicing y métodos construyen y transforman listas en una línea.",
                "sorted() con key y reverse ordena estructuras complejas.",
                "Las comprensiones condensan filtro y transformación.",
              ],
              lang: "python",
              exampleFile: "listas",
              desc: "Slicing, ordenación y comprensiones de listas.",
            },
          },
          {
            id: "strings-y-metodos",
            title: "Strings y sus métodos",
            duration: "9 min",
            level: "intermedio",
            category: "strings",
            cardText: "Limpia, divide, une y formatea texto con los métodos de str.",
            desc: "Limpia, divide, une y formatea texto con los métodos de str.",
            body: {
              intro: [
                "El texto es la materia prima de cualquier API: los JSONs llegan como strings, los logs se leen como strings y las URLs se parsean como strings.",
                "Los strings en Python son inmutables: cada método devuelve un string nuevo. Conocer sus métodos te ahorra horas de parseo manual.",
              ],
              def: {
                par: "Un string es una secuencia inmutable de caracteres Unicode que ofrece métodos de limpieza, búsqueda, división y formato.",
                porQue: "Los datos que entran por HTTP nunca llegan limpios: con espacios, mayúsculas y saltos. Los métodos de str los normalizan en una línea.",
                cuando: "Usa métodos string para limpiar inputs, dividir emails, unir rutas y generar texto de respuestas.",
              },
              concepts: [
                "strip() elimina espacios y saltos; lower() y upper() cambian mayúsculas.",
                "split(sep) divide; join(sep) une con un separador.",
                "startswith(), endswith() e in para buscar patrones.",
                "replace(a, b) sustituye substrings.",
                "Las f-strings y format() interpolan variables.",
              ],
              basic: {
                p: "Limpieza de una entrada sucia y división de una línea de texto.",
                code: [
                  'entrada = "  ana.ejemplo@MAIL.COM\\n "',
                  "limpio = entrada.strip().lower()",
                  "print(repr(limpio))",
                  "",
                  'linea = "GET /api/usuarios HTTP/1.1"',
                  'metodo, ruta, protocolo = linea.split(" ")',
                  "print(metodo, ruta)",
                ],
                out: "'ana.ejemplo@mail.com'\nGET /api/usuarios",
                lang: "python",
              },
              intermediate: {
                p: "Normalizar nombres propios y construir una URL con join y f-strings.",
                caption: "Capitalización, join y f-strings",
                code: [
                  'nombre = "ana maria garcia"',
                  "nombre_norm = nombre.title()",
                  "print(nombre_norm)",
                  "",
                  'partes = ["api", "v1", "usuarios", "activos"]',
                  'url = "/".join(partes)',
                  'print(f"GET https://mi-servidor.com/{url}")',
                ],
              },
              real: {
                p: "Parsear una línea de log y extraer sus campos de forma robusta.",
                ul: [
                  "Usa rsplit para cortar desde el final del texto.",
                  "Divide el parseo en pasos pequeños y verificables.",
                  "Normaliza los valores extraídos para indexarlos.",
                ],
                code: [
                  'log = "2026-09-23T10:12:33Z WARN timeout en db-01"',
                  'fecha, _, nivel, resto = log.split(" ", 3)',
                  "print(\"Nivel:\", nivel)",
                  "print(\"Fecha:\", fecha)",
                  "print(\"Mensaje:\", resto)",
                  "",
                  'clave = fecha.replace("-", "")',
                  "print(\"Clave de index:\", clave)",
                ],
                nota: "str.split(sep, maxsplit) limita la cantidad de cortes: evita listas rotas cuando el mensaje trae espacios.",
              },
              expl: [
                "Los strings son inmutables; toda transformación devuelve un objeto nuevo.",
                "split con maxsplit protege el contenido que contiene el separador.",
                "join es el inverso natural de split y es más rápido que += en bucles.",
                "repr() es excelente para ver saltos y espacios ocultos en depuración.",
              ],
              errors: [
                "ValueError al desempaquetar split() cuando la cantidad de partes no calza.",
                "Comparar strings con mayúsculas distintas y creer que son iguales.",
                "Olvidar que replace devuelve un string nuevo y no muta el original.",
              ],
              tips: [
                "Normaliza (strip + lower) todo input de usuario antes de comparar.",
                "En bucles, concatena con join, no con +=.",
                "Usa f-strings por claridad en lugar de muchos format.",
                "Valida rutas y tipos MIME con startswith y endswith.",
              ],
              exercise: [
                "Normaliza una lista de emails sucios a minúsculas y sin espacios.",
                "Divide una línea tipo clave=valor y extrae ambos campos.",
                "Con una lista de nombres sucios, escribe title() sobre cada uno.",
              ],
              summary: [
                "Los strings son inmutables y ricos en métodos de limpieza y búsqueda.",
                "split y join dividen y reconstruyen texto en una línea.",
                "Normalizar con strip, lower y title evita bugs con datos reales.",
              ],
              lang: "python",
              exampleFile: "strings",
              desc: "Métodos de strings para limpiar y formatear texto.",
            },
          },
          {
            id: "tuplas-y-desempaquetado",
            title: "Tuplas y desempaquetado",
            duration: "9 min",
            level: "intermedio",
            category: "tuplas",
            cardText: "Agrupa y desagrupa datos con tuplas y desempaquetado seguro.",
            desc: "Tuplas inmutables y desempaquetado de iterables.",
            body: {
              intro: [
                "Cuando una función devuelve varios valores o quieres un grupo inmutable, las tuplas son la elección natural.",
                "Junto con el desempaquetado, escriben código muy expresivo en backend: coordenadas, pares clave-valor, intervalos.",
              ],
              def: {
                par: "Una tupla es una secuencia inmutable que agrupa valores; el desempaquetado asigna sus elementos a variables en una sola línea.",
                porQue: "Los datos estructurales (un rango, un endpoint con verbo, una coordenada) no deberían mutar; las tuplas protegen y documentan eso.",
                cuando: "Usa tuplas para retornos múltiples, claves de dict, configs inmutables y conjuntos de constantes.",
              },
              concepts: [
                "Una tupla se crea con (a, b) o con la coma: a, b = 1, 2.",
                "Es inmutable: no admite append ni asignación de índices.",
                "El desempaquetado a, b = tupla funciona con cualquier iterable.",
                "El asterisco captura el resto: primero, *resto = datos.",
                "Para ignorar valores usa el guion bajo.",
              ],
              basic: {
                p: "Crear y desempaquetar una tupla de configuración.",
                code: [
                  'config = ("db-01", 5432)',
                  "servidor, puerto = config",
                  'print(f"Conectando a {servidor} por el puerto {puerto}")',
                  "",
                  "primero, *resto = [1, 2, 3, 4]",
                  "print(primero, resto)",
                ],
                out: "Conectando a db-01 por el puerto 5432\n1 [2, 3, 4]",
                lang: "python",
              },
              intermediate: {
                p: "Recorrer un dict y desempaquetar clave y valor con .items().",
                caption: "Desempaquetado de pares clave-valor en un bucle",
                code: [
                  'usuario = {"nombre": "Ana", "rol": "admin", "activo": True}',
                  "for clave, valor in usuario.items():",
                  '    print(f"{clave}: {valor}")',
                  "",
                  'nombre, rol, _ = (usuario["nombre"], usuario["rol"], usuario["activo"])',
                  "print(nombre, rol)",
                ],
              },
              real: {
                p: "Intercambiar variables y devolver rangos desde utilidades de paginación.",
                ul: [
                  "Swap de variables sin variable temporal.",
                  "Retorno múltiple que el llamador desempaqueta.",
                  "Valores inmutables para constantes de configuración.",
                ],
                code: [
                  "def rango_pagina(pagina, limite):",
                  "    inicio = (pagina - 1) * limite",
                  "    return inicio, inicio + limite",
                  "",
                  "inicio, fin = rango_pagina(3, 10)",
                  'print(f"Rows {inicio} a {fin}")',
                  "",
                  'a, b = "db-01", "db-02"',
                  "b, a = a, b   # swap",
                  "print(a, b)",
                ],
                nota: "El swap b, a = a, b funciona porque Python evalúa el lado derecho completo antes de asignar.",
              },
              expl: [
                "Las tuplas son inmutables: imposible modificar datos por accidente.",
                "El desempaquetado obliga a que la cantidad de variables coincida con la de elementos.",
                "El asterisco captura el resto y facilita trabajar con listas de tamaño variable.",
                "Los pares clave-valor de .items() se desempaquetan naturalmente en bucles.",
              ],
              errors: [
                "ValueError: too many values to unpack al usar más variables que elementos.",
                "Tuple index assignment lanza TypeError por inmutabilidad.",
                "Olvidar la coma en tuplas de un solo elemento: (5) no es tupla, (5,) sí.",
              ],
              tips: [
                "Usa tuplas para datos que nunca deban cambiar: rutas, puertos, constantes.",
                "Desempaqueta con nombres claros que expliquen cada elemento.",
                "Usa _ para marcar los valores que descartas.",
                "Prefiere tuplas a listas para los retornos múltiples de funciones.",
              ],
              exercise: [
                "Crea una tupla llamada constantes con (10, 20, 30) y desempaquétala en 3 variables.",
                "Escribe una función devolver_estado() que retorne (codigo, mensaje) y captura ambos.",
                "Intercambia dos variables sin variable auxiliar.",
              ],
              summary: [
                "Las tuplas agrupan datos inmutables.",
                "El desempaquetado asigna muchos valores en una línea.",
                "*resto y _ hacen flexible el unpacking con listas variables.",
              ],
              lang: "python",
              exampleFile: "tuplas",
              desc: "Tuplas y desempaquetado de iterables.",
            },
          },
        ],
      },
      {
        id: "modulo-04",
        n: 4,
        t: "Programación orientada a objetos",
        level: "intermedio",
        caption: "Modela el dominio de tu backend con clases.",
        desc: "Clases, herencia, encapsulación y métodos mágicos para organizar el dominio de tu aplicación en objetos.",
        subtitle: "Clases y objetos para estructurar la lógica de negocio.",
        objectives: [
          "Definir clases y crear objetos con atributos y métodos",
          "Reutilizar y especializar con herencia y polimorfismo",
          "Proteger el estado con encapsulación y propiedades",
          "Personalizar objetos con métodos mágicos __dunder__",
        ],
        lessons: [
          {
            id: "clases-y-objetos",
            title: "Clases y objetos",
            duration: "11 min",
            level: "intermedio",
            category: "poo",
            cardText: "Define clases con atributos y métodos para modelar tu dominio.",
            desc: "Clases, objetos, atributos y métodos en Python.",
            body: {
              intro: [
                "Los objetos agrupan datos y el comportamiento que actúa sobre ellos. En backend, las clases modelan el dominio: Usuario, Pedido, Token, Servicio.",
                "Conocer clases te permite escribir código organizado, reutilizable y testeable.",
              ],
              def: {
                par: "Una clase es una plantilla; un objeto es una instancia concreta creada a partir de ella, con su propio estado.",
                porQue: "Modelar el dominio en clases hace que la lógica de negocio viva junto a sus datos y sea fácil de probar y extender.",
                cuando: "Usa clases para entidades con estado (usuarios, pedidos) y para agrupar operaciones relacionadas de un servicio.",
              },
              concepts: [
                "class Nombre: define la plantilla.",
                "El método __init__ configura el estado inicial de cada objeto.",
                "self referencia la instancia actual dentro de los métodos.",
                "Los atributos son datos; los métodos son funciones del objeto.",
                "Se instancia con NombreClase(*args): cada objeto tiene identidad propia.",
              ],
              basic: {
                p: "Modelamos un Usuario con campos y una acción de presentación.",
                code: [
                  "class Usuario:",
                  "    def __init__(self, nombre, email):",
                  "        self.nombre = nombre",
                  "        self.email = email",
                  "",
                  "    def saludo(self):",
                  '        return f"Hola, soy {self.nombre}"',
                  "",
                  'ana = Usuario("Ana", "ana@mail.com")',
                  'luis = Usuario("Luis", "luis@mail.com")',
                  "print(ana.saludo())",
                  "print(luis.email)",
                ],
                out: "Hola, soy Ana\nluis@mail.com",
                lang: "python",
              },
              intermediate: {
                p: "Agregamos métodos que operan sobre el estado: cambiar el email y validar permisos.",
                caption: "Métodos que leen y actualizan el estado del objeto",
                code: [
                  'class Usuario:',
                  '    def __init__(self, nombre, email, rol="viewer"):',
                  "        self.nombre = nombre",
                  "        self.email = email",
                  "        self.rol = rol",
                  "",
                  "    def actualizar_email(self, nuevo_email):",
                  '        if "@" not in nuevo_email:',
                  '            raise ValueError("Email invalido")',
                  "        self.email = nuevo_email",
                  "",
                  "    def puede_escribir(self):",
                  '        return self.rol in ("admin", "editor")',
                  "",
                  'ana = Usuario("Ana", "ana@mail.com", "admin")',
                  'ana.actualizar_email("ana.p@mail.com")',
                  "print(ana.email)",
                  "print(ana.puede_escribir())",
                ],
              },
              real: {
                p: "Entidad de dominio con un método to_dict para exponerla en una API JSON.",
                ul: [
                  "El modelo contiene su propia validación.",
                  "to_dict controla qué se expone (por ejemplo, sin contraseñas).",
                  "El código de la API queda simple: recoger datos e invocar el objeto.",
                ],
                code: [
                  "class Pedido:",
                  "    def __init__(self, pedido_id, cliente, total):",
                  "        self.id = pedido_id",
                  "        self.cliente = cliente",
                  "        self.total = total",
                  '        self.estado = "pendiente"',
                  "",
                  "    def confirmar(self):",
                  '        self.estado = "confirmado"',
                  "        self.total = round(self.total * 1.16, 2)",
                  "",
                  "    def to_dict(self):",
                  "        return {",
                  '            "id": self.id,',
                  '            "cliente": self.cliente,',
                  '            "total": self.total,',
                  '            "estado": self.estado,',
                  "        }",
                  "",
                  'pedido = Pedido(1, "Ana", 100)',
                  "pedido.confirmar()",
                  "import json",
                  "print(json.dumps(pedido.to_dict(), indent=2))",
                ],
                nota: "Este mapeo de objeto a dict es el paso previo a Pydantic y FastAPI, que verás en el módulo 7.",
              },
              expl: [
                "__init__ no crea el objeto: lo inicializa. El objeto nace cuando llamas a la clase.",
                "self es obligatorio y siempre va primero; agrupa el estado de la instancia.",
                "Los atributos y métodos se acceden con el punto.",
                "Varias instancias comparten el código de la clase pero no su estado.",
              ],
              errors: [
                "TypeError de __init__() faltando argumentos cuando olvidas los requeridos.",
                "NameError si usas un atributo sin declararlo en __init__.",
                "Olvidar self en un método provoca TypeError por argumentos descolocados.",
              ],
              tips: [
                "Declara todos los atributos en __init__ para que el estado sea predecible.",
                "Escribe los métodos como frases de acción: confirmar_pedido, cancelar.",
                "Mantén las clases pequeñas y con una sola responsabilidad.",
                "Usa to_dict para controlar la serialización y no filtrar campos.",
              ],
              exercise: [
                "Crea una clase Producto con nombre, precio y stock, y un método rebajar(precio).",
                "Agrega a Usuario un método es_activo que valide su estado.",
                "Escribe to_dict para un Cliente con el email ofuscado.",
              ],
              summary: [
                "Las clases agrupan datos y comportamiento del dominio.",
                "__init__ inicializa el estado y self enlaza la instancia.",
                "to_dict facilita la serialización hacia APIs.",
              ],
              lang: "python",
              exampleFile: "clases",
              desc: "Clases y objetos para modelar tu dominio.",
            },
          },
          {
            id: "herencia-y-polimorfismo",
            title: "Herencia y polimorfismo",
            duration: "12 min",
            level: "intermedio",
            category: "poo",
            cardText: "Reutiliza y especializa código con herencia y polimorfismo.",
            desc: "Herencia y polimorfismo en Python.",
            body: {
              intro: [
                "Cuando muchas clases comparten comportamiento, la herencia evita duplicar: define lo común en una base y especializa en las hijas.",
                "En backend aparece al modelar roles, tipos de notificación, proveedores de pago o handlers de eventos.",
              ],
              def: {
                par: "La herencia permite que una clase derive de otra reutilizando sus atributos y métodos, y el polimorfismo permite tratar objetos distintos a través de una interfaz común.",
                porQue: "Un código sin herencia duplica la lógica y cada cambio exige tocar mil sitios; heredar centraliza y el polimorfismo simplifica a quien consume.",
                cuando: "Usa herencia cuando exista una relación es un clara (Admin es un Usuario) y haya comportamiento común real que reutilizar.",
              },
              concepts: [
                "class Hija(Padre): hace que Hija herede de Padre.",
                "super().__init__(...) llama al inicializador de la clase padre.",
                "Los métodos se sobrescriben redefiniéndolos en la hija.",
                "Polimorfismo: llamar el mismo método y que cada clase haga lo suyo.",
                "Evita herencias profundas; prefiere composición cuando solo sea conveniencia.",
              ],
              basic: {
                p: "Una clase base Usuario y la subclase Admin que reutiliza el código.",
                code: [
                  "class Usuario:",
                  "    def __init__(self, nombre, email):",
                  "        self.nombre = nombre",
                  "        self.email = email",
                  "",
                  "    def permisos(self):",
                  '        return ["leer"]',
                  "",
                  "class Admin(Usuario):",
                  "    def permisos(self):",
                  '        return ["leer", "escribir", "borrar"]',
                  "",
                  'u = Usuario("Ana", "ana@mail.com")',
                  'a = Admin("Luis", "luis@mail.com")',
                  "print(u.permisos())",
                  "print(a.permisos())",
                ],
                out: "['leer']\n['leer', 'escribir', 'borrar']",
                lang: "python",
              },
              intermediate: {
                p: "Herencia con super() para no repetir la inicialización de los campos comunes.",
                caption: "Reutilizando el __init__ del padre con super()",
                code: [
                  "class Notificacion:",
                  "    def __init__(self, destino, mensaje):",
                  "        self.destino = destino",
                  "        self.mensaje = mensaje",
                  "",
                  "    def enviar(self):",
                  "        raise NotImplementedError",
                  "",
                  "class EmailNotif(Notificacion):",
                  "    def __init__(self, destino, mensaje, asunto):",
                  "        super().__init__(destino, mensaje)",
                  "        self.asunto = asunto",
                  "",
                  "    def enviar(self):",
                  '        print(f"EMAIL a {self.destino}: {self.asunto} - {self.mensaje}")',
                  "",
                  "class SMSNotif(Notificacion):",
                  "    def enviar(self):",
                  '        print(f"SMS a {self.destino}: {self.mensaje}")',
                  "",
                  'n1 = EmailNotif("ana@mail.com", "Hola", "Bienvenida")',
                  'n2 = SMSNotif("+5699999", "Codigo: 4821")',
                  "n1.enviar()",
                  "n2.enviar()",
                ],
              },
              real: {
                p: "Polimorfismo en acción: un mismo bucle despacha notificaciones de tipos distintos.",
                ul: [
                  "Todos implementan enviar() y el llamador no pregunta el tipo.",
                  "Agregar un nuevo canal (PushNotif) no obliga a tocar el bucle.",
                  "NotImplementedError avisa si olvidas implementar el contrato.",
                ],
                code: [
                  "class PushNotif(Notificacion):",
                  "    def enviar(self):",
                  '        print(f"PUSH a dispositivo {self.destino}: {self.mensaje}")',
                  "",
                  'todas = [n1, n2, PushNotif("dev-42", "Nuevo pedido")]',
                  "for notif in todas:",
                  "    notif.enviar()",
                  "",
                  'print("Todas las notificaciones enviadas con polimorfismo")',
                ],
                nota: "FastAPI y casi todos los frameworks usan polimorfismo interno: tú llamas a un endpoint y el framework ejecuta el handler correcto.",
              },
              expl: [
                "La hija hereda todo lo público del padre y puede sobrescribirlo.",
                "super() delega y evita duplicar la inicialización del padre.",
                "Polimorfismo es una llamada, muchos comportamientos según la clase real.",
                "Un método base que levanta NotImplementedError define el contrato obligatorio.",
                "Prefiere composición (tiene un) sobre herencia profunda (es un dudoso).",
              ],
              errors: [
                "TypeError por no llamar super().__init__() cuando el padre exige argumentos.",
                "Olvidar sobrescribir un método y heredar uno inapropiado.",
                "Herencia múltiple confusa con un MRO inesperado; evítala cuando sea posible.",
              ],
              tips: [
                "Pregunta siempre si es un antes de heredar.",
                "Usa super() con los argumentos correctos y solo cuando haga falta.",
                "Define el contrato con métodos que levanten NotImplementedError o con ABC.",
                "Documenta qué método sobrescribe qué.",
              ],
              exercise: [
                "Crea una clase ImpuestosBase y las subclases Iva y Retencion con calcular().",
                "Agrega PushNotif completa a la jerarquía de notificaciones.",
                "Escribe un validador polimórfico con EmailValidator y UrlValidator.",
              ],
              summary: [
                "La herencia reutiliza y especializa el comportamiento común.",
                "super() conecta la hija con el padre sin duplicar.",
                "El polimorfismo permite despachar objetos distintos con una llamada única.",
              ],
              lang: "python",
              exampleFile: "herencia",
              desc: "Herencia y polimorfismo en Python.",
            },
          },
          {
            id: "encapsulacion-y-propiedades",
            title: "Encapsulación y propiedades",
            duration: "11 min",
            level: "intermedio",
            category: "poo",
            cardText: "Protege el estado interno con encapsulación y propiedades @property.",
            desc: "Encapsulación y propiedades @property en Python.",
            body: {
              intro: [
                "Un objeto de backend no debe exponer su estado a cualquier golpe: un usuario no puede quedar sin email ni con un saldo negativo.",
                "La encapsulación oculta el estado y las propiedades (@property) lo validan al leer y escribir.",
              ],
              def: {
                par: "La encapsulación restringe el acceso directo a los atributos internos; las propiedades de Python convierten métodos en accesos estilo atributo con getters y setters.",
                porQue: "Proteger las invariantes del dominio evita estados inválidos: emails vacíos, precios negativos, tokens vencidos.",
                cuando: "Usa propiedades cuando el acceso a un atributo requiera validación, cálculo o control de escritura.",
              },
              concepts: [
                "Un guion bajo (self._precio) marca el atributo como interno por convención.",
                "@property define un getter: se lee como atributo sin paréntesis.",
                "@nombre.setter valida la escritura.",
                "@nombre.deleter controla el borrado.",
                "Los setters lanzan ValueError para rechazar estados inválidos.",
              ],
              basic: {
                p: "Una propiedad cuyo setter valida y normaliza el email.",
                code: [
                  "class Usuario:",
                  "    def __init__(self, nombre, email):",
                  "        self.nombre = nombre",
                  "        self._email = email",
                  "",
                  "    @property",
                  "    def email(self):",
                  "        return self._email",
                  "",
                  "    @email.setter",
                  "    def email(self, valor):",
                  "        valor = valor.strip().lower()",
                  '        if "@" not in valor:',
                  '            raise ValueError("Email invalido")',
                  "        self._email = valor",
                  "",
                  'ana = Usuario("Ana", "ANA@MAIL.COM")',
                  'ana.email = "  otro@mail.com "',
                  "print(ana.email)",
                ],
                out: "otro@mail.com",
                lang: "python",
              },
              intermediate: {
                p: "Validar rangos numéricos con un setter en un modelo de producto.",
                caption: "Setter que mantiene invariantes de negocio",
                code: [
                  "class Producto:",
                  "    def __init__(self, nombre, precio):",
                  "        self.nombre = nombre",
                  "        self._precio = 0",
                  "        self.precio = precio",
                  "",
                  "    @property",
                  "    def precio(self):",
                  "        return self._precio",
                  "",
                  "    @precio.setter",
                  "    def precio(self, valor):",
                  '        if valor < 0:',
                  '            raise ValueError("El precio no puede ser negativo")',
                  "        self._precio = round(float(valor), 2)",
                  "",
                  'p = Producto("Teclado", 49.5)',
                  "try:",
                  "    p.precio = -5",
                  "except ValueError as e:",
                  "    print(e)",
                  "print(p.precio)",
                ],
              },
              real: {
                p: "Propiedad calculada: la duración de una sesión a partir de timestamps internos.",
                ul: [
                  "Un getter calculado no guarda estado duplicado.",
                  "Sin setter, la propiedad es de solo lectura.",
                  "El dominio queda a salvo de manipulaciones externas.",
                ],
                code: [
                  "from datetime import datetime",
                  "",
                  "class Sesion:",
                  "    def __init__(self, inicio, fin=None):",
                  "        self.inicio = inicio",
                  "        self.fin = fin or datetime.utcnow()",
                  "",
                  "    @property",
                  "    def duracion(self):",
                  "        delta = (self.fin - self.inicio).total_seconds()",
                  '        return format(delta / 60, ".1f")',
                  "",
                  's = Sesion(datetime(2026, 9, 23, 10, 0))',
                  'print(f"La sesion duro {s.duracion} minutos")',
                ],
                nota: "Las propiedades calculadas mantienen una sola fuente de verdad y nunca se desincronizan.",
              },
              expl: [
                "El guion bajo es convención, no seguridad: documenta la intención.",
                "Las propiedades permiten cambiar la implementación interna sin romper a los consumidores.",
                "Un getter puede calcular valores sobre la marcha sin atributo extra.",
                "Solo lectura equivale a una property sin setter.",
                "Validar en el setter concentra las reglas del negocio en un solo punto.",
              ],
              errors: [
                "RecursionError si el getter y el setter usan self.precio en vez de self._precio.",
                "AttributeError: can't set attribute al escribir una property sin setter.",
                "Validar con tipos incorrectos y fallar en runtime, no al asignar.",
              ],
              tips: [
                "Nunca hagas self.precio dentro del setter de precio: usa self._precio.",
                "Normaliza antes de validar (strip, lower, tipos).",
                "Prefiere propiedades a los métodos get_ y set_ al estilo Java.",
                "Usa __slots__ solo si necesitas restringir atributos dinámicos.",
              ],
              exercise: [
                "Agrega una propiedad edad a Usuario calculada desde su anio_nacimiento.",
                "Valida en un setter que el stock no baje de cero.",
                "Crea una propiedad de solo lectura full_nombre que concatene nombre y apellido.",
              ],
              summary: [
                "El guion bajo marca atributos internos y @property controla el acceso.",
                "Los setters validan y mantienen las invariantes de negocio.",
                "Las propiedades calculadas evitan estado duplicado y desincronizado.",
              ],
              lang: "python",
              exampleFile: "encapsulacion",
              desc: "Encapsulación y propiedades @property.",
            },
          },
          {
            id: "metodos-especiales-dunder",
            title: "Métodos especiales: __dunder__",
            duration: "10 min",
            level: "intermedio",
            category: "poo",
            cardText: "Da superpoderes a tus clases con __str__, __repr__, __eq__ y __enter__.",
            desc: "Métodos especiales dunder para integrar tus clases con Python.",
            body: {
              intro: [
                "Los métodos dunder (doble guion bajo) permiten que tus objetos usen la sintaxis nativa de Python: para print, len(), in, with, sumas y más.",
                "Son lo que hace que una clase se integre con el lenguaje en vez de sentirse una jaula aparte.",
              ],
              def: {
                par: "Los métodos dunder son métodos predefinidos que Python invoca automáticamente ante operadores y funciones: str(obj), len(obj), obj == otro, with objeto, etc.",
                porQue: "Implementarlos hace tus clases naturales de usar y depurar, y el intérprete y las librerías las tratan como ciudadanas de primera clase.",
                cuando: "Define dunder cuando quieras que tus objetos se impriman bien, se comparen, se midan o se usen como context managers.",
              },
              concepts: [
                "__str__ es lo que ve el usuario: str() y print().",
                "__repr__ es la representación para depuración; ideal que reproduzca la creación.",
                "__eq__ define la igualdad entre objetos.",
                "__len__ habilita len(obj) y el test de vacío.",
                "__enter__ y __exit__ hacen usable la clase con with.",
              ],
              basic: {
                p: "Imprentas legibles de un usuario con __str__ y __repr__.",
                code: [
                  "class Usuario:",
                  "    def __init__(self, nombre, email):",
                  "        self.nombre = nombre",
                  "        self.email = email",
                  "",
                  "    def __str__(self):",
                  '        return f"Usuario {self.nombre} ({self.email})"',
                  "",
                  "    def __repr__(self):",
                  '        return f"Usuario({self.nombre!r}, {self.email!r})"',
                  "",
                  'ana = Usuario("Ana", "ana@mail.com")',
                  "print(ana)",
                  "print(repr(ana))",
                ],
                out: "Usuario Ana (ana@mail.com)\nUsuario('Ana', 'ana@mail.com')",
                lang: "python",
              },
              intermediate: {
                p: "Comparar pedidos por su id con __eq__ y medirlos con __len__.",
                caption: "Igualdad y longitud personalizadas",
                code: [
                  "class Pedido:",
                  "    def __init__(self, pedido_id, items):",
                  "        self.id = pedido_id",
                  "        self.items = items",
                  "",
                  "    def __eq__(self, otro):",
                  "        return isinstance(otro, Pedido) and self.id == otro.id",
                  "",
                  "    def __len__(self):",
                  "        return len(self.items)",
                  "",
                  'a = Pedido(1, ["teclado", "raton"])',
                  'b = Pedido(1, ["monitor"])',
                  "print(a == b)",
                  "print(len(b))",
                ],
              },
              real: {
                p: "Un context manager (with) para una conexión simulada que siempre se cierra.",
                ul: [
                  "with garantiza el cierre aunque haya una excepción.",
                  "Es el patrón estándar en bases de datos, sockets y archivos.",
                  "Libera al código de negocio de la limpieza manual.",
                ],
                code: [
                  "class Conexion:",
                  "    def __enter__(self):",
                  '        print("Abriendo conexion...")',
                  "        return self",
                  "",
                  "    def consultar(self, sql):",
                  '        print(f"Ejecutando: {sql}")',
                  "",
                  "    def __exit__(self, exc_type, exc_val, exc_tb):",
                  '        print("Cerrando conexion...")',
                  "",
                  "with Conexion() as db:",
                  '    db.consultar("SELECT * FROM usuarios LIMIT 1")',
                ],
                nota: "FastAPI usa un estilo parecido con dependency injection para abrir y cerrar sesiones de base de datos.",
              },
              expl: [
                "__str__ y __repr__ conviven: uno para presentar, otro para depurar.",
                "__eq__ sin __hash__ rompe la usabilidad en dicts y sets; suelen ir juntos.",
                "Un __len__ correcto hace que bool(obj) y los condicionales tengan sentido.",
                "__enter__ y __exit__ materializan el patrón with de recursos.",
                "Sin __eq__ Python compara por identidad, no por valor.",
              ],
              errors: [
                "Implementar __eq__ sin __hash__ inhabilita el objeto como clave de dict.",
                "Un __len__ que devuelve algo que no es entero lanza TypeError.",
                "Olvidar __contains__ y usar in con escaneo completo lento.",
              ],
              tips: [
                "Escribe un __repr__ que, evaluado, reconstruya el objeto.",
                "Siempre define __str__ para logs y texto dirigido al usuario.",
                "Mantén __eq__ consistente con __hash__.",
                "Usa @dataclass para ahorrarte dunders repetitivos en el 80% de los casos.",
              ],
              exercise: [
                "Añade __str__ y __repr__ a una clase Producto.",
                "Implementa __lt__ en un Pedido para poder ordenarlo por total.",
                "Convierte la clase Sesion en un context manager usable con with.",
              ],
              summary: [
                "Los dunder integran tus objetos con la sintaxis nativa de Python.",
                "__str__ y __repr__ hacen el código depurable; __eq__ define la igualdad.",
                "__enter__ y __exit__ habilitan la limpieza automática con with.",
              ],
              lang: "python",
              exampleFile: "dunder",
              desc: "Métodos especiales dunder.",
            },
          },
        ],
      },
      {
        id: "modulo-05",
        n: 5,
        t: "Módulos, archivos y excepciones",
        level: "intermedio",
        caption: "Organiza el código en piezas reutilizables y maneja errores.",
        desc: "Divide tu proyecto en módulos y paquetes, lee y escribe archivos y JSON, y controla errores con excepciones.",
        subtitle: "Estructura de proyecto, archivos, JSON y control de errores.",
        objectives: [
          "Reutilizar código con módulos y paquetes",
          "Leer y escribir archivos de forma segura",
          "Serializar datos con JSON",
          "Manejar errores con try/except y excepciones propias",
        ],
        lessons: [
          {
            id: "modulos-y-paquetes",
            title: "Módulos y paquetes",
            duration: "9 min",
            level: "intermedio",
            category: "modulos",
            cardText: "Divide tu proyecto en módulos y paquetes reutilizables.",
            desc: "Divide tu proyecto en módulos y paquetes.",
            body: {
              intro: [
                "Un backend de verdad se organiza en muchos archivos: modelos, utilidades, configuración, rutas. Los módulos y paquetes hacen que el código viaje de archivo en archivo.",
                "En Python, un módulo es cualquier archivo .py y un paquete es una carpeta con __init__.py.",
              ],
              def: {
                par: "Un módulo agrupa funciones, clases y constantes reutilizables; un paquete es una carpeta con __init__.py que agrupa módulos relacionados bajo un espacio de nombres.",
                porQue: "Dividir el código en módulos permite testear de forma aislada, reutilizar y mantener proyectos de cientos de archivos ordenados.",
                cuando: "Usa módulos desde el primer helper; arma paquetes cuando el proyecto crece: utils, config, models.",
              },
              concepts: [
                "import modulo o from modulo import funcion.",
                "El bloque if __name__ == \"__main__\": evita ejecutar al importar.",
                "Alias: import modulo as m.",
                "Una carpeta con __init__.py es un paquete importable.",
                "Los imports relativos (from .modulo import x) funcionan dentro del paquete.",
              ],
              basic: {
                p: "Un utilitario reutilizable en su propio módulo y su uso desde otro archivo.",
                code: [
                  "# archivo: utilidades.py",
                  "def normalizar_email(email):",
                  "    return email.strip().lower()",
                  "",
                  "def contar_elementos(items):",
                  "    return len(items)",
                  "",
                  'if __name__ == "__main__":',
                  '    print("Probando utilidades...")',
                  "",
                  "# archivo: app.py (misma carpeta)",
                  "import utilidades",
                  'email = utilidades.normalizar_email("  ANA@MAIL.COM  ")',
                  "print(email)",
                  "print(utilidades.contar_elementos([1, 2, 3]))",
                ],
                out: "ana@mail.com\n3",
                lang: "python",
              },
              intermediate: {
                p: "Organizar el proyecto con un paquete y controlar la ejecución directa.",
                caption: "Paquete con __init__.py e import con alias",
                code: [
                  "# estructura:",
                  "# mi_proyecto/",
                  "#   __init__.py",
                  "#   db.py",
                  "#   utils.py",
                  "",
                  "# __init__.py",
                  "from . import db",
                  "from . import utils",
                  "",
                  "# db.py",
                  "def conectar(host):",
                  '    return f"Conectado a {host}"',
                  "",
                  "# utils.py",
                  "def limpiar_texto(texto):",
                  "    return texto.strip()",
                  "",
                  "# en otro archivo",
                  "import mi_proyecto as proy",
                  'print(proy.db.conectar("db-01"))',
                  'print(proy.utils.limpiar_texto("  ok  "))',
                ],
              },
              real: {
                p: "Estructura típica de un pequeño backend por capas, con config y versionado.",
                ul: [
                  "Separa responsabilidades en archivos: config, utils, modelos, rutas.",
                  "Los imports explícitos revelan las dependencias.",
                  "El guard __main__ permite probar cada archivo por separado.",
                ],
                code: [
                  "# config.py",
                  "DEBUG = True",
                  "PUERTO = 8000",
                  "",
                  "# versions.py",
                  'VERSION_API = "v1"',
                  "",
                  "# main.py",
                  "import config",
                  "import versions",
                  "",
                  "def arrancar():",
                  "    print(f\"Arrancando API {versions.VERSION_API} en el puerto {config.PUERTO}\")",
                  "",
                  'if __name__ == "__main__":',
                  "    arrancar()",
                ],
                nota: "Los frameworks reales (FastAPI) usan paquetes de carpetas: routers, schemas, models. La misma mecánica que acabas de ver.",
              },
              expl: [
                "Importar ejecuta el módulo una sola vez y lo guarda en sys.modules.",
                "El guard __name__ evita efectos colaterales al importar.",
                "Los paquetes sin __init__.py (namespace) existen, pero añadirlo es explícito.",
                "Los imports relativos de un paquete evitan rutas frágiles.",
              ],
              errors: [
                "ModuleNotFoundError cuando la ruta o el nombre no coinciden.",
                "Import circular (a importa b y b importa a) deja funciones sin definir.",
                "Ejecutar código de arranque al importar por olvidar el guard __main__.",
              ],
              tips: [
                "Prefiere imports explícitos: from utilidades import normalizar_email.",
                "Evita los imports circulares moviendo el punto de cruce a un tercer módulo.",
                "Nombra módulos y paquetes en minúsculas (PEP 8).",
                "Guarda secretos en variables de entorno, no en módulos de código.",
              ],
              exercise: [
                "Divide tu ejercicio de funciones en utilidades.py y app.py.",
                "Crea un paquete con __init__.py y dos módulos, luego impórtalos.",
                "Ejecuta un módulo directamente y también vía import, observando __name__.",
              ],
              summary: [
                "Un módulo es un archivo .py; un paquete una carpeta con __init__.py.",
                "El guard __name__ == \"__main__\" separa import de ejecución.",
                "Un backend por capas se apoya en paquetes organizados.",
              ],
              lang: "python",
              exampleFile: "modulos",
              desc: "Módulos y paquetes en Python.",
            },
          },
          {
            id: "lectura-y-escritura-de-archivos",
            title: "Lectura y escritura de archivos",
            duration: "10 min",
            level: "intermedio",
            category: "archivos",
            cardText: "Lee y escribe archivos con with, modos y lectura línea a línea.",
            desc: "Lectura y escritura de archivos en Python.",
            body: {
              intro: [
                "Tu backend procesará archivos a diario: logs, configs, plantillas, reportes CSV o subidas de usuarios.",
                "Python tiene la API de archivos más cómoda que existe: con with open(...) as f controlas la apertura, la lectura y el cierre automático.",
              ],
              def: {
                par: "open() devuelve un objeto de archivo; los modos r, w y a controlan lectura, escritura y apéndice; with garantiza el cierre aunque ocurra un error.",
                porQue: "Toda operación de archivo puede fallar a media ejecución; con with evitas archivos corruptos o fugas de manejadores.",
                cuando: "Usa este patrón para leer configs, escribir logs, exportar CSV y procesar ficheros grandes línea a línea.",
              },
              concepts: [
                "with open(ruta, \"r\") as f: abre y cierra automáticamente.",
                "f.read() lee todo; f.readline() lee una línea; iterar f recorre líneas.",
                "Los modos: r, w (sobrescribe), a (añade) y b para binario.",
                "encoding utf-8 evita errores de acentos.",
                "f.write() escribe texto en el archivo abierto.",
              ],
              basic: {
                p: "Registrar un log de eventos en un archivo y leerlo después.",
                code: [
                  'with open("eventos.log", "w", encoding="utf-8") as f:',
                  '    f.write("2026-09-23 10:00 inicio de servicio\\n")',
                  '    f.write("2026-09-23 10:01 peticion recibida\\n")',
                  "",
                  'with open("eventos.log", "r", encoding="utf-8") as f:',
                  "    for linea in f:",
                  "        print(linea.strip())",
                ],
                out: "2026-09-23 10:00 inicio de servicio\n2026-09-23 10:01 peticion recibida",
                lang: "python",
              },
              intermediate: {
                p: "Añadir registros al archivo sin borrar lo anterior y escribir varias líneas de golpe.",
                caption: "Modo append (a) y escritura por lotes con writelines",
                code: [
                  "nuevas = [",
                  '    "2026-09-23 10:02 consulta a la base\\n",',
                  '    "2026-09-23 10:03 respuesta 200\\n",',
                  "]",
                  'with open("eventos.log", "a", encoding="utf-8") as f:',
                  "    f.writelines(nuevas)",
                  "",
                  'print("Terminado. Contenido final:")',
                  'with open("eventos.log", "r", encoding="utf-8") as f:',
                  "    print(f.read())",
                ],
              },
              real: {
                p: "Procesar un archivo CSV de pedidos y extraer un resumen por cliente.",
                ul: [
                  "Iterar el archivo línea a línea economiza memoria.",
                  "Pasar por alto los registros malos con comprobaciones y try/except.",
                  "Escribir la salida en un nuevo archivo derivado.",
                ],
                code: [
                  "resumen = []",
                  'with open("pedidos.csv", "r", encoding="utf-8") as f:',
                  "    for num, linea in enumerate(f):",
                  "        if num == 0:",
                  "            continue  # cabecera",
                  '        campos = linea.strip().split(",")',
                  "        if len(campos) < 3:",
                  "            continue",
                  "        cliente, total, estado = campos",
                  '        if estado == "entregado":',
                  '            resumen.append(f"{cliente};{total}")',
                  "",
                  'with open("resumen.csv", "w", encoding="utf-8") as f:',
                  '    f.write("cliente;total\\n")',
                  '    f.writelines(linea + "\\n" for linea in resumen)',
                  'print("Resumen generado con", len(resumen), "pedidos")',
                ],
                nota: "Para CSV reales con comillas usa el módulo csv, pero esta iteración manual asienta el concepto base.",
              },
              expl: [
                "with cierra el archivo automáticamente incluso con excepciones.",
                "El modo w trunca el contenido previo; usa a para conservarlo.",
                "Recorrer f línea a línea no carga el archivo entero en memoria.",
                "encoding utf-8 previene UnicodeDecodeError con acentos.",
              ],
              errors: [
                "FileNotFoundError al leer una ruta inexistente sin try/except.",
                "UnicodeDecodeError al abrir sin encoding utf-8 un archivo con acentos.",
                "PermissionError si otro proceso retiene el archivo en Windows.",
              ],
              tips: [
                "Siempre con with open, nunca open a secas.",
                "Fija encoding utf-8 de forma explícita.",
                "Procesa archivos grandes por iteración, no con read().",
                "Usa pathlib (Path.read_text y write_text) si prefieres la API moderna.",
              ],
              exercise: [
                "Escribe un archivo con 3 líneas y luego imprímelas leyéndolo.",
                "Añade más líneas con modo append y verifica el resultado.",
                "Procesa pedidos.csv y escribe un resumen filtrando estados.",
              ],
              summary: [
                "with open gestiona el ciclo de vida del archivo.",
                "Los modos r, w y a controlan lectura, sobrescritura y apéndice.",
                "Iterar por líneas permite procesar archivos grandes eficientemente.",
              ],
              lang: "python",
              exampleFile: "archivos",
              desc: "Lectura y escritura de archivos.",
            },
          },
          {
            id: "json-y-serializacion",
            title: "Trabajar con JSON y serialización",
            duration: "10 min",
            level: "intermedio",
            category: "json",
            cardText: "Serializa datos con JSON para comunicarte con APIs y archivos.",
            desc: "Serialización de datos con el módulo json.",
            body: {
              intro: [
                "El JSON es el formato de intercambio estándar de las APIs: tu backend lo recibe en las peticiones y lo devuelve en las respuestas.",
                "El módulo json convierte tus estructuras Python (dicts, listas) en texto JSON y viceversa con dos líneas.",
              ],
              def: {
                par: "Serializar es convertir una estructura Python en una cadena JSON (json.dumps); deserializar es lo opuesto (json.loads). La salida se guarda en archivos con dump y load.",
                porQue: "Sin serialización no hay intercambio de datos entre servicios: tu API solo entiende texto (JSON) y tu código piensa en dicts y clases.",
                cuando: "Usa json para responder peticiones, leer archivos de config, guardar cachés y comunicarte con otras APIs.",
              },
              concepts: [
                "json.dumps(python) convierte a cadena JSON.",
                "json.loads(cadena) convierte a estructura Python.",
                "json.dump y json.load escriben y leen directamente archivos.",
                "ensure_ascii=False conserva los acentos en la salida.",
                "indent=2 formatea el JSON para humanos.",
              ],
              basic: {
                p: "Convertir un dict de usuario a JSON y volver a Python.",
                code: [
                  "import json",
                  "",
                  'datos = {"nombre": "Ana", "roles": ["admin"], "activo": True}',
                  "cadena = json.dumps(datos, ensure_ascii=False)",
                  "print(cadena)",
                  "print(type(cadena))",
                  "",
                  "de_vuelta = json.loads(cadena)",
                  'print(de_vuelta["roles"])',
                  "print(type(de_vuelta))",
                ],
                out: '{"nombre": "Ana", "roles": ["admin"], "activo": true}\n<class \'str\'>\n[\'admin\']\n<class \'dict\'>',
                lang: "python",
              },
              intermediate: {
                p: "Guardar la configuración de la aplicación en un archivo JSON y cargarla al arrancar.",
                caption: "Persistencia de config en JSON con dump y load",
                code: [
                  "import json",
                  "",
                  "config = {",
                  '    "host": "localhost",',
                  "    \"puerto\": 8000,",
                  '    "debug": False,',
                  "}",
                  "",
                  'with open("config.json", "w", encoding="utf-8") as f:',
                  "    json.dump(config, f, indent=2, ensure_ascii=False)",
                  "",
                  'with open("config.json", "r", encoding="utf-8") as f:',
                  "    cargada = json.load(f)",
                  "",
                  "print(cargada)",
                  'print(cargada["puerto"] + 1)',
                ],
              },
              real: {
                p: "Construir la respuesta de una API: dict a JSON, y recibir JSON a dict en la petición.",
                ul: [
                  "Envías los datos de la respuesta serializados con dumps.",
                  "Recibes el body de la petición con loads.",
                  "Maneja JSON inválido con try/except para responder 400.",
                ],
                code: [
                  "import json",
                  "",
                  'pedido = {"id": 7, "cliente": "Luis", "total": 120.50}',
                  "respuesta = json.dumps(pedido, ensure_ascii=False)",
                  'print("HTTP 200", respuesta)',
                  "",
                  'body = \'{"id": 7, "cliente": "Luis"}\'',
                  "try:",
                  "    datos = json.loads(body)",
                  "except json.JSONDecodeError:",
                  "    datos = None",
                  '    print("HTTP 400: JSON invalido")',
                  'print("Procesando cliente:", datos and datos.get("cliente"))',
                ],
                nota: "En FastAPI no llamas a json a mano: Pydantic valida y serializa por ti. Dominar json aquí te da la intuición de lo que ocurre debajo.",
              },
              expl: [
                "dumps vs dump: uno a cadena, otro a archivo; loads vs load igual.",
                "Los booleanos True y False se convierten a true y false; None a null.",
                "Los dicts se serializan respetando su orden de inserción.",
                "Objetos no serializables (datetime, clases) lanzan TypeError sin hook.",
                "ensure_ascii=False emite acentos reales en lugar de secuencias \\uXXXX.",
              ],
              errors: [
                "json.JSONDecodeError cuando el texto recibido no es JSON válido.",
                "TypeError al serializar un objeto arbitrario sin default.",
                "Cargar un JSON esperando un dict y recibir una lista.",
              ],
              tips: [
                "Envuelve loads en try/except siempre que el dato venga de fuera.",
                "Usa indent=2 solo en desarrollo; en producción mantén la salida compacta.",
                "Fija ensure_ascii=False para texto legible.",
                "Para tipos complejos define default=str o usa modelos (Pydantic).",
              ],
              exercise: [
                "Serializa una lista de 3 pedidos a JSON con indent=2.",
                "Carga un archivo config.json y corrige un dato sin romper el resto.",
                "Crea un helper leer_json(ruta) que devuelva None si el archivo está corrupto.",
              ],
              summary: [
                "json.dumps y loads convierten entre Python y texto JSON.",
                "dump y load trabajan directo con archivos.",
                "Valida y maneja JSONDecodeError cuando el dato viene de fuera.",
              ],
              lang: "python",
              exampleFile: "json",
              desc: "Trabajar con JSON y serialización.",
            },
          },
          {
            id: "excepciones-y-try-except",
            title: "Excepciones y try/except",
            duration: "10 min",
            level: "intermedio",
            category: "excepciones",
            cardText: "Controla errores con try/except y levanta excepciones claras.",
            desc: "Manejo de errores con try/except en Python.",
            body: {
              intro: [
                "El backend vive rodeado de fallos: una base de datos caída, un parseo roto, un timeout, una entrada inesperada. Las excepciones son el mecanismo de Python para manejarlos.",
                "Con try/except decides qué hacer ante cada error, en lugar de estrellarte.",
              ],
              def: {
                par: "Una excepción interrumpe el flujo normal; try/except la captura para manejarla, finally limpia siempre y raise la dispara manualmente.",
                porQue: "Un error no gestionado tumba el servicio o responde 500; manejado, tu API responde 400 o reintenta con criterio.",
                cuando: "Usa try/except en puntos de entrada (parsing, I/O, llamadas HTTP) y raise para validar reglas de negocio.",
              },
              concepts: [
                "try/except captura errores concretos: except ValueError, except KeyError.",
                "else se ejecuta solo si no hubo excepción.",
                "finally se ejecuta siempre (cerrar conexiones).",
                "raise lanza una excepción propia o re-lanza la capturada.",
                "La jerarquía: BaseException, luego Exception, luego los errores concretos.",
              ],
              basic: {
                p: "Proteger una división y accesos a claves que pueden faltar.",
                code: [
                  "def dividir_seguro(a, b):",
                  "    try:",
                  "        return a / b",
                  "    except ZeroDivisionError:",
                  "        return None",
                  "",
                  "print(dividir_seguro(10, 2))",
                  "print(dividir_seguro(10, 0))",
                ],
                out: "5.0\nNone",
                lang: "python",
              },
              intermediate: {
                p: "Capturar varios tipos, usar else y finally al procesar un archivo.",
                caption: "Múltiples except, else y finally",
                code: [
                  "def procesar(numero, ruta):",
                  "    try:",
                  "        valor = int(numero)",
                  '        with open(ruta, "r", encoding="utf-8") as f:',
                  "            contenido = f.read()",
                  "    except (ValueError, TypeError):",
                  '        print("Entrada invalida")',
                  "    except FileNotFoundError:",
                  '        print("Archivo inexistente")',
                  "    else:",
                  '        print(f"Valor valido: {valor}")',
                  "    finally:",
                  '        print("Limpieza lista")',
                  "",
                  'procesar("42", "data.txt")',
                  'procesar("abc", "data.txt")',
                ],
              },
              real: {
                p: "Validar la entrada de una petición y responder un error controlado en vez de un crash.",
                ul: [
                  "Las excepciones llevan mensajes claros al cliente.",
                  "raise en validaciones del dominio mantiene el código legible.",
                  "Registra el error antes de devolver 500 en servicios de producción.",
                ],
                code: [
                  "def crear_usuario(email, edad):",
                  "    if not isinstance(edad, int) or edad < 18:",
                  '        raise ValueError("Edad minima requerida: 18")',
                  '    if "@" not in email:',
                  '        raise ValueError("Email invalido")',
                  '    return {"email": email, "edad": edad}',
                  "",
                  "try:",
                  '    usuario = crear_usuario("ana@mail.com", 15)',
                  "except ValueError as error:",
                  '    print("HTTP 400:", error)',
                  "else:",
                  '    print("HTTP 201:", usuario)',
                ],
                nota: "FastAPI valida este tipo de invariantes con Pydantic; pero el patrón try/except con raise te lo da escrito en arquitecturas custom.",
              },
              expl: [
                "Captura las excepciones específicas que sabes manejar; no solo except Exception.",
                "else no corre si entró en except, lo que hace el flujo explícito.",
                "finally corre SIEMPRE, perfecto para cerrar recursos o limpiar.",
                "raise sin argumentos dentro de except re-lanza la excepción original.",
                "Una jerarquía ordenada evita que except demasiado generales atrapen todo.",
              ],
              errors: [
                "Capturar BaseException (incluye KeyboardInterrupt) y tragar señales.",
                "Un except silencioso (except: pass) que oculta bugs críticos.",
                "Levantar excepciones genéricas Exception sin contexto.",
              ],
              tips: [
                "Sé específico con el tipo de excepción que capturas.",
                "Registra el error (logging) antes de mostrarlo al cliente.",
                "Nunca devuelvas stack traces; limita el mensaje.",
                "Define excepciones propias heredando de Exception para tu dominio.",
              ],
              exercise: [
                "Protege procesar_datos() ante TypeError y KeyError con mensajes claros.",
                "Crea una excepción personalizada PedidoInvalido y lánzala en una validación.",
                "Escribe un bloque con else y finally que cuente los procesos exitosos.",
              ],
              summary: [
                "try/except maneja fallos puntuales sin tumbar el servicio.",
                "else y finally afinan la ejecución y la limpieza.",
                "raise convierte validaciones en excepciones comunicables.",
              ],
              lang: "python",
              exampleFile: "excepciones",
              desc: "Manejo de errores con excepciones.",
            },
          },
        ],
      },
      {
        id: "modulo-06",
        n: 6,
        t: "Web con Flask",
        level: "avanzado",
        caption: "Tu primer backend en Python con un framework web.",
        desc: "Flask te permite exponer lógica Python a Internet con rutas, peticiones HTTP, formularios y sesiones en minutos.",
        subtitle: "Construye tu primer servidor web con Flask.",
        objectives: [
          "Levantar un servidor Flask y servir las primeras rutas",
          "Gestionar rutas, métodos HTTP y parámetros",
          "Recibir datos con formularios y mantener estado con sesiones",
        ],
        lessons: [
          {
            id: "introduccion-a-flask",
            title: "Introducción a Flask",
            duration: "12 min",
            level: "avanzado",
            category: "flask",
            cardText: "Levanta tu primer servidor web con Flask: instalación, rutas y ejecución.",
            desc: "Introducción a Flask para backend web.",
            body: {
              intro: [
                "Flask es un micro framework que en pocas líneas convierte tu lógica Python en un servidor web capaz de responder peticiones.",
                "Es ideal para aprender web en Python: sin magia, rutas explícitas y control total.",
              ],
              def: {
                par: "Flask es un framework web mínimo: la app se crea con Flask(__name__), @app.route asocia URLs a funciones y app.run() levanta el servidor.",
                porQue: "Con un servidor web tus funciones dejan de ser scripts: clientes, navegadores y otras APIs pueden consumirlas.",
                cuando: "Usa Flask para prototipos, microservicios pequeños, dashboards internos o como puente a APIs externas.",
              },
              concepts: [
                "pip install flask instala la librería.",
                "Flask(__name__) crea la aplicación.",
                "@app.route(\"/\") asocia la URL raíz a una vista.",
                "app.run(debug=True) arranca con recarga automática.",
                "Las vistas devuelven texto, HTML o JSON con jsonify.",
              ],
              basic: {
                p: "Instala Flask en tu entorno virtual (recomendado) desde la terminal.",
                code: [
                  "python -m venv venv",
                  "venv\\Scripts\\activate  # Windows",
                  "source venv/bin/activate  # Linux/macOS",
                  "pip install flask",
                  "python -c \"import flask; print(flask.__version__)\"",
                ],
                out: "3.1.x",
                lang: "bash",
              },
              intermediate: {
                p: "El servidor mínimo: la app, la ruta raíz y el arranque.",
                caption: "Hola mundo con ruta decorada",
                code: [
                  "from flask import Flask",
                  "",
                  "app = Flask(__name__)",
                  "",
                  "@app.route(\"/\")",
                  "def inicio():",
                  '    return "Hola desde mi backend Flask"',
                  "",
                  "@app.route(\"/estado\")",
                  "def estado():",
                  '    return {"servicio": "ok", "version": "1.0"}',
                  "",
                  'if __name__ == "__main__":',
                  '    app.run(debug=True, host="127.0.0.1", port=5000)',
                ],
              },
              real: {
                p: "Consumir tu API Flask con curl y con requests desde otro script: el flujo cliente-servidor.",
                ul: [
                  "El navegador y curl hacen GET en la URL raíz.",
                  "La segunda vista devuelve JSON nativo que Flask serializa.",
                  "debug=True recarga y muestra errores útiles en desarrollo.",
                ],
                code: [
                  "# terminal 1: iniciar server",
                  "python app.py",
                  "",
                  "# terminal 2: pedir la API",
                  "curl http://127.0.0.1:5000/estado",
                  "",
                  "# o desde un script Python",
                  "import requests",
                  'resp = requests.get("http://127.0.0.1:5000/estado")',
                  "print(resp.status_code, resp.json())",
                ],
                nota: "Para desarrollo usa app.run; en producción el servidor WSGI (gunicorn) ejecutará tu app. Mismo código, distinto ejecutor.",
              },
              expl: [
                "@app.route registra una URL; varias rutas pueden compartir una vista.",
                "Devolver un dict en Flask produce automáticamente un JSON con 200.",
                "debug=True incluye recarga y tracebacks en el navegador.",
                "Flask(__name__) usa el nombre del módulo para localizar templates y estáticos.",
                "app.run solo debe ejecutarse desde el guard __main__.",
              ],
              errors: [
                "Address already in use si el puerto 5000 está ocupado; cambia el port.",
                "RuntimeError working outside of request context al usar request fuera de una vista.",
                "ModuleNotFoundError: No module named 'flask' por instalar fuera del venv correcto.",
              ],
              tips: [
                "Trabaja siempre con un venv para no ensuciar tu Python global.",
                "Activa debug solo en desarrollo; desactívala en producción.",
                "Devuelve dicts para JSON; no construyas la serialización manualmente.",
                "Usa puertos no convencionales para evitar conflictos del sistema.",
              ],
              exercise: [
                "Crea una ruta /saludo que responda Hola Mundo.",
                "Agrega una ruta /version que devuelva un JSON con version y fecha.",
                "Consume ambas con curl y con requests desde un script.",
              ],
              summary: [
                "Flask crea un servidor con @app.route y app.run().",
                "Devolver dicts genera JSON automáticamente.",
                "curl y requests permiten probar tu backend sin navegador.",
              ],
              lang: "python",
              exampleFile: "flask",
              desc: "Introducción a Flask.",
            },
          },
          {
            id: "rutas-y-vistas",
            title: "Rutas, vistas y peticiones HTTP",
            duration: "11 min",
            level: "avanzado",
            category: "flask",
            cardText: "Maneja rutas dinámicas, métodos HTTP y peticiones con request.",
            desc: "Rutas, vistas y peticiones HTTP en Flask.",
            body: {
              intro: [
                "Una API no son solo URLs fijas: recibe parámetros, distingue GET de POST y lee el cuerpo de la petición.",
                "En Flask, request te da todo lo que el cliente te manda.",
              ],
              def: {
                par: "request es el objeto que envuelve la petición actual: métodos (GET, POST...), query params, JSON del body y cabeceras. Las rutas dinámicas se escriben con <tipo:nombre>.",
                porQue: "El backend vive de las peticiones que recibe; sin request no hay entrada de datos, ni parámetros, ni bodies que procesar.",
                cuando: "Usa rutas dinámicas para recursos con id (/usuarios/<id>) y request para leer JSON y query params de cada petición.",
              },
              concepts: [
                "@app.route(\"/usuarios/<int:id>\") captura un entero de la URL.",
                "methods=[\"GET\", \"POST\"] limita los verbos aceptados en la ruta.",
                "request.get_json() lee el cuerpo JSON de la petición.",
                "request.args guarda los parámetros de consulta (?pagina=1).",
                "request.method dice con qué verbo llegó la petición.",
              ],
              basic: {
                p: "Una ruta dinámica que responde según el identificador recibido.",
                code: [
                  "from flask import Flask",
                  "app = Flask(__name__)",
                  "",
                  "@app.route(\"/usuarios/<int:usuario_id>\")",
                  "def ver_usuario(usuario_id):",
                  '    return {"id": usuario_id, "nombre": f"Usuario {usuario_id}"}',
                  "",
                  'if __name__ == "__main__":',
                  "    app.run(debug=True)",
                ],
                lang: "python",
              },
              intermediate: {
                p: "Aceptar GET y POST en la misma ruta para listar y crear usuarios.",
                caption: "Métodos múltiples y lectura del body JSON",
                code: [
                  "from flask import Flask, request, jsonify",
                  "",
                  "app = Flask(__name__)",
                  "usuarios = []",
                  "",
                  '@app.route("/usuarios", methods=["GET", "POST"])',
                  "def gestionar_usuarios():",
                  '    if request.method == "POST":',
                  "        datos = request.get_json() or {}",
                  '        usuarios.append({"id": len(usuarios) + 1, **datos})',
                  "        return jsonify(usuarios[-1]), 201",
                  "    return jsonify(usuarios)",
                  "",
                  'if __name__ == "__main__":',
                  "    app.run(debug=True)",
                ],
              },
              real: {
                p: "Probar las rutas con curl: listar y crear, incluyendo un query param de paginación.",
                ul: [
                  "curl -X POST envía el verbo correcto.",
                  "-H establece el tipo de contenido esperado.",
                  "-d manda el JSON del cuerpo.",
                ],
                code: [
                  "curl http://127.0.0.1:5000/usuarios",
                  "",
                  "curl -X POST http://127.0.0.1:5000/usuarios \\",
                  '  -H "Content-Type: application/json" \\',
                  '  -d "{\\"nombre\\": \\"Ana\\", \\"email\\": \\"ana@mail.com\\"}"',
                  "",
                  'curl "http://127.0.0.1:5000/usuarios?pagina=1"',
                ],
                nota: "request.get_json() devuelve None si el body no es JSON; abre siempre con fallback {} y valida después.",
              },
              expl: [
                "Los convertidores <int:id> validan el tipo en la URL; /usuarios/abc da 404 directo.",
                "request.args es un MultiDict: si un query param puede faltar, usa .get() con default.",
                "jsonify fuerza el Content-Type correcto y permite códigos de estado.",
                "Por defecto una ruta solo acepta GET hasta que declares methods.",
                "Los handlers pueden devolver (cuerpo, status).",
              ],
              errors: [
                "Unsupported MediaType 415 al enviar JSON sin el Content-Type correcto.",
                "request.get_json() con body vacío devuelve None y revienta tu código.",
                "404 porque la ruta esté mal escrita o el convertidor no haga match.",
              ],
              tips: [
                "Valida que los datos estén presentes con .get() antes de usarlos.",
                "Devuelve códigos explícitos: 201 en creación, 404 en no encontrado.",
                "Agrupa rutas por recurso con GET y POST en la misma URL.",
                "Usa jsonify en vez de construir strings JSON a mano.",
              ],
              exercise: [
                "Crea /saludo?nombre=X que responda Hola X.",
                "Añade POST /usuarios que valide el email requerido y responda 400 si falta.",
                "Implementa /pedidos/<int:id> que devuelva 404 si el pedido no existe.",
              ],
              summary: [
                "Las rutas dinámicas <int:id> capturan parámetros de la URL.",
                "methods controla los verbos HTTP por ruta.",
                "request.get_json() y request.args leen la petición entrante.",
              ],
              lang: "python",
              exampleFile: "flask_rutas",
              desc: "Rutas, vistas y peticiones HTTP.",
            },
          },
          {
            id: "formularios-y-sesiones",
            title: "Formularios y sesiones",
            duration: "12 min",
            level: "avanzado",
            category: "flask",
            cardText: "Recibe formularios y mantiene sesiones de usuario con Flask.",
            desc: "Formularios y sesiones en Flask.",
            body: {
              intro: [
                "No todo llega por JSON: los formularios HTML envían datos con application/x-www-form-urlencoded. Y para saber quién es cada cliente entre peticiones usas sesiones.",
                "Flask maneja ambas cosas con request.form y la sesión firmada por cookies.",
              ],
              def: {
                par: "request.form lee los campos enviados por un formulario; session guarda datos por cliente usando una cookie firmada con app.secret_key.",
                porQue: "Las sesiones permiten recordar el estado del usuario (login, carrito, preferencias) sin re-enviar credenciales en cada petición.",
                cuando: "Usa formularios en vistas HTML (login, alta de clientes) y sesiones para mantener identidad y preferencias entre peticiones.",
              },
              concepts: [
                "request.form[\"campo\"] lee un campo del formulario.",
                "request.form.get permite un fallback si el campo falta.",
                "session es un dict por cliente respaldado por una cookie firmada.",
                "app.secret_key firma las cookies; sin ella session no funciona.",
                "session.clear() cierra la sesión.",
              ],
              basic: {
                p: "Recibir los datos de un formulario de login y mostrarlos.",
                code: [
                  "from flask import Flask, request",
                  "app = Flask(__name__)",
                  "",
                  '@app.route("/login", methods=["POST"])',
                  "def login():",
                  '    usuario = request.form.get("usuario", "")',
                  '    return {"recibido": usuario}',
                  "",
                  'if __name__ == "__main__":',
                  "    app.run(debug=True)",
                ],
                lang: "python",
              },
              intermediate: {
                p: "Guardar el usuario autenticado en la sesión y leerlo en otra ruta.",
                caption: "Escribir y leer la sesión con secret_key configurada",
                code: [
                  "from flask import Flask, session, request",
                  "",
                  "app = Flask(__name__)",
                  'app.secret_key = "cambia-esta-clave-en-produccion"',
                  "",
                  '@app.route("/login", methods=["POST"])',
                  "def login():",
                  '    usuario = request.form.get("usuario")',
                  '    session["usuario"] = usuario',
                  '    session["perfil"] = "admin" if usuario == "root" else "usuario"',
                  '    return {"login": "ok"}',
                  "",
                  "@app.route(\"/perfil\")",
                  "def perfil():",
                  '    usuario = session.get("usuario")',
                  "    if not usuario:",
                  '        return {"error": "no autenticado"}, 401',
                  '    return {"usuario": usuario, "perfil": session.get("perfil")}',
                  "",
                  "@app.route(\"/logout\")",
                  "def logout():",
                  "    session.clear()",
                  '    return {"login": "adios"}',
                ],
              },
              real: {
                p: "Flujo completo con curl: guardar la cookie, reutilizarla y cerrar sesión.",
                ul: [
                  "La cookie firmada vuelve en cada petición automáticamente.",
                  "Sin cookie, /perfil responde 401.",
                  "session.clear() invalida la identidad.",
                ],
                code: [
                  'curl -c cookies.txt -X POST -d "usuario=root" http://127.0.0.1:5000/login',
                  "",
                  "curl -b cookies.txt http://127.0.0.1:5000/perfil",
                  "",
                  "curl -b cookies.txt http://127.0.0.1:5000/logout",
                ],
                nota: "La cookie de sesión NO está cifrada, solo firmada: no guardes secretos en session. En producción suele sustituirse por sesiones en servidor (Redis).",
              },
              expl: [
                "request.form es un dict-like de los campos del formulario.",
                "session se comporta como un dict persistente por cliente.",
                "Sin secret_key Flask lanza un error al tocar session.",
                "La cookie con el flag Secure exige servir por HTTPS.",
                "La sesión expira según la config del navegador y de expires.",
              ],
              errors: [
                "RuntimeError: The session is unavailable porque falta app.secret_key.",
                "KeyError al pedir un campo de formulario que no llegó; usa get().",
                "401 en /perfil porque no pasaste por /login o la cookie se perdió.",
              ],
              tips: [
                "Fija app.secret_key desde variables de entorno, jamás en el código.",
                "Usa request.form.get con defaults para tolerar campos faltantes.",
                "Recorta y valida los campos del formulario como viste en la lección de strings.",
                "En producción sube las sesiones a un servidor (Redis) para escalar.",
              ],
              exercise: [
                "Crea /preferencias que guarde un color en session y lo devuelva.",
                "Protege /perfil devolviendo 401 si no hay un usuario en session.",
                "Escribe una ruta /contador que aumente un valor por petición usando session.",
              ],
              summary: [
                "request.form lee los datos de formularios HTML.",
                "session guarda el estado del cliente en una cookie firmada.",
                "secret_key es obligatoria y se toma de variables de entorno en producción.",
              ],
              lang: "python",
              exampleFile: "flask_sesiones",
              desc: "Formularios y sesiones en Flask.",
            },
          },
        ],
      },
      {
        id: "modulo-07",
        n: 7,
        t: "APIs con FastAPI",
        level: "avanzado",
        caption: "APIs modernas, tipadas y autodocumentadas.",
        desc: "FastAPI combina Python moderno, tipado y validación automática con Pydantic para construir APIs rápidas y autodocumentadas.",
        subtitle: "Construye APIs rápidas, tipadas y con documentación automática.",
        objectives: [
          "Levantar FastAPI con uvicorn y escribir los primeros endpoints",
          "Modelar y validar datos con Pydantic",
          "Construir un CRUD completo con documentación automática",
        ],
        lessons: [
          {
            id: "introduccion-a-fastapi",
            title: "Introducción a FastAPI",
            duration: "12 min",
            level: "avanzado",
            category: "fastapi",
            cardText: "Arranca FastAPI con uvicorn y crea endpoints con tipado moderno.",
            desc: "Introducción a FastAPI y uvicorn.",
            body: {
              intro: [
                "FastAPI es el framework moderno de Python para APIs: construido sobre la base ASGI, con validación con Pydantic y documentación OpenAPI automática.",
                "Su marca distintiva: anotaciones de tipos y endpoints en minutos, con docs interactivas en /docs.",
              ],
              def: {
                par: "FastAPI construye la API declarando funciones con parámetros tipados; uvicorn es el servidor ASGI que la ejecuta; Pydantic valida los modelos.",
                porQue: "Al tipar parámetros obtienes validación, documentación y autocompletado sin escribir código extra: la productividad que un backend moderno exige.",
                cuando: "Elige FastAPI para APIs nuevas, microservicios y proyectos donde el tipado y la documentación automática ahorren mantenimiento.",
              },
              concepts: [
                "pip install fastapi uvicorn[standard].",
                "from fastapi import FastAPI; app = FastAPI().",
                "@app.get(\"/\") convierte la función en endpoint.",
                "Los parámetros tipados en la firma se convierten en query params.",
                "uvicorn app:app --reload levanta el servidor con recarga.",
              ],
              basic: {
                p: "Instalar el stack y arrancar uvicorn desde la terminal.",
                code: [
                  'pip install "fastapi[standard]"',
                  "",
                  "# terminal",
                  "uvicorn main:app --reload",
                ],
                out: "Uvicorn running on http://127.0.0.1:8000",
                lang: "bash",
              },
              intermediate: {
                p: "El endpoint más simple y uno que usa query params tipados.",
                caption: "Primeras rutas con FastAPI",
                code: [
                  "from fastapi import FastAPI",
                  "",
                  'app = FastAPI(title="Mi API")',
                  "",
                  "@app.get(\"/\")",
                  "def raiz():",
                  '    return {"servicio": "FastAPI ok"}',
                  "",
                  "@app.get(\"/saludo\")",
                  "def saludo(nombre: str):",
                  '    return {"mensaje": f"Hola {nombre}"}',
                ],
              },
              real: {
                p: "Probar los endpoints y descubrir la documentación interactiva generada al instante.",
                ul: [
                  "/docs ofrece Swagger UI para probar sin curl.",
                  "/redoc ofrece la versión alternativa de documentación.",
                  "Los query params tipados aparecen en la documentación automática.",
                ],
                code: [
                  "curl \"http://127.0.0.1:8000/saludo?nombre=Ana\"",
                  "",
                  "# abre en el navegador:",
                  "# http://127.0.0.1:8000/docs",
                  "# http://127.0.0.1:8000/redoc",
                ],
                nota: "FastAPI detecta los tipos en la firma y genera el schema OpenAPI completo de tu API sin configurar nada.",
              },
              expl: [
                "El servidor ASGI (uvicorn) ejecuta la aplicación app que vive en el archivo main.py.",
                "Los query params tipados se declaran como argumentos de la función.",
                "Un tipado int obliga a FastAPI a rechazar textos no numéricos con 422.",
                "@app.get() crea el mapeo de verbo y ruta.",
                "--reload reinicia el servidor ante cada guardado, solo en desarrollo.",
              ],
              errors: [
                "ModuleNotFoundError: No module named 'fastapi' por instalar en el venv equivocado.",
                "uvicorn main:app no encuentra app si tu archivo no la llama así o la ruta está mal.",
                "422 Unprocessable Entity cuando el tipo de un parámetro no coincide.",
              ],
              tips: [
                "Usa venv e instala con fastapi[standard] para incluir herramientas útiles.",
                "Desactiva --reload en producción.",
                "Nombra tu aplicación app para el patrón uvicorn main:app sin pelear.",
                "Empieza probando en /docs antes de escribir tests.",
              ],
              exercise: [
                "Crea un endpoint /version que devuelva version y autor.",
                "Agrega /suma?x=2&y=3 con enteros tipados y devuelve el resultado.",
                "Abre /docs y prueba tus dos endpoints ahí.",
              ],
              summary: [
                "FastAPI + uvicorn levantan una API tipada en segundos.",
                "Los parámetros de función tipados son query params validados.",
                "/docs documenta y permite probar la API al instante.",
              ],
              lang: "python",
              exampleFile: "fastapi",
              desc: "Introducción a FastAPI.",
            },
          },
          {
            id: "pydantic-y-modelos",
            title: "Pydantic y modelos de datos",
            duration: "11 min",
            level: "avanzado",
            category: "fastapi",
            cardText: "Define modelos con Pydantic y valida los datos que entran y salen.",
            desc: "Modelos de datos con Pydantic.",
            body: {
              intro: [
                "La parte que hace brillar a FastAPI es Pydantic: defines modelos con clases y anotaciones, y obtienes validación, coerción y serialización automáticas.",
                "Un modelo Pydantic se parece a una dataclass con superpoderes de validación.",
              ],
              def: {
                par: "Pydantic valida datos usando modelos (subclases de BaseModel) con campos tipados: el framework los aplica a peticiones y respuestas automáticamente.",
                porQue: "Elimina decenas de if manuales: declaras el contrato y el framework responde 422 ante datos inválidos por ti.",
                cuando: "Usa modelos para el body de peticiones, parámetros complejos, esquemas de respuesta y configuración de la app.",
              },
              concepts: [
                "class Usuario(BaseModel) define el modelo.",
                "Los campos se anotan: nombre: str, activo: bool.",
                "Valores por defecto: rol: str = \"usuario\".",
                "Field permite restricciones: min_length, gt, regex.",
                "model_dump() serializa; el JSON de salida se genera solo.",
              ],
              basic: {
                p: "Un modelo Pydantic para recibir a un usuario en el body de un POST.",
                code: [
                  "from pydantic import BaseModel",
                  "",
                  "class UsuarioEntrada(BaseModel):",
                  "    nombre: str",
                  "    email: str",
                  "    edad: int",
                  "    activo: bool = True",
                  "",
                  'u = UsuarioEntrada(nombre="Ana", email="ana@mail.com", edad=30)',
                  "print(u.model_dump())",
                ],
                out: "{'nombre': 'Ana', 'email': 'ana@mail.com', 'edad': 30, 'activo': True}",
                lang: "python",
              },
              intermediate: {
                p: "Restricciones con Field para que la validación sea estricta.",
                caption: "Validación avanzada con Field",
                code: [
                  "from pydantic import BaseModel, Field",
                  "",
                  "class Producto(BaseModel):",
                  '    nombre: str = Field(min_length=3, max_length=80)',
                  "    precio: float = Field(gt=0)",
                  "    stock: int = Field(ge=0)",
                  "",
                  "try:",
                  '    Producto(nombre="Te", precio=-5, stock=0)',
                  "except Exception as error:",
                  "    print(error)",
                ],
              },
              real: {
                p: "Conectar el modelo a un endpoint: FastAPI valida el body y responde 422 ante errores.",
                ul: [
                  "Pydantic valida y convierte tipos automáticamente.",
                  "El 422 incluye el motivo exacto de cada campo.",
                  "La API declara el contrato en el schema OpenAPI.",
                ],
                code: [
                  "from fastapi import FastAPI",
                  "from pydantic import BaseModel",
                  "",
                  "app = FastAPI()",
                  "",
                  "class UsuarioEntrada(BaseModel):",
                  "    nombre: str",
                  "    email: str",
                  "",
                  '@app.post("/usuarios", status_code=201)',
                  "def crear_usuario(usuario: UsuarioEntrada):",
                  '    return {"recibido": usuario.nombre, "email": usuario.email}',
                  "",
                  "# curl -X POST -H \"Content-Type: application/json\" \\",
                  '#      -d "{\\"nombre\\": \\"Ana\\"}" http://127.0.0.1:8000/usuarios',
                ],
                nota: "Envía un body sin email y verás un 422 detallado en /docs: es la validación de Pydantic en acción.",
              },
              expl: [
                "Las instancias validadas se convierten al tipo anotado (por ejemplo, \"30\" a 30).",
                "Field permite min_length, gt, ge, lt, le y pattern.",
                "Enviar campos de más o incompletos dispara un 422 automático.",
                "El modelo define a la vez el schema de entrada, la documentación y la validación.",
                "Con model_dump() vuelves a dict para devolver JSON.",
              ],
              errors: [
                "NameError si la clase se define sin importar BaseModel.",
                "422 al enviar campos que faltan o que no están anotados.",
                "Olvidar que los campos SIN default son obligatorios.",
              ],
              tips: [
                "Separa modelos de entrada y salida, no mezcles campos internos.",
                "Usa Field con restricciones y mensajes claros.",
                "Explota los defaults para campos opcionales y evita Optional innecesarios.",
                "Valida también la configuración de tu app con modelos Pydantic.",
              ],
              exercise: [
                "Crea un modelo Pedido con id, total y estado usando Field.",
                "Añade validación de email con pattern a UsuarioEntrada.",
                "Expón el modelo en un endpoint POST y comprueba los 422 en /docs.",
              ],
              summary: [
                "Pydantic valida datos con modelos y tipos puros.",
                "Field añade restricciones detalladas.",
                "FastAPI usa el modelo para validar, documentar y serializar.",
              ],
              lang: "python",
              exampleFile: "pydantic",
              desc: "Modelos de datos con Pydantic.",
            },
          },
          {
            id: "endpoints-y-documentacion",
            title: "Endpoints CRUD y documentación automática",
            duration: "13 min",
            level: "avanzado",
            category: "fastapi",
            cardText: "Construye un CRUD completo y documentación OpenAPI automática.",
            desc: "Endpoints CRUD y documentación automática.",
            body: {
              intro: [
                "Reúne todo lo aprendido: modela con Pydantic, expón rutas con FastAPI y construye un CRUD (Crear, Leer, Actualizar, Borrar) completo.",
                "Como bonus, FastAPI documenta cada endpoint automáticamente en OpenAPI.",
              ],
              def: {
                par: "Un CRUD expone operaciones sobre un recurso: POST crea, GET lee (lista o detalle), PUT actualiza y DELETE borra; en FastAPI cada una es una ruta con status codes y response_model.",
                porQue: "Es la base de casi toda API de backend: los recursos del dominio (usuarios, pedidos) se manejan con este patrón estándar.",
                cuando: "Usa este patrón para tu primer API real con almacenamiento en memoria; luego sustituye la lista por una base de datos.",
              },
              concepts: [
                "response_model fija el esquema de salida tipado.",
                "status_code=201 para creación, 204 para borrado.",
                "HTTPException con status y detail controla los errores.",
                "Los path params int:id señalan el recurso concreto.",
                "FastAPI genera y sirve la documentación OpenAPI en /docs.",
              ],
              basic: {
                p: "El modelo y el almacenamiento en memoria del CRUD.",
                caption: "Modelo y store en memoria",
                code: [
                  "from fastapi import FastAPI, HTTPException",
                  "from pydantic import BaseModel",
                  "",
                  'app = FastAPI(title="API de Pedidos")',
                  "",
                  "class Pedido(BaseModel):",
                  "    cliente: str",
                  "    total: float",
                  "",
                  "pedidos = {}",
                  "contador = 0",
                ],
                lang: "python",
              },
              intermediate: {
                p: "Los endpoints para crear y listar, con response_model y status_code.",
                caption: "Crear (POST) y listar (GET)",
                code: [
                  '@app.post("/pedidos", status_code=201)',
                  "def crear_pedido(pedido: Pedido):",
                  "    global contador",
                  "    contador += 1",
                  "    pedidos[contador] = pedido",
                  '    return {"id": contador, **pedido.model_dump()}',
                  "",
                  "@app.get(\"/pedidos\")",
                  "def listar_pedidos():",
                  '    return [{"id": i, **p.model_dump()} for i, p in pedidos.items()]',
                ],
              },
              real: {
                p: "CRUD completo: detalle, actualizar y borrar con HTTPException en 404.",
                ul: [
                  "response_model documenta lo que devuelve cada endpoint.",
                  "HTTPException mantiene errores controlados en JSON.",
                  "La documentación /docs permite probar todo el flujo.",
                ],
                code: [
                  '@app.get("/pedidos/{id}")',
                  "def detalle_pedido(id: int):",
                  "    if id not in pedidos:",
                  '        raise HTTPException(status_code=404, detail="Pedido no encontrado")',
                  '    return {"id": id, **pedidos[id].model_dump()}',
                  "",
                  '@app.put("/pedidos/{id}")',
                  "def actualizar_pedido(id: int, pedido: Pedido):",
                  "    if id not in pedidos:",
                  '        raise HTTPException(status_code=404, detail="Pedido no encontrado")',
                  "    pedidos[id] = pedido",
                  '    return {"id": id, **pedido.model_dump()}',
                  "",
                  '@app.delete("/pedidos/{id}", status_code=204)',
                  "def borrar_pedido(id: int):",
                  "    if id not in pedidos:",
                  '        raise HTTPException(status_code=404, detail="Pedido no encontrado")',
                  "    del pedidos[id]",
                  "    return None",
                ],
                nota: "Abre /docs: Swagger UI te deja crear, listar y borrar pedidos sin escribir una línea de curl. Esa documentación nació del tipado.",
              },
              expl: [
                "response_model describe la respuesta y la valida contra el esquema.",
                "status_code por ruta informa al cliente del estado esperado.",
                "HTTPException mantiene los errores en JSON válido.",
                "global contador hace mutable el id incremental.",
                "Al cambiar la lista por una base de datos, los endpoints apenas cambian: mismo contrato.",
              ],
              errors: [
                "404 controlados vs 500 crudos: usa HTTPException y no KeyError.",
                "Olvidar el status_code=201 y responder 200 en creaciones.",
                "Un response_model que no calza con lo devuelto genera errores de validación.",
              ],
              tips: [
                "Agrupa tus RUTAS en routers (APIRouter) cuando el proyecto crezca.",
                "Usa modelos de salida separados para no exponer campos internos.",
                "Prueba los 4 verbos desde /docs antes de escribir tests con pytest.",
                "En producción sustituye pedidos {} por la base de datos; las rutas se mantienen.",
              ],
              exercise: [
                "Agrega un campo estado a Pedido y un endpoint para marcarlo entregado.",
                "Crea un endpoint /pedidos/{id}/total que devuelva el total con iva.",
                "Añade un GET /pedidos/estadisticas con totales y conteo.",
              ],
              summary: [
                "Un CRUD estándar usa POST, GET, PUT y DELETE sobre un recurso.",
                "HTTPException y status_code producen respuestas correctas.",
                "response_model y /docs documentan y prueban la API sin esfuerzo.",
              ],
              lang: "python",
              exampleFile: "crud",
              desc: "Endpoints CRUD y documentación automática.",
            },
          },
        ],
      },
    ],
  },
];