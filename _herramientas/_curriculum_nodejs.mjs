// _curriculum_nodejs.mjs
// Currículum del curso de Node.js desde cero hasta avanzado.
// Exporta COURSES con todos los módulos y lecciones.
// Las lecciones existentes en disco se listan SIN body (no se regeneran);
// las nuevas (módulos 04-07) llevan body completo (11 secciones estándar + metadatos).

export const COURSES = [
  {
    slug: "nodejs",
    title: "Node.js desde cero hasta avanzado",
    modules: [
      {
        id: "modulo-01",
        n: 1,
        t: "Introducción a Node.js",
        level: "intermedio",
        caption: "El runtime, el event loop y tus primeros scripts en el servidor.",
        desc: "Instala Node.js y npm, ejecuta tu primer script, y comprende el event loop, el runtime V8 y los módulos core path, fs y os.",
        subtitle: "JavaScript también vive en el servidor: conócelo por dentro.",
        objectives: [
          "Instalar Node.js y npm correctamente",
          "Ejecutar tu primer script en el servidor",
          "Entender el event loop y el modelo de concurrencia",
          "Conocer el runtime V8 y las APIs nativas",
          "Usar los módulos core path, fs y os",
        ],
        lessons: [
          {
            id: "ejecutando-tu-primer-script",
            title: "Ejecutando tu primer script",
            duration: "8 min",
            level: "intermedio",
            category: "fundamentos",
            cardText: "node archivo.js: tu primer programa fuera del navegador.",
            desc: "Crea y ejecuta tu primer script de Node.js en la terminal.",
          },
          {
            id: "el-event-loop",
            title: "El Event Loop de Node.js",
            duration: "12 min",
            level: "intermedio",
            category: "fundamentos",
            cardText: "Una sola hebra, miles de peticiones: así funciona el bucle de eventos.",
            desc: "Comprende el event loop y el modelo de concurrencia de Node.js.",
          },
          {
            id: "el-runtime-de-node",
            title: "El runtime de Node: V8 y APIs nativas",
            duration: "10 min",
            level: "intermedio",
            category: "fundamentos",
            cardText: "V8 compila JavaScript y las APIs nativas lo acercan al sistema.",
            desc: "Conoce el runtime de Node: el motor V8 y sus APIs nativas.",
          },
          {
            id: "instalando-node-y-npm",
            title: "Instalando Node.js y npm",
            duration: "8 min",
            level: "intermedio",
            category: "entorno",
            cardText: "nvm, node y npm: instala y verifica tu entorno de desarrollo.",
            desc: "Instala Node.js y npm y verifica que funcionan en tu sistema.",
          },
          {
            id: "modulos-core-path-fs-os",
            title: "Módulos core: path, fs y os",
            duration: "10 min",
            level: "intermedio",
            category: "fundamentos",
            cardText: "ruta, filesystem y sistema operativo: las API que siembran Node.",
            desc: "Usa los módulos core path, fs y os para trabajar con el sistema.",
          },
        ],
      },
      {
        id: "modulo-02",
        n: 2,
        t: "Módulos y npm",
        level: "intermedio",
        caption: "Organiza el código en módulos y gestiona dependencias con npm.",
        desc: "Escribe módulos propios con CommonJS y ES Modules, crea dependencias con lockfile, usa npm init/install/scripts y versiona paquetes.",
        subtitle: "Tu código en piezas, tus librerías bajo control con npm.",
        objectives: [
          "Distinguir CommonJS y ES Modules",
          "Crear y exportar tus propios módulos",
          "Entender dependencias y lockfile",
          "Usar npm init, install y scripts",
          "Versionar y publicar paquetes",
        ],
        lessons: [
          {
            id: "commonjs-y-es-modules",
            title: "CommonJS y ES Modules",
            duration: "10 min",
            level: "intermedio",
            category: "módulos",
            cardText: "require/module.exports frente a import/export: los dos estándares.",
            desc: "Conoce las diferencias entre CommonJS y ES Modules en Node.js.",
          },
          {
            id: "creando-tus-propios-modulos",
            title: "Creando tus propios módulos",
            duration: "10 min",
            level: "intermedio",
            category: "módulos",
            cardText: "Divide tu backend en archivos que se importan y se reutilizan.",
            desc: "Crea módulos propios, expórta sus partes y reutilízalos.",
          },
          {
            id: "dependencias-y-lockfile",
            title: "Dependencias y lockfile",
            duration: "9 min",
            level: "intermedio",
            category: "npm",
            cardText: "package-lock.json fija versiones exactas y hace el proyecto reproducible.",
            desc: "Comprende las dependencias y el papel del lockfile.",
          },
          {
            id: "npm-init-install-scripts",
            title: "npm init, install y scripts",
            duration: "10 min",
            level: "intermedio",
            category: "npm",
            cardText: "Inicia tu proyecto, instala paquetes y define scripts con npm run.",
            desc: "Gestiona tu proyecto con npm init, install y scripts.",
          },
          {
            id: "versionando-y-publicando-paquetes",
            title: "Versionando y publicando paquetes",
            duration: "9 min",
            level: "intermedio",
            category: "npm",
            cardText: "SemVer y npm publish para compartir tus módulos con el mundo.",
            desc: "Versiona con SemVer y publica paquetes en el registro de npm.",
          },
        ],
      },
      {
        id: "modulo-03",
        n: 3,
        t: "Asincronía avanzada",
        level: "intermedio",
        caption: "Callbacks, promesas y async/await: controla el tiempo en tu servidor.",
        desc: "Domina el código asíncrono: callbacks, promesas con encadenamiento, Promise.all/race, async/await con try/catch y workering multihilo con worker_threads.",
        subtitle: "El tiempo es el recurso más escaso de un backend asíncrono.",
        objectives: [
          "Escribir callbacks y evitar el callback hell",
          "Encadenar y componer promesas",
          "Combinar Promise.all y Promise.race",
          "Usar async/await con try/catch",
          "Mover cómputo pesado a worker_threads",
        ],
        lessons: [
          {
            id: "async-await-y-try-catch",
            title: "async/await y try/catch",
            duration: "10 min",
            level: "intermedio",
            category: "asincronía",
            cardText: "Código asíncrono con sintaxis síncrona: la forma moderna de Node.",
            desc: "Escribe código asíncrono legible con async/await y maneja errores.",
          },
          {
            id: "callbacks",
            title: "Callbacks",
            duration: "10 min",
            level: "intermedio",
            category: "asincronía",
            cardText: "Las funciones que se llaman cuando algo termina: la base de todo.",
            desc: "Aprende el patrón callback y sus trampas en Node.js.",
          },
          {
            id: "promesas-y-encadenamiento",
            title: "Promesas y encadenamiento",
            duration: "12 min",
            level: "intermedio",
            category: "asincronía",
            cardText: "then/catch y el encadenamiento de operaciones asíncronas.",
            desc: "Domina las promesas y su encadenamiento en Node.js.",
          },
          {
            id: "promise-all-y-promise-race",
            title: "Promise.all y Promise.race",
            duration: "10 min",
            level: "intermedio",
            category: "asincronía",
            cardText: "Aguanta peticiones en paralelo y corre tus propias carreras.",
            desc: "Combina promesas con Promise.all y Promise.race.",
          },
          {
            id: "worker-threads",
            title: "Worker Threads",
            duration: "12 min",
            level: "intermedio",
            category: "asincronía",
            cardText: "Cómputo CPU intensivo sin bloquear el event loop, en otras hebras.",
            desc: "Mueve tareas pesadas a hilos con worker_threads.",
          },
        ],
      },
      {
        id: "modulo-04",
        n: 4,
        t: "Express avanzado",
        level: "intermedio",
        caption: "Más allá del hola mundo: middlewares, rutas y errores en Express.",
        desc: "Construye APIs ordenadas con Express: el pipeline de middlewares, enrutado modular con Router, manejo centralizado de errores, body-parser y controladores asíncronos.",
        subtitle: "Una API Express bien estructurada que escala con tu proyecto.",
        objectives: [
          "Entender el pipeline de middlewares",
          "Organizar el código con el Router de Express",
          "Centralizar el manejo de errores",
          "Procesar cuerpos de petición con body-parser",
          "Encapsular la lógica en controladores asíncronos",
        ],
        lessons: [
          {
            id: "middleware-pipeline-express",
            title: "El pipeline de middlewares",
            duration: "11 min",
            level: "intermedio",
            category: "express",
            cardText: "app.use encadena funciones que pasan la petición de mano en mano.",
            desc: "Construye y encadena middlewares en el pipeline de Express.",
            body: {
              intro: [
                "Casi todo lo que hace una API Express ocurre dentro de funciones encadenadas: loguear, autenticar, parsear, responder. Cada una de esas funciones es un middleware.",
                "El pipeline es una cebolla: la petición entra pasa capa por capa, y cada middleware decide si responde ya o llama a next() para continuar.",
              ],
              def: {
                par: "Un middleware es una función (req, res, next) que se registra con app.use y se ejecuta en orden para cada petición, pudiendo terminar la respuesta o delegar con next().",
                porQue: "Las preocupaciones transversales (logs, auth, CORS, parseo) se resuelven una vez por petición sin repetir código en cada ruta.",
                cuando: "Usa middlewares para tareas en cada petición: logueo, compresión, autenticación, cabeceras. Las tareas específicas de una ruta quedan en su controlador.",
              },
              concepts: [
                "app.use(middleware) registra una función que corre en cada petición.",
                "next() continúa la cadena; sin next la petición queda colgada.",
                "El orden importa: los middlewares se ejecutan en el orden registrado.",
                "Un middleware responde o llama a next(), nunca ambas cosas.",
                "app.use('/ruta', mw) limita el middleware a ese prefijo.",
              ],
              basic: {
                p: "Un middleware que loguea toda petición antes de llegar a la ruta.",
                code: [
                  "const express = require('express');",
                  "const app = express();",
                  "",
                  "const log = (req, res, next) => {",
                  "  console.log(`${req.method} ${req.url}`);",
                  "  next();",
                  "};",
                  "",
                  "app.use(log);",
                  "",
                  "app.get('/', (req, res) => {",
                  "  res.json({ ok: true });",
                  "});",
                  "",
                  "app.listen(3000);",
                ],
                out: "GET / -> se imprime 'GET /' y responde {\"ok\":true}",
                lang: "js",
              },
              intermediate: {
                p: "Middlewares que responden ellos mismos, como un gatekeeper de autenticación.",
                caption: "Middleware que corta la cadena con 401",
                code: [
                  "const requireToken = (req, res, next) => {",
                  "  const token = req.headers['authorization'];",
                  "",
                  "  if (!token) {",
                  "    return res.status(401).json({ error: 'no autorizado' });",
                  "  }",
                  "",
                  "  next();",
                  "};",
                  "",
                  "app.get('/pedidos', requireToken, (req, res) => {",
                  "  res.json({ pedidos: [] });",
                  "});",
                ],
                lang: "js",
              },
              real: {
                p: "Un pipeline típico de producción: parseo, log, seguridad y rutas, cada pieza con un propósito y en el orden correcto.",
                ul: [
                  "Registra los middlewares globales antes de las rutas.",
                  "Pon el middleware específico justo en la ruta que lo necesita.",
                  "Responde con next(err) en errores para delegar al manejador central.",
                ],
                code: [
                  "const express = require('express');",
                  "const helmet = require('helmet');",
                  "const app = express();",
                  "",
                  "app.use(helmet());",
                  "app.use(express.json());",
                  "app.use((req, res, next) => {",
                  "  console.log(`${req.method} ${req.url} - ${Date.now()}`);",
                  "  next();",
                  "});",
                  "",
                  "app.use('/clientes', require('./rutas/clientes'));",
                  "app.use('/pedidos', require('./rutas/pedidos'));",
                  "",
                  "app.use((err, req, res, next) => {",
                  "  console.error(err);",
                  "  res.status(500).json({ error: 'fallo interno' });",
                  "});",
                ],
                nota: "El middleware de error tiene CUATRO parámetros: su firma le dice a Express que es el manejador global de fallos.",
                lang: "js",
              },
              expl: [
                "Cada petición recorre el pipeline en el orden en que registraste las capas.",
                "next() sin argumentos continúa; next(err) salta directo al manejador de error.",
                "Un middleware que no responde ni llama a next() cuelga la petición para siempre.",
                "Las rutas son middlewares con verbo y ruta: Express las selecciona en la cola.",
                "El orden correcto resuelve la mayoría de los bugs de middleware.",
              ],
              errors: [
                "Registrar rutas antes que los middlewares globales y no recibirlos.",
                "Olvidar next() en un middleware: la petición se queda colgada.",
                "Llamar a next() tras haber enviado la respuesta.",
              ],
              tips: [
                "Mantén cada middleware en una sola responsabilidad.",
                "Usa app.use('/ruta', mw) para no enganchar middlewares a todo.",
                "Pon el manejador de errores el último del pipeline.",
                "Comprueba con curl -v que las cabeceras y el orden se cumplen.",
              ],
              exercise: [
                "Añade un middleware que mida y loguee la duración de cada petición.",
                "Protege la ruta /admin con un middleware que corte con 403 si no hay token.",
                "Reordena el pipeline y observa cómo cambia el comportamiento.",
              ],
              summary: [
                "Los middlewares encadenan el tratamiento de cada petición.",
                "next() continúa, responder corta, next(err) salta a errores.",
                "El orden de registro define el orden de ejecución.",
              ],
              lang: "js",
              exampleFile: "middleware-express",
              desc: "Construye y encadena middlewares en el pipeline de Express.",
            },
          },
          {
            id: "enrutado-modular-con-router",
            title: "Enrutado modular con Router",
            duration: "10 min",
            level: "intermedio",
            category: "express",
            cardText: "express.Router agrupa rutas por recurso y las monta en la app.",
            desc: "Organiza tus endpoints con express.Router y apps modulares.",
            body: {
              intro: [
                "Cuando tu API crece, un solo archivo con todas las rutas se convierte en un caos de quinientas líneas. Express resuelve esto con express.Router.",
                "Un Router agrupa las rutas de un recurso (pedidos, usuarios) en su propio módulo, con su propia lógica de middlewares, y se monta en la app como una pieza independiente.",
              ],
              def: {
                par: "express.Router() crea una instancia aislada de rutas y middlewares que se exporta como módulo y se monta en la app con app.use('/recurso', router).",
                porQue: "El enrutado modular divide el código por dominio, facilita testear cada recurso y evita que una app gigante se convierta en un solo archivo insostenible.",
                cuando: "Úsalo desde la primera carpeta de rutas. Cada recurso o grupo de funcionalidad merece su propio Router.",
              },
              concepts: [
                "const router = express.Router() abre un enrutador aislado.",
                "router.get('/:id', ...) define rutas relativas al prefijo de montaje.",
                "module.exports = router exporta el Router para la app.",
                "app.use('/pedidos', pedidosRouter) lo monta con un prefijo común.",
                "Los Routers pueden llevar sus propios middlewares con router.use().",
              ],
              basic: {
                p: "Separamos las rutas de pedidos en su propio módulo y lo montamos en la app.",
                code: [
                  "// rutas/pedidos.js",
                  "const express = require('express');",
                  "const router = express.Router();",
                  "",
                  "router.get('/', (req, res) => {",
                  "  res.json({ pedidos: [] });",
                  "});",
                  "",
                  "router.post('/', (req, res) => {",
                  "  res.status(201).json({ id: 1, ...req.body });",
                  "});",
                  "",
                  "module.exports = router;",
                  "",
                  "// app.js",
                  "const pedidosRouter = require('./rutas/pedidos');",
                  "app.use('/pedidos', pedidosRouter);",
                ],
                out: "GET /pedidos y POST /pedidos quedan montados con el prefijo común.",
                lang: "js",
              },
              intermediate: {
                p: "Las rutas con parámetro se agrupan y sus middlewares específicos se resuelven dentro del Router.",
                caption: "Rutas con parámetro en el Router",
                code: [
                  "const router = express.Router();",
                  "",
                  "const cargarPedido = (req, res, next) => {",
                  "  const id = Number(req.params.id);",
                  "  if (!Number.isInteger(id)) {",
                  "    return res.status(400).json({ error: 'id invalido' });",
                  "  }",
                  "  req.pedidoId = id;",
                  "  next();",
                  "};",
                  "",
                  "router.use('/:id', cargarPedido);",
                  "",
                  "router.get('/:id', (req, res) => {",
                  "  res.json({ id: req.pedidoId });",
                  "});",
                  "",
                  "module.exports = router;",
                ],
                lang: "js",
              },
              real: {
                p: "Una estructura de carpetas por dominio (rutas/controladores/servicios) hace que cada Recurso crezca aislado y los tests apunten a lo concreto.",
                ul: [
                  "Un Router por recurso: pedidos, usuarios, productos.",
                  "File-level: building Express modular ya es material de seniority.",
                  "Monta los Routers con prefijos plurales y versiona en la URL si lo necesitas.",
                ],
                code: [
                  "// app.js",
                  "const express = require('express');",
                  "const app = express();",
                  "",
                  "app.use('/usuarios', require('./rutas/usuarios'));",
                  "app.use('/pedidos', require('./rutas/pedidos'));",
                  "app.use('/productos', require('./rutas/productos'));",
                  "",
                  "// Api REST coherente",
                  "//   GET    /usuarios        listar",
                  "//   POST   /usuarios        crear",
                  "//   GET    /usuarios/:id    detalle",
                  "//   PUT    /usuarios/:id    actualizar",
                  "//   DELETE /usuarios/:id    borrar",
                ],
                nota: "Puedes anidar Routers: dentro de pedidos, un sub-router para líneas. La jerarquía refleja la realidad del recurso.",
                lang: "js",
              },
              expl: [
                "El Router es una app Express en miniatura: monta capas y rutas.",
                "Al montarlo con /pedidos, todas sus rutas relativas heredan el prefijo.",
                "router.use() aplica middlewares a todas las rutas del Router.",
                "Exportar e importar Routers mantiene tu código en piezas pequeñas.",
                "La estructura por recurso hace el mantenimiento predecible.",
              ],
              errors: [
                "Definir en el Router rutas con el prefijo ya añadido (/pedidos/pedidos).",
                "Olvidar module.exports = router y recibir app.use de undefined.",
                "Crear una app Express por Router en vez de montarlos en una sola.",
              ],
              tips: [
                "Usa prefijos plurales: /pedidos, no /pedido.",
                "Mantén Router pequeño; si crece, rompe el recurso en sub-routers.",
                "Aplica en el Router solo los middlewares que necesitan sus rutas.",
              ],
              exercise: [
                "Extrae las rutas de usuarios a su propio Router y móntalo.",
                "Añade una ruta con parámetro /:id validando el id en un middleware.",
                "Crea un sub-router de líneas dentro de pedidos.",
              ],
              summary: [
                "express.Router agrupa rutas por recurso en un módulo.",
                "app.use con prefijo monta el Router en la app principal.",
                "La modularización crece con tu dominio sin inflar un archivo.",
              ],
              lang: "js",
              exampleFile: "router-express",
              desc: "Organiza tus endpoints con express.Router y apps modulares.",
            },
          },
          {
            id: "manejo-centralizado-de-errores",
            title: "Manejo centralizado de errores",
            duration: "11 min",
            level: "intermedio",
            category: "express",
            cardText: "Un solo middleware de error que responde API en JSON y nunca filtra internos.",
            desc: "Centraliza el manejo de errores de tu API Express.",
            body: {
              intro: [
                "Cuando cada ruta maneja sus errores a su manera, la API contesta formatos dispares y los fallos internos se filtran al cliente. El manejo centralizado pone a todos los errores en una sola puerta de salida.",
                "Express distingue los middlewares de error por su firma de cuatro parámetros: reciben el fallo de cualquier ruta anterior y deciden la respuesta.",
              ],
              def: {
                par: "Un middleware de error tiene la firma (err, req, res, next), se registra al final del pipeline y recibe cualquier error que next(err) o una excepción lanzada provoquen en las rutas anteriores.",
                porQue: "Respuestas de error uniformes, logs sin secretos y la lógica de errores en un único lugar hacen el backend debugeable y seguro.",
                cuando: "Úsalo en toda API de tamaño medio. Combínalo con una clase propia de errores para enviar códigos HTTP precisos.",
              },
              concepts: [
                "La firma (err, req, res, next) identifica al manejador de error.",
                "next(err) salta a la primera capa de error desde cualquier punto.",
                "Una clase ErrorHttp con statusCode transporta el código HTTP.",
                "Los errores desconocidos se mapean a 500 y se registran sin filtrar.",
                "El manejador va el ÚLTIMO, tras las rutas y middlewares.",
              ],
              basic: {
                p: "Nuestro ErrorHttp con código de estado y el manejador que lo traduce a JSON.",
                code: [
                  "class ErrorHttp extends Error {",
                  "  constructor(statusCode, message) {",
                  "    super(message);",
                  "    this.statusCode = statusCode;",
                  "  }",
                  "}",
                  "",
                  "app.get('/pepinos/:id', (req, res, next) => {",
                  "  const id = Number(req.params.id);",
                  "",
                  "  if (!Number.isInteger(id)) {",
                  "    return next(new ErrorHttp(400, 'id invalido'));",
                  "  }",
                  "",
                  "  next(new ErrorHttp(404, 'no existe'));",
                  "});",
                  "",
                  "app.use((err, req, res, next) => {",
                  "  const status = err.statusCode || 500;",
                  "  res.status(status).json({ error: err.message });",
                  "});",
                ],
                out: "GET /pepinos/abc -> 400 {\"error\":\"id invalido\"}",
                lang: "js",
              },
              intermediate: {
                p: "Separando errores conocidos (con código) y desconocidos (500 con log interno).",
                caption: "Dividir errores conocidos y fallos internos",
                code: [
                  "app.use((err, req, res, next) => {",
                  "  if (err instanceof ErrorHttp) {",
                  "    return res.status(err.statusCode).json({ error: err.message });",
                  "  }",
                  "",
                  "  console.error('Fallo no controlado:', err);",
                  "  res.status(500).json({ error: 'fallo interno del servidor' });",
                  "});",
                ],
                lang: "js",
              },
              real: {
                p: "El manejo centralizado convierte fallos de rutas, validaciones, base de datos y JSON mal formado en respuestas coherentes de un solo formato.",
                ul: [
                  "Usa codes HTTP precisos: 400, 401, 403, 404, 409, 422, 500.",
                  "Loggea el stack en el servidor pero nunca lo envíes al cliente.",
                  "Lanza ErrorHttp en servicios, no solo en las rutas.",
                ],
                code: [
                  "async function buscarPedido(id) {",
                  "  const pedido = await db.buscar('pedidos', id);",
                  "",
                  "  if (!pedido) {",
                  "    throw new ErrorHttp(404, 'pedido no encontrado');",
                  "  }",
                  "",
                  "  return pedido;",
                  "}",
                  "",
                  "app.get('/pedidos/:id', async (req, res, next) => {",
                  "  try {",
                  "    const pedido = await buscarPedido(req.params.id);",
                  "    res.json(pedido);",
                  "  } catch (err) {",
                  "    next(err);",
                  "  }",
                  "});",
                ],
                nota: "Con async/await en las rutas, envuelve el try/catch para reenviar a next(err). Un helper wrapAsync te ahorra repetirlo (lo verás en la siguiente lección).",
                lang: "js",
              },
              expl: [
                "Express solo ejecuta un middleware de error cuando detecta los 4 parámetros.",
                "next(err) detiene el pipeline normal y salta a los manejadores de error.",
                "Una excepción no capturada en un async handler se pierde: captúrala.",
                "Mapear desconocidos a 500 mantiene la API consistente.",
                "Los logs internos quedan para operaciones, no para el cliente.",
              ],
              errors: [
                "Registrar el manejador de error antes de las rutas (no se ejecutará).",
                "Filtrar el stack de errores al JSON de respuesta.",
                "Olvidar capturar async: el error se pierde y la petición se cuelga.",
              ],
              tips: [
                "Crea ErrorHttp como única vía de errores con código.",
                "Mantén los mensajes en español, sin detalles internos.",
                "Registra siempre: el log es tu faro al debugear producción.",
              ],
              exercise: [
                "Crea ErrorHttp y haz que una ruta devuelva 404 al faltar el recurso.",
                "Registra el manejador de error al final y verifica un 500 genérico.",
                "Envía un error 422 cuando la validación falle.",
              ],
              summary: [
                "El manejador central de errores vive al final del pipeline.",
                "ErrorHttp transporta el código HTTP hasta la respuesta.",
                "Los fallos internos se loguean y nunca se exponen al cliente.",
              ],
              lang: "js",
              exampleFile: "errores-express",
              desc: "Centraliza el manejo de errores de tu API Express.",
            },
          },
          {
            id: "body-parser-y-peticiones",
            title: "body-parser y peticiones entrantes",
            duration: "9 min",
            level: "intermedio",
            category: "express",
            cardText: "express.json() convierte los cuerpos JSON en req.body listo para usar.",
            desc: "Procesa cuerpos de petición con express.json() y urlencoded.",
            body: {
              intro: [
                "Un cliente manda datos en el cuerpo de la petición: JSON en las APIs modernas. Express no los interpreta solo: necesita un middleware que convierta ese texto crudo en req.body.",
                "express.json() (antiguo body-parser) parsea el cuerpo JSON y deja los datos tipados a tu lógica.",
              ],
              def: {
                par: "express.json() es un middleware que lee el cuerpo bruto de la petición con header Content-Type: application/json y lo deserializa en req.body, ya como objeto JavaScript.",
                porQue: "Sin él, req.body es undefined y toda inserción de datos se rompe o acaba parseando a mano de forma insegura.",
                cuando: "Actívalo globalmente en APIs JSON. express.urlencoded() procesa formularios (application/x-www-form-urlencoded) cuando tengas webs con formularios clásicos.",
              },
              concepts: [
                "app.use(express.json()) activa el parseo JSON global.",
                "req.body contiene los datos ya deserializados como objeto.",
                "express.urlencoded({ extended: true }) procesa formularios anidados.",
                "Los cuerpos sin Content-Type adecuado no se parsean.",
                "Puedes limitar el tamaño con { limit: '10mb' }.",
              ],
              basic: {
                p: "POST que recibe JSON del cliente y lo reutiliza en la respuesta.",
                code: [
                  "const express = require('express');",
                  "const app = express();",
                  "",
                  "app.use(express.json());",
                  "",
                  "app.post('/pedidos', (req, res) => {",
                  "  const total = req.body.total;",
                  "  res.status(201).json({ recibido: total });",
                  "});",
                  "",
                  "// POST /pedidos  {'total': 99.9}",
                  "// -> 201 {'recibido': 99.9}",
                ],
                out: "Con curl -d '{\"total\":99.9}' responde 201 con el total recibido.",
                lang: "js",
              },
              intermediate: {
                p: "Con urlencoded, los formularios web clásicos rellenan req.body igual que JSON.",
                caption: "Formularios clásicos con urlencoded",
                code: [
                  "const express = require('express');",
                  "const app = express();",
                  "",
                  "app.use(express.urlencoded({ extended: true }));",
                  "",
                  "app.post('/login', (req, res) => {",
                  "  const { email, password } = req.body;",
                  "  res.json({ acceso: !!email && !!password });",
                  "});",
                  "",
                  "// curl -d 'email=a@mail.com&password=x' /login",
                ],
                lang: "js",
              },
              real: {
                p: "En producción conviene limitar el tamaño del cuerpo y proteger contra arrays de gran tamaño, además de validar lo parseado.",
                ul: [
                  "Fija un límite razonable de cuerpo según tu caso (1mb-10mb).",
                  "Valida y sanea req.body tras el parseo: no confíes en el cliente.",
                  "Rechaza JSON no válido con el manejador de errores.",
                ],
                code: [
                  "const express = require('express');",
                  "const app = express();",
                  "",
                  "app.use(express.json({ limit: '1mb' }));",
                  "",
                  "app.post('/pedidos', (req, res, next) => {",
                  "  const data = req.body;",
                  "",
                  "  if (!data || typeof data.total !== 'number') {",
                  "    return next(new ErrorHttp(422, 'cuerpo invalido'));",
                  "  }",
                  "",
                  "  res.status(201).json({ ok: true });",
                  "});",
                ],
                nota: "Si el JSON llega mal formado, express.json() lanza SyntaxError con status 400 y tu manejador central de errores lo traducirá si contemplas el caso.",
                lang: "js",
              },
              expl: [
                "express.json() sustituye al antiguo paquete body-parser en Express 4.16+.",
                "El middleware se encadena y solo actúa si el Content-Type es application/json.",
                "Un limit previene DoS por cuerpos gigantes.",
                "req.body se limpia del parseo: los datos pasan a tu lógica como objetos.",
                "urlencoded convierte los pares clave=valor de los formularios clásicos.",
              ],
              errors: [
                "No registrar el middleware y acceder a req.body undefined.",
                "Enviar datos sin el Content-Type y esperar que se parseen.",
                "Confiar en req.body sin validar: es entrada del usuario.",
              ],
              tips: [
                "Activa express.json() una vez, globalmente, al crear la app.",
                "Pon un límite de tamaño acorde a tu API.",
                "Combina con validación (Zod) justo después del parseo.",
              ],
              exercise: [
                "Crea un POST que reciba JSON y devuelva el total calculado.",
                "Añade express.urlencoded y prueba con un formulario simple.",
                "Fija un límite de 1mb y comprueba el error con un cuerpo enorme.",
              ],
              summary: [
                "express.json() parsea el cuerpo JSON a req.body.",
                "urlencoded cubre los formularios clásicos.",
                "Tras parsear, valida: el cuerpo sigue siendo entrada del usuario.",
              ],
              lang: "js",
              exampleFile: "body-parser",
              desc: "Procesa cuerpos de petición con express.json() y urlencoded.",
            },
          },
          {
            id: "async-handlers-y-controladores",
            title: "Controladores asíncronos y async handlers",
            duration: "10 min",
            level: "intermedio",
            category: "express",
            cardText: "Los errores async no se pierden: envuelve tus handlers y extrae controladores.",
            desc: "Envuelve controladores asíncronos y evita que los errores se pierdan.",
            body: {
              intro: [
                "Un controlador asíncrono que lanza una excepción no la entrega a Express: al haber un await entre medias, la promesa rechazada se pierde y la petición queda colgada.",
                "La solución es el patrón asyncHandler: una envoltura que captura el rechazo y lo reenvía a next(err), y la extracción de la lógica a controladores testables.",
              ],
              def: {
                par: "Un asyncHandler es una función que envuelve un controlador (req, res, next), ejecuta la promesa que devuelve y captura las excepciones con .catch(next) para llevarlas al manejador central de errores.",
                porQue: "Sin la envoltura, cada moderno framework Node que usa async/await se pierde los fallos. El asyncHandler lo arregla en una línea.",
                cuando: "Úsalo en toda ruta asíncrona que toque la base de datos, servicios HTTP o cualquier promesa. Es la base de los controladores en producción.",
              },
              concepts: [
                "async (req, res) => ... devuelve una promesa que Express no escucha.",
                "asyncHandler captura el rechazo y llama a next(err).",
                "Los controladores extraídos se testean sin instanciar Express.",
                "El controlador no responde dos veces: si habla, no delega.",
                "Express 5 captura rechazos automáticamente; en v4 se usa el wrapper.",
              ],
              basic: {
                p: "Un asyncHandler que evita que el error del servicio se trague.",
                code: [
                  "const asyncHandler = (fn) => (req, res, next) =>",
                  "  Promise.resolve(fn(req, res, next)).catch(next);",
                  "",
                  "app.get('/pedidos/:id', asyncHandler(async (req, res) => {",
                  "  const pedido = await buscarPedido(req.params.id);",
                  "  res.json(pedido);",
                  "}));",
                ],
                out: "Si buscarPedido lanza, next(err) lo lleva al manejador central en vez de colgar la petición.",
                lang: "js",
              },
              intermediate: {
                p: "La lógica pasa de la ruta a controladores reutilizables que reciben los datos que necesitan.",
                caption: "Extracción del controlador para testear",
                code: [
                  "const { pedidoServicio } = require('../servicios/pedidos');",
                  "",
                  "const controladores = {",
                  "  detalle: async (req, res) => {",
                  "    const pedido = await pedidoServicio.porId(req.params.id);",
                  "    res.json(pedido);",
                  "  },",
                  "",
                  "  crear: async (req, res) => {",
                  "    const id = await pedidoServicio.crear(req.body);",
                  "    res.status(201).json({ id });",
                  "  },",
                  "};",
                  "",
                  "const rutas = require('express').Router();",
                  "rutas.get('/:id', asyncHandler(controladores.detalle));",
                  "rutas.post('/', asyncHandler(controladores.crear));",
                ],
                lang: "js",
              },
              real: {
                p: "Con el servicio separado y el handler envuelto, cada pieza se prueba en aislamiento y la API no vuelve a colgarse por un fallo de base de datos.",
                ul: [
                  "Extrae la lógica a controladores con una sola responsabilidad.",
                  "Envuelve todo handler async en asyncHandler para no perder fallos.",
                  "Deja que el manejador central de errores decida la respuesta.",
                ],
                code: [
                  "// utilidades/asyncHandler.js",
                  "module.exports = (fn) => (req, res, next) =>",
                  "  Promise.resolve(fn(req, res, next)).catch(next);",
                  "",
                  "// rutas/pedidos.js",
                  "const asyncHandler = require('../utilidades/asyncHandler');",
                  "",
                  "router.post('/:id/pagar', asyncHandler(async (req, res) => {",
                  "  const resultado = await pedidoServicio.pagar(",
                  "    req.params.id,",
                  "    req.body.pago,",
                  "  );",
                  "",
                  "  res.json({ ok: true, saldo: resultado.saldo });",
                  "}));",
                ],
                nota: "El patrón funciona igual con promesas: Promise.resolve() cubre funciones que devuelven promesa y no-promesa a la vez.",
                lang: "js",
              },
              expl: [
                "Un await abre una vía de rechazo que Express 4 no vigila por defecto.",
                "La envoltura convierte el rechazo en un next(err) rutinario.",
                "Los controladores extraídos se prueban con asserts sin levantar el servidor.",
                "El manejador central sigue siendo la única puerta de respuestas de error.",
                "Express 5 detecta los rechazos automáticamente, pero el wrapper sigue siendo portable.",
              ],
              errors: [
                "Asumir que Express 4 captura un throw dentro de un async handler.",
                "Responder dos veces (res.json en la ruta y en el catch).",
                "Olvidar importar el wrapper y ver peticiones colgadas en producción.",
              ],
              tips: [
                "Crea el wrapper en utilidades y úsalo en todas las rutas.",
                "Mantén controladores finos y servicios con la lógica.",
                "Actualiza a Express 5 cuando puedas: la captura mejora.",
              ],
              exercise: [
                "Crea un asyncHandler y úsalo en una ruta que llame a un servicio.",
                "Extrae un controlador detalle y escríbele un test con método doble.",
                "Comprueba que un error de servicio llega al manejador 500 central.",
              ],
              summary: [
                "Los errores de un async handler se pierden sin envoltura.",
                "asyncHandler relega el rechazo a next(err) en una línea.",
                "Controladores extraídos + wrapper = API robusta y testable.",
              ],
              lang: "js",
              exampleFile: "async-handlers",
              desc: "Envuelve controladores asíncronos y evita que los errores se pierdan.",
            },
          },
        ],
      },
      {
        id: "modulo-05",
        n: 5,
        t: "Bases de datos con Node",
        level: "intermedio",
        caption: "Conecta, consulta y migra PostgreSQL desde tu API Node.",
        desc: "Integra bases de datos relacionales: conexión con pg, consultas parametrizadas, pool de conexiones, migraciones con seeds y modelado con Prisma.",
        subtitle: "De las variables a la base de datos, sin sacrificar seguridad.",
        objectives: [
          "Conectar a PostgreSQL con el driver pg",
          "Ejecutar consultas parametrizadas y seguras",
          "Gestionar un pool de conexiones",
          "Aplicar migraciones y sembrar datos",
          "Modelar el dominio con Prisma",
        ],
        lessons: [
          {
            id: "conexion-postgresql-con-pg",
            title: "Conectando a PostgreSQL con pg",
            duration: "11 min",
            level: "intermedio",
            category: "bases de datos",
            cardText: "El cliente pg conecta tu Node a PostgreSQL con promesas.",
            desc: "Conecta tu API a PostgreSQL con el cliente pg.",
            body: {
              intro: [
                "La mayoría de los backends terminan guardando su estado en una base de datos relacional. Con Node, el cliente estándar para PostgreSQL es pg.",
                "pg ofrece dos modos: el Client para una conexión puntual y el Pool para reutilizar conexiones. Esta lección arranca con lo básico: conectar y ejecutar la primera consulta.",
              ],
              def: {
                par: "pg es el driver oficial de PostgreSQL para Node. Con new Client o new Pool y un objeto de configuración (host, port, database, user, password) ejecutas consultas mediante métodos que devuelven promesas.",
                porQue: "Persistir tus datos con un driver probado y promesas de serie te permite querying seguro y estructurado sin salir del ecosistema Node.",
                cuando: "Usa Client para scripts puntuales y Pool para servidores en producción que reciben muchas peticiones por segundo.",
              },
              concepts: [
                "npm install pg añade el driver al proyecto.",
                "new Client() abre una conexión única para scripts.",
                "client.query(sql, params) devuelve una promesa con rows.",
                "Las credenciales viven en variables de entorno, no en el código.",
                "El parámetro connectionString agrupa toda la configuración.",
              ],
              basic: {
                p: "Instalamos y conectamos, ejecutando la primera consulta con pg.",
                code: [
                  "const { Client } = require('pg');",
                  "",
                  "const client = new Client({",
                  "  connectionString: process.env.DATABASE_URL,",
                  "});",
                  "",
                  "async function saludar() {",
                  "  await client.connect();",
                  "  const res = await client.query('SELECT version()');",
                  "  console.log(res.rows[0].version);",
                  "  await client.end();",
                  "}",
                  "",
                  "saludar();",
                ],
                out: "PostgreSQL 16.x on x86_64-pc-linux-gnu ...",
                lang: "js",
              },
              intermediate: {
                p: "Con un modelo de datos real, una tabla usuarios recién creada responde de inmediato.",
                caption: "Primera consulta a una tabla real",
                code: [
                  "const { Client } = require('pg');",
                  "const client = new Client({",
                  "  host: '127.0.0.1',",
                  "  database: 'tienda',",
                  "  user: 'app',",
                  "  password: process.env.DB_PASS,",
                  "});",
                  "",
                  "async function listar() {",
                  "  await client.connect();",
                  "  const { rows } = await client.query(",
                  "    'SELECT id, email FROM usuarios ORDER BY id LIMIT 5'",
                  "  );",
                  "  console.log(rows);",
                  "  await client.end();",
                  "}",
                  "",
                  "listar().catch((err) => console.error(err));",
                ],
                lang: "js",
              },
              real: {
                p: "Cada petición HTTP pide datos: la conexión se abre una vez y el query se ejecuta con parámetros para no perderla ni exponerla.",
                ul: [
                  "Cierra el cliente en scripts; en un servidor reutiliza el Pool.",
                  "Nunca interpoles valores en el SQL: usa parámetros.",
                  "Saca las credenciales a variables de entorno.",
                ],
                code: [
                  "const { Client } = require('pg');",
                  "",
                  "async function saldoDe(usuarioId) {",
                  "  const client = new Client({ connectionString: process.env.DATABASE_URL });",
                  "  await client.connect();",
                  "",
                  "  try {",
                  "    const { rows } = await client.query(",
                  "      'SELECT saldo FROM cuentas WHERE usuario_id = $1',",
                  "      [usuarioId]",
                  "    );",
                  "    return rows[0] ? rows[0].saldo : 0;",
                  "  } finally {",
                  "    await client.end();",
                  "  }",
                  "}",
                  "",
                  "saldoDe(7).then(console.log);",
                ],
                nota: "Abrir y cerrar conexión por petición es caro. Este patrón es didáctico; en servidores reales usarás un Pool (siguiente lección).",
                lang: "js",
              },
              expl: [
                "pg devuelve filas en el array rows de cada resultado.",
                "Los parámetros $1, $2 evitan la inyección SQL.",
                "El callback de errores en la promesa evita promesas sin catch.",
                "client.end() libera la conexión en scripts cortos.",
                "connectionString es el estándar de despliegues gestionados.",
              ],
              errors: [
                "Olvidar client.end() y dejar procesos colgados en scripts.",
                "Hardcodear la contraseña en el archivo versionado.",
                "Interpolar valores en el SQL (inyección a la vista).",
              ],
              tips: [
                "Usa connectionString y variables de entorno desde el inicio.",
                "Parse loss ids con Number antes de query.",
                "En un server, siempre Pool; ya verás por qué en la siguiente lección.",
              ],
              exercise: [
                "Conecta a una base local y muestra las tablas de tu esquema.",
                "Ejecuta una consulta parametrizada y observa rows.",
                "Mueve las credenciales a un archivo .env cargado en runtime.",
              ],
              summary: [
                "pg es el driver estándar de PostgreSQL para Node.",
                "client.query devuelve promesas con filas en rows.",
                "Las credenciales y la seguridad por parámetros mandan desde el día uno.",
              ],
              lang: "js",
              exampleFile: "pg-conexion",
              desc: "Conecta tu API a PostgreSQL con el cliente pg.",
            },
          },
          {
            id: "consultas-parametrizadas-y-seguras",
            title: "Consultas parametrizadas y seguras",
            duration: "12 min",
            level: "intermedio",
            category: "bases de datos",
            cardText: "Los $1, $2 de pg blindan el SQL frente a la inyección.",
            desc: "Ejecuta consultas seguras con parámetros posicionales en pg.",
            body: {
              intro: [
                "El atajo de pegar el valor del usuario dentro del SQL es la puerta que han usado décadas de ataques por inyección. Node no escapa de ese peligro: la defensa está en usar parámetros.",
                "pg soporta parámetros posicionales ($1, $2, ...) que el servidor de PostgreSQL une de forma segura, transportando los datos y no el código.",
              ],
              def: {
                par: "Una consulta parametrizada separa el texto SQL de sus valores: los marcadores $1, $2 se sustituyen en el servidor (PostgreSQL los trata como datos), eliminando la inyección por construcción.",
                porQue: "Con parámetros, un valor malicioso como '; DROP TABLE usuarios; -- no puede alterar la estructura del comando: es solo un dato.",
                cuando: "Usa parámetros en TOTA consulta que contenga datos del exterior: body, query params, path params o cualquier valor dinámico.",
              },
              concepts: [
                "query(text, valores) separa SQL de parámetros.",
                "$1, $2 son marcadores posicionales de PostgreSQL.",
                "pasar un array como parámetro sirve así para IN (…) en muchas versiones.",
                "Las tablas y columnas NO van como parámetro: se validan con lista blanca.",
                "Los parámetros preservan el tipo (number, string, boolean, null).",
              ],
              basic: {
                p: "Buscamos un usuario por id y email usando parámetros posicionales.",
                code: [
                  "const resultado = await pool.query(",
                  "  'SELECT id, email FROM usuarios WHERE id = $1 AND email = $2',",
                  "  [id, email],",
                  ");",
                  "",
                  "console.log(resultado.rowCount, resultado.rows);",
                ],
                out: "1 [ { id: 3, email: 'ana@mail.com' } ]",
                lang: "js",
              },
              intermediate: {
                p: "Los inserts parametrizados y el RETURNING nos devuelven la fila creada.",
                caption: "INSERT con RETURNING",
                code: [
                  "const { rows } = await pool.query(",
                  "  `INSERT INTO usuarios (email, hash) VALUES ($1, $2)",
                  "   RETURNING id, email`,",
                  "  [email, hash],",
                  ");",
                  "",
                  "console.log('Creado:', rows[0]);",
                ],
                lang: "js",
              },
              real: {
                p: "Casos reales: búsquedas con filtros dinámicos y la lista blanca para columnas.",
                ul: [
                  "Construye el WHERE por partes añadiendo condiciones con parámetros.",
                  "Valida y mapea las columnas ordenables a una lista fija.",
                  "Usa ORDER BY con identificadores ya conocidos, nunca con entrada cruda.",
                ],
                code: [
                  "function buscar({ email, activo, columnaOrden = 'id', limite = 50 }) {",
                  "  const condiciones = [];",
                  "  const valores = [];",
                  "",
                  "  if (email) {",
                  "    valores.push(email);",
                  "    condiciones.push(`email = $${valores.length}`);",
                  "  }",
                  "",
                  "  if (activo !== undefined) {",
                  "    valores.push(activo);",
                  "    condiciones.push(`activo = $${valores.length}`);",
                  "  }",
                  "",
                  "  // Lista blanca: solo estas columnas son ordenables",
                  "  const ordenables = ['id', 'email', 'creado_en'];",
                  "  const orden = ordenables.includes(columnaOrden) ? columnaOrden : 'id';",
                  "",
                  "  const where = condiciones.length ? `WHERE ${condiciones.join(' AND ')}` : '';",
                  "  return pool.query(",
                  "    `SELECT id, email FROM usuarios ${where} ORDER BY ${orden} LIMIT ${limite}`,",
                  "    valores,",
                  "  );",
                  "}",
                ],
                nota: "Los límites y el orden se interpolan SOLO tras sanitizarlos (parseo a número y lista blanca). Los valores viajan por parámetros.",
                lang: "js",
              },
              expl: [
                "PostgreSQL prepara el plan con marcadores y sustituye valores después.",
                "Un parámetro nunca se interpreta como código SQL.",
                "El motor conserva los tipos: comparaciones felices y sin '1' == 1.",
                "La lista blanca para columnas cierra el único hueco restante.",
                "Los counts y límites se parsean a número antes de interpolar.",
              ],
              errors: [
                "Pegar el valor con template literals directamente en SQL.",
                "Poner nombre de tabla o columna como parámetro ($1 no vale ahí).",
                "Olvidar comprobar resultado.rowCount para saber si encontró algo.",
              ],
              tips: [
                "Sanea filtros con una lista blanca por cada columna permitida.",
                "Revisa los logs: una excepción de tipos delata un parámetro mal tipado.",
                "Haz counts con RETURNING o rowCount en vez de suponer.",
              ],
              exercise: [
                "Reescribe una consulta interpolada en una parametrizada.",
                "Construye un filtro dinámico por email y activo con params.",
                "Añade una lista blanca de columnas ordenables en tu endpoint.",
              ],
              summary: [
                "$1, $2 transportan datos seguros al servidor y eliminan la inyección.",
                "Las columnas se mapean con listas blancas, no con parámetros.",
                "Los límites y conteos se parsean antes de interpolar.",
              ],
              lang: "js",
              exampleFile: "pg-parametrizadas",
              desc: "Ejecuta consultas seguras con parámetros posicionales en pg.",
            },
          },
          {
            id: "pool-de-conexiones-y-reutilizacion",
            title: "Pool de conexiones y reutilización",
            duration: "10 min",
            level: "intermedio",
            category: "bases de datos",
            cardText: "Un Pool reutiliza conexiones y aguanta cientos de peticiones.",
            desc: "Reutiliza conexiones con el Pool de pg para escalar tu API.",
            body: {
              intro: [
                "Abrir una conexión TCP a PostgreSQL cuesta tiempo y recursos. Si tu API abre una por petición, un par de decenas de peticiones simultáneas la hunden.",
                "El Pool de pg mantiene un lote de conexiones abiertas y reutilizables: cada petición pide una, la usa y la devuelve, sin el coste de abrir ni cerrar.",
              ],
              def: {
                par: "new Pool() crea una colección de conexiones reutilizables. Cada query del pool toma una conexión libre, la ejecuta y la libera, compartiendo el mismo conjunto de conexiones entre muchas peticiones.",
                porQue: "Elimina el overhead de conectar en cada operación y da control (límites, tiempos de espera, errores de saturación) que un Client a secas no ofrece.",
                cuando: "Usa Pool para cualquier servidor que atienda más de una petición. Es la elección por defecto en APIs Express en producción.",
              },
              concepts: [
                "const pool = new Pool({...}) crea el conjunto compartido.",
                "pool.query() toma y libera una conexión automáticamente.",
                "max y min controlan cuántas conexiones mantener.",
                "connectionTimeoutMillis e idleTimeoutMillis gestionan esperas.",
                "El pool emite error si una conexión enferma.",
              ],
              basic: {
                p: "Sustituye el Client por un Pool que las rutas reutilizan.",
                code: [
                  "const { Pool } = require('pg');",
                  "",
                  "const pool = new Pool({",
                  "  connectionString: process.env.DATABASE_URL,",
                  "  max: 10,",
                  "  idleTimeoutMillis: 30000,",
                  "});",
                  "",
                  "async function buscar(id) {",
                  "  const { rows } = await pool.query(",
                  "    'SELECT id, email FROM usuarios WHERE id = $1',",
                  "    [id],",
                  "  );",
                  "  return rows[0] || null;",
                  "}",
                  "",
                  "module.exports = { pool, buscar };",
                ],
                out: "Un módulo de persistencia que toda la API puede importar.",
                lang: "js",
              },
              intermediate: {
                p: "Una conexión dedicada del pool permite abrir transacciones reales.",
                caption: "Transacciones con una conexión del pool",
                code: [
                  "const client = await pool.connect();",
                  "",
                  "try {",
                  "  await client.query('BEGIN');",
                  "  await client.query(",
                  "    'UPDATE cuentas SET saldo = saldo - $1 WHERE id = $2',",
                  "    [100, 1],",
                  "  );",
                  "  await client.query(",
                  "    'UPDATE cuentas SET saldo = saldo + $1 WHERE id = $2',",
                  "    [100, 2],",
                  "  );",
                  "  await client.query('COMMIT');",
                  "} catch (err) {",
                  "  await client.query('ROLLBACK');",
                  "  throw err;",
                  "} finally {",
                  "  client.release();",
                  "}",
                ],
                lang: "js",
              },
              real: {
                p: "En producción, el tamaño del pool se ajusta a la capacidad de PostgreSQL: demasiadas conexiones saturan el servidor; demasiado pocas encolan.",
                ul: [
                  "Apunta a 1-10 conexiones por instancia de tu API.",
                  "Monitorea el número de conexiones activas del servidor.",
                  "Usa client.release() en finally: si nunca sueltas, agotas el pool.",
                ],
                code: [
                  "const { Pool } = require('pg');",
                  "",
                  "const pool = new Pool({",
                  "  connectionString: process.env.DATABASE_URL,",
                  "  max: process.env.PG_POOL_MAX ? Number(process.env.PG_POOL_MAX) : 10,",
                  "  connectionTimeoutMillis: 2000,",
                  "});",
                  "",
                  "pool.on('error', (err) => {",
                  "  console.error('Conexion del pool caida:', err.message);",
                  "});",
                  "",
                  "// Uso en una ruta Express",
                  "app.get('/cuentas/:id/saldo', async (req, res) => {",
                  "  const { rows } = await pool.query(",
                  "    'SELECT saldo FROM cuentas WHERE id = $1',",
                  "    [req.params.id],",
                  "  );",
                  "  res.json(rows[0] || { saldo: 0 });",
                  "});",
                ],
                nota: "Las transacciones deben tener una conexión dedicada del pool; por eso existe pool.connect(). Un pool.query en medio de una transacción usaría otra conexión.",
                lang: "js",
              },
              expl: [
                "El Pool amortiza el coste de conexión reutilizando sockets abiertos.",
                "max limita la concurrencia real hacia PostgreSQL.",
                "Cuando todas las conexiones están ocupadas, el query espera.",
                "connectionTimeoutMillis evita esperas infinitas de saturación.",
                "Liberar la conexión (release/finally) es tu responsabilidad máxima.",
              ],
              errors: [
                "Olvidar client.release() en transacciones y agotar el pool.",
                "Conectar de nuevo por petición en vez de reutilizar el pool.",
                "Poner max demasiado alto y saturar la base de datos.",
              ],
              tips: [
                "Crea el pool una vez en un módulo y compártelo por import.",
                "Ajusta max según la memoria y conexiones de PostgreSQL.",
                "Escucha el evento error del pool para no perder conexiones caídas.",
              ],
              exercise: [
                "Refactoriza tus rutas para usar un Pool compartido.",
                "Envuelve una operación de dos pasos en una transacción del pool.",
                "Fija max y connectionTimeoutMillis y carga urls bajo soporte con siege.",
              ],
              summary: [
                "El Pool reutiliza conexiones y aguanta peticiones concurrentes.",
                "Las transacciones usan pool.connect() con release en finally.",
                "max ajustado y monitoreo previenen la saturación de PostgreSQL.",
              ],
              lang: "js",
              exampleFile: "pool-conexiones",
              desc: "Reutiliza conexiones con el Pool de pg para escalar tu API.",
            },
          },
          {
            id: "migraciones-y-seeds",
            title: "Migraciones y seeds",
            duration: "12 min",
            level: "intermedio",
            category: "bases de datos",
            cardText: "El esquema evoluciona en scripts versionados, nunca a mano.",
            desc: "Aplica migraciones y sembrados de datos en tu base PostgreSQL.",
            body: {
              intro: [
                "Cambiar la base de datos a mano —un ALTER aquí, un INSERT allá— termina con tablas distintas en dev, staging y producción. Las migraciones versionan esos cambios como código.",
                "Una migración es un script SQL que evoluciona el esquema; los seeds siembran datos iniciales. Ambos se ejecutan en orden y se reproducen en cualquier entorno.",
              ],
              def: {
                par: "Una migración describe un cambio del esquema (crear tabla, añadir columna) con ida y vuelta (migrate/rollback). Los seeds insertan datos de ejemplo o maestranza. pg-migrate y Node-pg-migrate lo automatizan.",
                porQue: "El esquema deja de ser un secreto tribal: cada cambio queda registrado, revisable y aplicable en orden contra cualquier base.",
                cuando: "Cree migraciones para cada cambio de esquema desde el primer día. Seeds para datos de arranque, maestros y desarrollo.",
              },
              concepts: [
                "Cada migración tiene un up (aplicar) y un down (deshacer).",
                "pg-migrate registra las aplicadas en una tabla de migraciones.",
                "migrate up aplica las pendientes en orden; migrate down revierte.",
                "Los seeds son scripts idempotentes o con TRUNCATE previo.",
                "En CI/CD, las migraciones se aplican antes de arrancar la API.",
              ],
              basic: {
                p: "Una migración con Node-pg-migrate que crea la tabla usuarios.",
                code: [
                  "exports.up = (pgm) => {",
                  "  pgm.createTable('usuarios', {",
                  "    id: 'serial',",
                  "    email: { type: 'varchar(255)', notNull: true, unique: true },",
                  "    hash: { type: 'varchar(255)', notNull: true },",
                  "    creado_en: { type: 'timestamptz', notNull: true, default: pgm.func('now()') },",
                  "  });",
                  "};",
                  "",
                  "exports.down = (pgm) => {",
                  "  pgm.dropTable('usuarios');",
                  "};",
                ],
                out: "Tras migrate up existe la tabla usuarios; migrate down la elimina.",
                lang: "js",
              },
              intermediate: {
                p: "Añadir una columna con datos por defecto y revertir después.",
                caption: "Evolución del esquema con up/down",
                code: [
                  "exports.up = (pgm) => {",
                  "  pgm.addColumn('usuarios', {",
                  "    rol: { type: 'varchar(20)', notNull: true, default: 'cliente' },",
                  "  });",
                  "};",
                  "",
                  "exports.down = (pgm) => {",
                  "  pgm.dropColumn('usuarios', 'rol');",
                  "};",
                ],
                lang: "js",
              },
              real: {
                p: "Las migraciones forman parte del deploy: en la maquinaria de CI se aplican en orden y los seeds se ejecutan solo en entornos que los necesitan.",
                ul: [
                  "Nunca revises contra producción a mano: migra por código.",
                  "Haz migraciones pequeñas y atómicas por cambio.",
                  "Seeds idempotentes: correrlos dos veces no duplica datos.",
                ],
                code: [
                  "# Flujo de despliegue",
                  "npm run migrate up",
                  "node src/scripts/seeds.js --env=production",
                  "",
                  "// scripts/seeds.js (idempotente)",
                  "const { pool } = require('../db');",
                  "",
                  "async function sembrar() {",
                  "  await pool.query(",
                  "    `INSERT INTO roles (nombre)",
                  "     VALUES ('admin') ",
                  "     ON CONFLICT (nombre) DO NOTHING`",
                  "  );",
                  "  console.log('Roles garantizados');",
                  "  await pool.end();",
                  "}",
                ],
                nota: "Nunca manipules el esquema desde la aplicación en runtime: las migraciones se aplican en el deploy, antes de pasar tráfico.",
                lang: "js",
              },
              expl: [
                "Las migraciones se registran en una tabla interna: aplicarlas dos veces machaca.",
                "up y down permiten volver atrás de forma controlada.",
                "Los cambios de esquema se revisan en PR como cualquier código.",
                "Los seeds con ON CONFLICT DO NOTHING son seguros de repetir.",
                "El orden de las migraciones en CI elimina las sorpresas de esquema.",
              ],
              errors: [
                "Editar una migración ya aplicada en vez de crear una nueva.",
                "Olvidar el down y no poder revertir.",
                "Aplicar migraciones a mano en producción y desincronizar entornos.",
              ],
              tips: [
                "Una migración, un cambio: fácil de revertir y de revisar.",
                "Prueba migrate up/down localmente antes de subir.",
                "Mantén seeds separados de migraciones para no mezclar esquema y datos.",
              ],
              exercise: [
                "Crea tu primera migración con tabla usuarios y aplícala.",
                "Añade una columna y revertirla con migrate down.",
                "Escribe un seed idempotente de roles y córrelo dos veces.",
              ],
              summary: [
                "Las migraciones versionan los cambios de esquema con up/down.",
                "Los seeds siembran datos iniciales de forma repetible.",
                "Migrar en el deploy mantiene todos los entornos alineados.",
              ],
              lang: "js",
              exampleFile: "migraciones-seeds",
              desc: "Aplica migraciones y sembrados de datos en tu base PostgreSQL.",
            },
          },
          {
            id: "modelando-datos-con-prisma",
            title: "Modelando datos con Prisma",
            duration: "12 min",
            level: "intermedio",
            category: "bases de datos",
            cardText: "Prisma declara el esquema en código y genera un cliente tipado.",
            desc: "Modela tu base de datos con Prisma ORM y su cliente tipado.",
            body: {
              intro: [
                "Escribir SQL a mano para cada consulta funciona, pero cuando el dominio crece surge el ORM: una capa que modela las tablas como objetos y las consultas como métodos con IntelliSense.",
                "Prisma destaca por su esquema declarativo (model), migraciones generadas y un cliente con tipos que convierten errores de columna en fallos de compilación.",
              ],
              def: {
                par: "Prisma define el esquema en schema.prisma (conectores, datasource y models), genera migraciones SQL y un Cliente (PrismaClient) con métodos tipados como prisma.usuario.findUnique().",
                porQue: "El esquema vive como código revisable, las relaciones se declaran (no se unen a mano) y el cliente generado evita errores de campo y tipa las respuestas.",
                cuando: "Usa Prisma en proyectos relacionales con PostgreSQL/MySQL/SQLite cuando la velocidad de desarrollo y el tipado importen más que el control manual del SQL.",
              },
              concepts: [
                "prisma/schema.prisma declara datasource, generador y models.",
                "Cada model mapea a una tabla y cada campo a una columna.",
                "@id, @unique y @default definen claves y valores por defecto.",
                "Las relaciones (usuario.pedidos) se declaran y se consultan con include.",
                "npx prisma migrate dev genera y aplica migraciones tipadas.",
              ],
              basic: {
                p: "Un schema.prisma con usuario y los pasos para llevarlo a la base.",
                code: [
                  "datasource db {",
                  "  provider = \"postgresql\"",
                  "  url      = env(\"DATABASE_URL\")",
                  "}",
                  "",
                  "generator client {",
                  "  provider = \"prisma-client-js\"",
                  "}",
                  "",
                  "model Usuario {",
                  "  id        Int      @id @default(autoincrement())",
                  "  email     String   @unique",
                  "  hash      String",
                  "  activo    Boolean  @default(true)",
                  "  creadoEn  DateTime @default(now())",
                  "  pedidos   Pedido[]",
                  "}",
                ],
                out: "Con npx prisma migrate dev la tabla usuarios queda creada con sus constraints.",
                lang: "prisma",
              },
              intermediate: {
                p: "El cliente tipado consulta usuarios, trae pedidos relacionados y respeta el tipo.",
                caption: "Consultas con relaciones tipadas",
                code: [
                  "const { PrismaClient } = require('@prisma/client');",
                  "const prisma = new PrismaClient();",
                  "",
                  "async function detalle(id) {",
                  "  return prisma.usuario.findUnique({",
                  "    where: { id: Number(id) },",
                  "    select: {",
                  "      id: true,",
                  "      email: true,",
                  "      pedidos: { select: { id: true, total: true } },",
                  "    },",
                  "  });",
                  "}",
                ],
                lang: "js",
              },
              real: {
                p: "En una API, el repositorio se vuelve casi decorativo: el cliente de Prisma ya desespacia SQL y datos, y las transacciones se manejan con $transaction.",
                ul: [
                  "Usa select para devolver solo lo que el cliente necesita.",
                  "Las transacciones con $transaction mantienen la atomicidad.",
                  "Cierra el cliente al apagar la app (prisma.$disconnect).",
                ],
                code: [
                  "async function crearPedido(usuarioId, lineas) {",
                  "  return prisma.$transaction(async (tx) => {",
                  "    const total = lineas.reduce((s, l) => s + l.subtotal, 0);",
                  "",
                  "    const pedido = await tx.pedido.create({",
                  "      data: { clienteId: usuarioId, total },",
                  "    });",
                  "",
                  "    for (const linea of lineas) {",
                  "      await tx.lineaPedido.create({",
                  "        data: { ...linea, pedidoId: pedido.id },",
                  "      });",
                  "    }",
                  "",
                  "    return pedido;",
                  "  });",
                  "}",
                ],
                nota: "Prisma genera el cliente cada vez que cambias el esquema (prisma generate). Los tipos de tus consultas reflejan el modelo exacto.",
                lang: "js",
              },
              expl: [
                "El esquema declarativo es una sola fuente de verdad para tablas y relaciones.",
                "Las migraciones de Prisma se revisan y aplican como cualquier cambio.",
                "El cliente tipado convierte errores de campo en fallos en desarrollo.",
                "Las relaciones evitan los JOIN escritos a mano y tipan las filas.",
                "$transaction conserva la atomicidad multi-tabla.",
              ],
              errors: [
                "Editar la base a mano y no tocar el schema: desincronización total.",
                "Olvidar prisma generate tras el schema (client desactualizado).",
                "Seleccionar todo un model y exponer el hash de contraseña.",
              ],
              tips: [
                "Mantén el hash fuera de las respuestas: usa select explícito.",
                "Aplica prisma migrate dev al cambiar el esquema.",
                "Define relaciones con onDelete para controlar los borrados.",
              ],
              exercise: [
                "Modela usuario y pedido con una relación one-to-many y migra.",
                "Consulta con include las relaciones y con select los campos mínimos.",
                "Escribe una transacción $transaction que cree pedido y líneas.",
              ],
              summary: [
                "Prisma declara el esquema y genera un cliente tipado.",
                "Las relaciones se declaran y se consultan sin JOIN a mano.",
                "$transaction mantiene la atomicidad en operaciones múltiples.",
              ],
              lang: "prisma",
              exampleFile: "prisma-modelo",
              desc: "Modela tu base de datos con Prisma ORM y su cliente tipado.",
            },
          },
        ],
      },
      {
        id: "modulo-06",
        n: 6,
        t: "APIs REST en Node",
        level: "intermedio",
        caption: "Diseña endpoints REST robustos y bien documentados en Express.",
        desc: "Define contratos REST claros: diseño de recursos, validación con Zod, respuestas JSON consistentes, paginación y filtros, y documentación con OpenAPI.",
        subtitle: "Endpoints consistentes que cualquier cliente puede consumir.",
        objectives: [
          "Diseñar endpoints REST coherentes",
          "Validar la entrada con Zod",
          "Devolver respuestas JSON consistentes",
          "Implementar paginación y filtros",
          "Documentar con OpenAPI",
        ],
        lessons: [
          {
            id: "disenando-endpoints-rest",
            title: "Diseñando endpoints REST",
            duration: "10 min",
            level: "intermedio",
            category: "apis",
            cardText: "Recursos en plural, verbos correctos y jerarquías claras.",
            desc: "Diseña endpoints REST con recursos, verbos y jerarquías correctos.",
            body: {
              intro: [
                "Una API no se diseña en el teclado: se diseña con reglas. El estilo REST hace una API predecible cuando cada recurso responde a un patrón claro de recursos y verbos.",
                "Las mejores APIs se leen como inglés: los recursos son sustantivos en plural y los verbos HTTP dicen qué hacer con ellos.",
              ],
              def: {
                par: "REST organiza la API por recursos (sustantivos en plural, /pedidos) y usa los verbos HTTP (GET, POST, PUT, PATCH, DELETE) para las operaciones: leer, crear, sustituir, actualizar o borrar.",
                porQue: "Un contrato coherente permite a cualquier cliente deducir el comportamiento sin documentación exhaustiva y facilita el mantenimiento del servidor.",
                cuando: "Aplica este diseño a toda API HTTP nueva; las excepciones (acciones, RPC) se modelan como sub-recursos o con verbos propios y se documentan.",
              },
              concepts: [
                "Recursos en plural: /pedidos, /clientes.",
                "GET /recursos lista; GET /recursos/:id lee uno.",
                "POST /crea; PUT /sustituye; PATCH /actualiza; DELETE /borra.",
                "Las jerarquías: /pedidos/:id/lineas expresan pertenencia.",
                "Los verbos de acción se evitan salvo casos justificados.",
              ],
              basic: {
                p: "El CRUD completo de un recurso con Express, en su Router de pedidos.",
                code: [
                  "router.get('/', listar);",
                  "router.post('/', crear);",
                  "router.get('/:id', detalle);",
                  "router.put('/:id', sustituir);",
                  "router.patch('/:id', actualizar);",
                  "router.delete('/:id', borrar);",
                  "",
                  "router.get('/:id/lineas', listarLineas);",
                ],
                out: "6 verbos sobre el recurso + una jerarquía: la API se deduce sin documentación.",
                lang: "js",
              },
              intermediate: {
                p: "Cada endpoint devuelve el código de estado correcto para cada operación.",
                caption: "Códigos de estado por operación",
                code: [
                  "router.post('/', asyncHandler(async (req, res) => {",
                  "  const pedido = await pedidoServicio.crear(req.body);",
                  "  res.status(201).json(pedido);",
                  "}));",
                  "",
                  "router.delete('/:id', asyncHandler(async (req, res) => {",
                  "  await pedidoServicio.borrar(req.params.id);",
                  "  res.status(204).send();",
                  "}));",
                ],
                lang: "js",
              },
              real: {
                p: "La jerarquía y la semántica bien empleadas hacen que un cliente sepa reaccionar sin leer el código de la API.",
                ul: [
                  "Nombra los recursos en plural y con foco en el dominio.",
                  "Usa 201 al crear, 204 al borrar, 404 si falta, 409 si hay conflicto.",
                  "Las acciones de negocio se modelan como sub-recursos (/pagar).",
                ],
                code: [
                  "// Recurso anidado con intención",
                  "router.post('/:id/pagar', asyncHandler(async (req, res) => {",
                  "  const factura = await pedidoServicio.pagar(req.params.id, req.body);",
                  "  res.status(200).json(factura);",
                  "}));",
                  "",
                  "// GET /pedidos/42          -> detalle",
                  "// POST /pedidos/42/pagar   -> pagar ese pedido",
                  "// GET /pedidos/42/lineas   -> lineas del pedido",
                ],
                nota: "Si una acción no encaja en la semántica de recursos, pregúntate antes si estás modelando bien el dominio.",
                lang: "js",
              },
              expl: [
                "Los sustantivos en plural describen colecciones de recursos.",
                "Los verbos HTTP codifican la intención sin ambigüedad.",
                "Las jerarquías expresan relaciones y no requieren query params extraños.",
                "Los códigos de estado completan la semántica de cada respuesta.",
                "La consistencia es más importante que la perfección teórica.",
              ],
              errors: [
                "Usar /obtenerPedidos o /listarPedidos: los verbos van en HTTP.",
                "Mezclar plural/singular entre recursos del mismo dominio.",
                "Devolver 200 en creaciones en vez de 201.",
              ],
              tips: [
                "Define la jerarquía pensando en cómo navegan los clientes.",
                "Mantén una tabla de verbos y códigos visible en el repo.",
                "Consistencia > opinión: elige un estilo y aplica.",
              ],
              exercise: [
                "Diseña el CRUD de un recurso nuevos (productos) con sus códigos.",
                "Modela una jerarquía pedido->lineas con sus endpoints.",
                "Revisa tu API y corrige verbos en URL o recursos en singular.",
              ],
              summary: [
                "REST usa recursos en plural y verbos HTTP para la intención.",
                "Los códigos de estado completan la semántica de cada operación.",
                "Las jerarquías de recursos expresan relaciones del dominio.",
              ],
              lang: "js",
              exampleFile: "diseno-endpoints",
              desc: "Diseña endpoints REST con recursos, verbos y jerarquías correctos.",
            },
          },
          {
            id: "validacion-de-entrada-con-zod",
            title: "Validación de entrada con Zod",
            duration: "11 min",
            level: "intermedio",
            category: "apis",
            cardText: "Zod define y valida el esquema de cada cuerpo en TypeScript o JS.",
            desc: "Valida los cuerpos de tus peticiones con Zod y errores claros.",
            body: {
              intro: [
                "El cuerpo de una petición es entrada del usuario: puede venir incompleto, mal tipado o con tipos confusos. Validarlo con erres manuales llena el controlador de if.",
                "Zod declara el esquema esperado como objetos (z.object) y valida de una vez: el resultado o una lista de errores, sin if interminables.",
              ],
              def: {
                par: "Zod es una librería de validación y parseo de esquemas: defines reglas con z.string(), z.number(), z.object(...) y parseas datos, obteniendo el valor limpio o un ZodError detallado.",
                porQue: "Centraliza las reglas en esquemas reutilizables, tipa las salidas y reduce los validadores caseros a un wrapper.",
                cuando: "Úsala en la frontera de tu API (antes de llegue al servicio) para cuerpos, query params y path params.",
              },
              concepts: [
                "npm install zod añade la librería.",
                "z.object({...}) declara la forma de un cuerpo JSON.",
                ".parse() valida y lanza; .safeParse() devuelve un resultado.",
                "Los errores de Zod traen ruta y mensaje por campo.",
                "Los esquemas también tipan: infer dar tipos automáticamente.",
              ],
              basic: {
                p: "Un middleware que valida el cuerpo de una petición con Zod.",
                code: [
                  "const { z } = require('zod');",
                  "",
                  "const crearPedidoSchema = z.object({",
                  "  clienteId: z.number().int().positive(),",
                  "  total: z.number().positive(),",
                  "  estado: z.enum(['pendiente', 'pagado']).default('pendiente'),",
                  "});",
                  "",
                  "app.post('/pedidos', (req, res) => {",
                  "  const resultado = crearPedidoSchema.safeParse(req.body);",
                  "",
                  "  if (!resultado.success) {",
                  "    return res.status(422).json(",
                  "      resultado.error.errors.map(e => ({ camino: e.path, mensaje: e.message })),",
                  "    );",
                  "  }",
                  "",
                  "  res.status(201).json(resultado.data);",
                  "});",
                ],
                out: "Con body inválido responde 422 con cada campo y su error; con body válido, 201.",
                lang: "js",
              },
              intermediate: {
                p: "Un middleware reutilizable que valida y pega el resultado en req.",
                caption: "Middleware de validación reutilizable",
                code: [
                  "const { z } = require('zod');",
                  "",
                  "const validar = (schema) => (req, res, next) => {",
                  "  const resultado = schema.safeParse(req.body);",
                  "",
                  "  if (!resultado.success) {",
                  "    return res.status(422).json({",
                  "      error: 'peticion invalida',",
                  "      campos: resultado.error.issues,",
                  "    });",
                  "  }",
                  "",
                  "  req.datos = resultado.data;",
                  "  next();",
                  "};",
                  "",
                  "router.post('/pedidos', validar(crearPedidoSchema), controladores.crear);",
                ],
                lang: "js",
              },
              real: {
                p: "Un validador de body y otro de query params protegen los endpoints sin tocar la lógica de los controladores.",
                ul: [
                  "Valida también query params en listados (página, límite, estado).",
                  "Mantén los esquemas junto a su endpoint o en su carpeta de contrato.",
                  "Los errores uniformes (422 con campos) simplifican al cliente.",
                ],
                code: [
                  "const listarSchema = z.object({",
                  "  pagina: z.coerce.number().int().positive().default(1),",
                  "  limite: z.coerce.number().int().min(1).max(100).default(20),",
                  "  estado: z.enum(['pendiente', 'pagado', 'cancelado']).optional(),",
                  "});",
                  "",
                  "router.get('/pedidos', validarQuery(listarSchema), asyncHandler(async (req, res) => {",
                  "  // req.filtros ya tipado y validado",
                  "  const listado = await pedidoServicio.listar(req.filtros);",
                  "  res.json(listado);",
                  "}));",
                ],
                nota: "z.coerce.number() convierte '2' en 2 antes de validar: perfecto para query strings.",
                lang: "js",
              },
              expl: [
                "El esquema declara qué esperas: tipos, rangos, enumerados.",
                "safeParse devuelve éxito o errores sin lanzar excepciones.",
                "El middleware valida en la frontera y deja req.datos limpio.",
                "Los errores de Zod incluyen la ruta del campo incorrecto.",
                "Librerías como express-zod-validator ya integran el patrón.",
              ],
              errors: [
                "Validar a medias y dejar pasar valores undefined.",
                "Responder 500 al fallo de validación en vez de 422.",
                "Enviar errores de Zod crudos con detalles internos del esquema.",
              ],
              tips: [
                "Siempre safeParse en producción (captura el ZodError tú mismo).",
                "Centraliza el middleware y el formato de error una sola vez.",
                "Documenta los esquemas: son parte del contrato.",
              ],
              exercise: [
                "Define el esquema de crear usuario (email, password mínima, edad > 18).",
                "Crea un middleware validar usado en tres rutas distintas.",
                "Valida query params de paginación con z.coerce.",
              ],
              summary: [
                "Zod declara esquemas y valida cuerpos de una vez.",
                "El middleware validar deja req.datos limpio y tipado.",
                "Los errores uniformes en 422 simplifican el cliente.",
              ],
              lang: "js",
              exampleFile: "zod-validacion",
              desc: "Valida los cuerpos de tus peticiones con Zod y errores claros.",
            },
          },
          {
            id: "respuestas-json-consistentes",
            title: "Respuestas JSON consistentes",
            duration: "9 min",
            level: "intermedio",
            category: "apis",
            cardText: "Una envoltura común para datos y errores hace la API predecible.",
            desc: "Estandariza el formato de tus respuestas JSON de éxito y error.",
            body: {
              intro: [
                "El éxito y el error de tu API se leen igual: JSON con un formato estable. Cuando un error parece un éxito y un listado devuelve la mano a veces, el cliente sufre.",
                "Una envoltura común (wrapper) define qué devolver siempre: datos, metadatos y errores con la misma estructura.",
              ],
              def: {
                par: "Una respuesta JSON consistente envuelve el contenido: { exito: true, datos: … } para líneas bien, y { exito: false, error: { codigo, mensaje } } para fallos, con el mismo Content-Type y códigos correctos.",
                porQue: "Un contrato de respuesta uniforme facilita el tipado del cliente, el manejo central de errores y la evolución de la API sin sorpresas.",
                cuando: "Úsalo desde el primer endpoint. La consistencia se paga cara de corregir más tarde.",
              },
              concepts: [
                "Envoltorio de datos: { exito: true, datos }.",
                "Envoltorio de error: { exito: false, error: { codigo, mensaje } }.",
                "El Content-Type: application/json se fija una vez.",
                "Los códigos HTTP transportan el estado global.",
                "Los listados añaden metadatos de paginación (total, página).",
              ],
              basic: {
                p: "Dos helpers que uniforman éxitos y errores en toda la API.",
                code: [
                  "const exito = (res, datos, codigo = 200) =>",
                  "  res.status(codigo).json({ exito: true, datos });",
                  "",
                  "const error = (res, mensaje, codigo = 500, detalle) =>",
                  "  res.status(codigo).json({",
                  "    exito: false,",
                  "    error: { codigo, mensaje },",
                  "    ...(detalle ? { detalle } : {}),",
                  "  });",
                  "",
                  "// Uso",
                  "exito(res, { pedidos: [] }, 200);",
                  "error(res, 'pedido no encontrado', 404);",
                ],
                out: "{\"exito\":true,\"datos\":{…}} y {\"exito\":false,\"error\":{…}}",
                lang: "js",
              },
              intermediate: {
                p: "Los listados incluyen metadatos de paginación en la misma envoltura.",
                caption: "Envoltura con metadatos de paginación",
                code: [
                  "router.get('/pedidos', asyncHandler(async (req, res) => {",
                  "  const { filas, total } = await pedidoServicio.listar(req.filtros);",
                  "",
                  "  exito(res, {",
                  "    pedidos: filas,",
                  "    paginacion: {",
                  "      pagina: req.filtros.pagina,",
                  "      limite: req.filtros.limite,",
                  "      total,",
                  "    },",
                  "  });",
                  "}));",
                ],
                lang: "js",
              },
              real: {
                p: "El formato común conviierte a las herramientas del cliente (axios, tipos) predecibles, y los errores de validación incluyen detalle con los campos.",
                ul: [
                  "Define el contrato en un README o en la spec OpenAPI.",
                  "Los errores 4xx detallan el campo; los 5xx ocultan internos.",
                  "Los códigos HTTP y la envoltura se complementan, no se contradicen.",
                ],
                code: [
                  "// GET /pedidos/abc",
                  "{",
                  '  "exito": false,',
                  '  "error": {',
                  '    "codigo": 400,',
                  '    "mensaje": "id invalido"',
                  "  }",
                  "}",
                  "",
                  "// POST /pedidos (cuerpo invalido, via Zod)",
                  "{",
                  '  "exito": false,',
                  '  "error": {',
                  '    "codigo": 422,',
                  '    "mensaje": "peticion invalida"',
                  "  },",
                  '  "detalle": { "clienteId": "obligatorio" }',
                  "}",
                ],
                nota: "Elige un esquema y adjúntalo a tu spec OpenAPI: el cliente copia y pega el tipo, y la API evoluciona con menos fricción.",
                lang: "json",
              },
              expl: [
                "La envoltura unifica datos y errores en el mismo molde.",
                "Los códigos HTTP y el cuerpo se complementan sin contradecirse.",
                "Los metadatos (paginación) viajan dentro de datos, no aparte.",
                "Los errores 4xx pueden detallar; los 5xx nunca filtran internos.",
                "El formato se define una vez y se aplica del helper central.",
              ],
              errors: [
                "Mezclar formatos: a veces {data}, a veces el objeto pelado.",
                "Devolver errores con stack o mensajes internos.",
                "Ignorar los códigos HTTP y fingir éxitos en el cuerpo.",
              ],
              tips: [
                "Crea los helpers exito/error y úsalos en toda la API desde el día uno.",
                "Documenta el envoltorio en OpenAPI como esquema común.",
                "Mantén 404/422/500 con el mismo molde de error.",
              ],
              exercise: [
                "Crea los helpers exito/error en un módulo utils.",
                "Aplica el envoltorio a un listado con paginación.",
                "Asegura que un 404 use el mismo formato que un 422.",
              ],
              summary: [
                "La envoltura JSON unifica éxitos y errores en un formato estable.",
                "Los metadatos de paginación viajan en datos.",
                "El formato común se define una vez y se documenta.",
              ],
              lang: "js",
              exampleFile: "respuestas-json",
              desc: "Estandariza el formato de tus respuestas JSON de éxito y error.",
            },
          },
          {
            id: "paginacion-y-filtros",
            title: "Paginación y filtros",
            duration: "11 min",
            level: "intermedio",
            category: "apis",
            cardText: "Listas enormes no se mandan enteras: página, límite y filtros.",
            desc: "Implementa paginación y filtros en los listados de tu API.",
            body: {
              intro: [
                "Cuando un listado crece a miles de filas, devolverlas todas entierra al servidor y al cliente. La paginación limita cada respuesta y los filtros dejan que el cliente afine.",
                "Query params estándar como page y limit controlan la ventana; los filtros por campos (estado, fecha, término) reducen el volumen antes de paginar.",
              ],
              def: {
                par: "La paginación por página corta los resultados en ventanas con page (página actual) y limit (tamaño). Los filtros se pasan por query params y se traducen a condiciones SQL con parámetros.",
                porQue: "Las respuestas se vuelven rápidas y pequeñas, el cliente navega por páginas conocidas y el servidor no paga el coste de leer y serializar toda la tabla.",
                cuando: "Paginación en todo listado con volumen potencial. Filtra en el servidor siempre que la lógica de negocio dependa de ello (no filtres en el cliente).",
              },
              concepts: [
                "page y limit viajan como query params.",
                "El límite se recorta a un máximo (p.ej. 100) para no abusar.",
                "offset = (page - 1) * limit lleva la ventana a la fila correcta.",
                "El total de filas se devuelve en los metadatos.",
                "Los filtros (estado, fecha, query) se añaden al WHERE con parámetros.",
              ],
              basic: {
                p: "Un listado paginado que respeta page/limit y devuelve el total.",
                code: [
                  "router.get('/pedidos', asyncHandler(async (req, res) => {",
                  "  const pagina = Math.max(1, Number(req.query.page) || 1);",
                  "  const limite = Math.min(100, Number(req.query.limit) || 20);",
                  "  const offset = (pagina - 1) * limite;",
                  "",
                  "  const { rows: filas } = await pool.query(",
                  "    'SELECT * FROM pedidos ORDER BY id DESC LIMIT $1 OFFSET $2',",
                  "    [limite, offset],",
                  "  );",
                  "",
                  "  const { rows: [c] } = await pool.query(",
                  "    'SELECT COUNT(*)::int AS total FROM pedidos'",
                  "  );",
                  "",
                  "  exito(res, { pedidos: filas, total: c.total, pagina, limite });",
                  "}));",
                ],
                out: "Limit 20, page 2 devuelve los pedidos 21..40 y el total en metadatos.",
                lang: "js",
              },
              intermediate: {
                p: "Los filtros por estado y término se integran al WHERE con parámetros.",
                caption: "Filtros dinámicos con parámetros",
                code: [
                  "router.get('/pedidos', asyncHandler(async (req, res) => {",
                  "  const { estado, termino } = req.filtros;",
                  "  const valores = [];",
                  "  const condiciones = [];",
                  "",
                  "  if (estado) {",
                  "    valores.push(estado);",
                  "    condiciones.push(`estado = $${valores.length}`);",
                  "  }",
                  "",
                  "  if (termino) {",
                  "    valores.push(`%${termino}%`);",
                  "    condiciones.push(`referencia ILIKE $${valores.length}`);",
                  "  }",
                  "",
                  "  const where = condiciones.length ? `WHERE ${condiciones.join(' AND ')}` : '';",
                  "  const { rows } = await pool.query(",
                  "    `SELECT * FROM pedidos ${where} ORDER BY id DESC LIMIT $${valores.length + 1} OFFSET $${valores.length + 2}`,",
                  "    [...valores, req.filtros.limite, req.filtros.offset],",
                  "  );",
                  "",
                  "  exito(res, { pedidos: rows });",
                  "}));",
                ],
                lang: "js",
              },
              real: {
                p: "El modelo cursor (page = id) evita saltos cuando entran filas nuevas entre páginas.",
                ul: [
                  "Filtra antes de paginar: cuenta la misma ventana.",
                  "Usa ORDER BY estable (id) para que la paginación sea fija.",
                  "Para feeds con volumen enorme, prefiere cursor sobre offset.",
                ],
                code: [
                  "// Paginacion por cursor: ?cursor=42&limit=20",
                  "router.get('/pedidos', asyncHandler(async (req, res) => {",
                  "  const cursor = Number(req.query.cursor) || 0;",
                  "  const limite = Math.min(100, Number(req.query.limit) || 20);",
                  "",
                  "  const { rows } = await pool.query(",
                  "    'SELECT * FROM pedidos WHERE id > $1 ORDER BY id LIMIT $2',",
                  "    [cursor, limite],",
                  "  );",
                  "",
                  "  const siguiente = rows.length ? rows[rows.length - 1].id : null;",
                  "  exito(res, { pedidos: rows, siguienteCursor: siguiente });",
                  "}));",
                ],
                nota: "El cursor elimina los saltos y los duplicados de la paginación por offset bajo escritura constante.",
                lang: "js",
              },
              expl: [
                "limit y page definen la ventana; offset la calcula.",
                "Los filtros se añaden al WHERE con parámetros y no cambian la paginación.",
                "El COUNT da el total para navegar la última página.",
                "Un ORDER BY estable garantiza que las ventanas sean coherentes.",
                "El cursor es la opción robusta para feeds de escritura continua.",
              ],
              errors: [
                "No limitar el valor máximo del límite (un limit=99999 vuelve al infierno).",
                "Paginación con ORDER BY no determinista que repite filas.",
                "Filtrar presente en SQL con valores interpolados sin sanitizar.",
              ],
              tips: [
                "Siempre devuelve total, pagina y limite en los metadatos.",
                "Valida y parsea los números (z.coerce) antes de usarlos.",
                "Indexa la columna del ORDER BY para que OFFSET no duela.",
              ],
              exercise: [
                "Añade limit máximo y paginación a un listado existente.",
                "Integra un filtro por estado con parámetros.",
                "Implementa paginación por cursor en una ruta de feed.",
              ],
              summary: [
                "page/limit recortan la ventana; offset la calcula.",
                "Los filtros viajan por query y entran con parámetros.",
                "El cursor estabiliza la paginación bajo escrituras.",
              ],
              lang: "js",
              exampleFile: "paginacion-filtros",
              desc: "Implementa paginación y filtros en los listados de tu API.",
            },
          },
          {
            id: "documentacion-con-openapi",
            title: "Documentación con OpenAPI y Swagger",
            duration: "11 min",
            level: "intermedio",
            category: "apis",
            cardText: "Describe tu API en OpenAPI y Swagger UI la explora al vuelo.",
            desc: "Documenta tu API con OpenAPI y explorala con Swagger UI.",
            body: {
              intro: [
                "Una API sin documentación es una caja negra: cada cliente adivina contratos y el soporte paga el precio. OpenAPI es el estándar para describir la API en un documento legible por máquinas y humanos.",
                "Con OpenAPI defines endpoints, parámetros y respuestas en un JSON/YAML, y Swagger UI lo convierte en una página interactiva para probar cada llamada.",
              ],
              def: {
                par: "OpenAPI (antes Swagger) es un estándar JSON/YAML que describe una API: rutas, métodos, parámetros, cuerpos y respuestas. Swagger UI renderiza esa spec y permite ejecutar peticiones desde el navegador.",
                porQue: 'La documentación viva se mantiene con el código, sirve para generar clientes y pruebas, y convierte el "cómo llamo a esto" en algo resuelto.',
                cuando: "Documenta desde el primer endpoint. Herramientas como swagger-ui-express exponen la spec en tu propio servidor.",
              },
              concepts: [
                "Un documento OpenAPI con paths, parameters y responses.",
                "Swagger UI muestra la spec interactiva en /api-docs.",
                "Los esquemas de cuerpo se reutilizan entre petición y respuesta.",
                "Los códigos de respuesta se documentan por endpoint.",
                "La spec se puede generar con herramientas (swagger-autogen, tsoa).",
              ],
              basic: {
                p: "Una spec OpenAPI mínima con un endpoint de saludo.",
                code: [
                  "const swaggerUi = require('swagger-ui-express');",
                  "const YAML = require('yamljs');",
                  "",
                  "const spec = {",
                  "  openapi: '3.0.0',",
                  "  info: { title: 'Tienda API', version: '1.0.0' },",
                  "  paths: {",
                  "    '/salud': {",
                  "      get: {",
                  "        summary: 'Estado del servicio',",
                  "        responses: {",
                  "          200: {",
                  "            description: 'OK',",
                  "            content: {",
                  "              'application/json': {",
                  "                schema: { type: 'object' },",
                  "              },",
                  "            },",
                  "          },",
                  "        },",
                  "      },",
                  "    },",
                  "  },",
                  "};",
                  "",
                  "app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(spec));",
                ],
                out: "GET /api-docs muestra la página interactiva con el endpoint /salud.",
                lang: "js",
              },
              intermediate: {
                p: "Documentar un CRUD completo con su esquema de pedido y sus códigos.",
                caption: "CRUD documentado en OpenAPI",
                code: [
                  "spec.paths['/pedidos'] = {",
                  "  get: {",
                  "    summary: 'Lista pedidos paginados',",
                  "    parameters: [",
                  "      { name: 'page', in: 'query', schema: { type: 'integer' } },",
                  "      { name: 'limit', in: 'query', schema: { type: 'integer' } },",
                  "    ],",
                  "    responses: { 200: { description: 'Listado' } },",
                  "  },",
                  "  post: {",
                  "    summary: 'Crea un pedido',",
                  "    requestBody: {",
                  "      content: { 'application/json': { schema: { $ref: '#/components/schemas/Pedido'} } },",
                  "    },",
                  "    responses: {",
                  "      201: { description: 'Creado' },",
                  "      422: { description: 'Cuerpo invalido' },",
                  "    },",
                  "  },",
                  "};",
                ],
                lang: "js",
              },
              real: {
                p: "La spec se coloca en un archivo versionado y se sirve en un middleware, de modo que la documentación viaja con el despliegue.",
                ul: [
                  "Mantén la spec en el repo y ciñela a la implementación.",
                  "Genera clientes tipados desde la spec (openapi-generator).",
                  "Protege /api-docs en producción si no quieres exponer el contrato.",
                ],
                code: [
                  "const YAML = require('yamljs');",
                  "const swaggerUi = require('swagger-ui-express');",
                  "",
                  "const spec = YAML.load('./openapi.yaml');",
                  "",
                  "if (process.env.NODE_ENV !== 'production') {",
                  "  app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(spec));",
                  "}",
                  "",
                  "// openapi.yaml",
                  "openapi: 3.0.0",
                  "info:",
                  "  title: Tienda API",
                  "  version: 1.0.0",
                  "paths:",
                  "  /pedidos:",
                  "    get:",
                  "      summary: Lista pedidos",
                  "      responses:",
                  "        '200':",
                  "          description: OK",
                ],
                nota: "Existen generadores automáticos (swagger-autogen, @tsoa/cli, tsoa) que derivan la spec de tu código, reduciendo la duplicación.",
                lang: "js",
              },
              expl: [
                "OpenAPI describe rutas, parámetros, cuerpos y respuestas en un doc.",
                "Swagger UI sirve la spec como página interactiva.",
                "Los $ref reutilizan esquemas sin repetirlos.",
                "La spec alimenta la generación de clientes y mocks.",
                "Mantener spec y código sincronizados es un contrato de equipo.",
              ],
              errors: [
                "Documentar y no mantener: la spec miente y confunde.",
                "Olvidar los códigos de error en un endpoint.",
                "Servir la spec solo desde memoria sin versionarla.",
              ],
              tips: [
                "Empieza documentando el CRUD con sus 4 verbos.",
                "Usa generación automática si el proyecto es grande.",
                "Revisa /api-docs en cada deploy de desarrollo.",
              ],
              exercise: [
                "Añade Swagger UI con una spec mínima a tu Express.",
                "Documenta el endpoint POST /pedidos con cuerpo y 422.",
                "Mueve la spec a un archivo openapi.yaml y cárgala.",
              ],
              summary: [
                "OpenAPI describe tu API en un estándar legible por máquinas.",
                "Swagger UI la convierte en una página interactiva.",
                "La spec versionada alimenta clientes, mocks y pruebas.",
              ],
              lang: "js",
              exampleFile: "openapi-express",
              desc: "Documenta tu API con OpenAPI y explorala con Swagger UI.",
            },
          },
        ],
      },
      {
        id: "modulo-07",
        n: 7,
        t: "Seguridad y despliegue",
        level: "intermedio",
        caption: "Envía tu API a producción protegida y monitorizada.",
        desc: "Endurece tu API con variables de entorno, Helmet y CORS, limita el abuso con rate limiting y despliega con PM2 detrás de Nginx.",
        subtitle: "Seguridad y operaciones para el Node en producción.",
        objectives: [
          "Gestionar secretos con variables de entorno",
          "Proteger la API con Helmet y CORS",
          "Limitar el uso con rate limiting",
          "Desplegar con PM2 y Nginx",
        ],
        lessons: [
          {
            id: "variables-de-entorno-y-secretos",
            title: "Variables de entorno y secretos",
            duration: "9 min",
            level: "intermedio",
            category: "seguridad",
            cardText: "El .env guarda secretos fuera del código y el runtime los inyecta.",
            desc: "Gestiona secretos y configuración con variables de entorno.",
            body: {
              intro: [
                "Una contraseña hardcodeada en el código termina en un repo público con una filtración. Los secretos (credenciales, API keys, tokens) viven fuera del código, en variables de entorno.",
                "En Node, dotenv carga un archivo .env al inicio y process.env expone esos valores al runtime sin comprometer el repositorio.",
              ],
              def: {
                par: "Las variables de entorno son pares clave=valor que el proceso recibe del sistema o de un archivo .env (cargado con dotenv). process.env.DB_PASS las expone en el código sin versionalas.",
                porQue: "Configuración desacoplada del código y secretos fuera del repo: el mismo código corre en dev, staging y producción con entornos distintos.",
                cuando: "Úsala para todo valor que cambia por entorno: credenciales, URLs, puertos, flags. Lo que no es secreto ni cambia, puede quedarse como constante.",
              },
              concepts: [
                "npm install dotenv carga el .env en process.env.",
                ".env se ignora en git (.gitignore).",
                ".env.example versiona la lista de variables sin valores.",
                "process.env.KEY devuelve el valor como string.",
                "Valida los valores al arrancar: falla rápido si falta algo.",
              ],
              basic: {
                p: "Un .env típico y la carga en el arranque de la API.",
                code: [
                  "require('dotenv').config();",
                  "",
                  "const { Pool } = require('pg');",
                  "",
                  "const pool = new Pool({",
                  "  connectionString: process.env.DATABASE_URL,",
                  "});",
                  "",
                  "app.listen(process.env.PORT || 3000);",
                ],
                out: "DATABASE_URL inyectado desde .env: ninguna credencial en el código.",
                lang: "js",
              },
              intermediate: {
                p: "Un validador de entorno que falla al arrancar si falta config.",
                caption: "Fallar rápido con config validada",
                code: [
                  "const { z } = require('zod');",
                  "",
                  "const configSchema = z.object({",
                  "  DATABASE_URL: z.string().min(1),",
                  "  JWT_SECRETO: z.string().min(32),",
                  "  PORT: z.coerce.number().default(3000),",
                  "});",
                  "",
                  "const resultado = configSchema.safeParse(process.env);",
                  "",
                  "if (!resultado.success) {",
                  "  console.error('Entorno incompleto:', resultado.error.issues);",
                  "  process.exit(1);",
                  "}",
                  "",
                  "module.exports = resultado.data;",
                ],
                lang: "js",
              },
              real: {
                p: "En producción, las variables las inyecta la plataforma (Vercel, Docker, systemd) y .env ni siquiera existe. El código solo lee process.env.",
                ul: [
                  "Versiona .env.example como referencia pública.",
                  "Nunca trampees un secreto al código: ni siquiera en comentarios.",
                  "Rota los secretos en caso de fuga.",
                ],
                code: [
                  "# .env (local, ignorado por git)",
                  "PORT=3000",
                  "DATABASE_URL=postgres://app:clave@localhost:5432/tienda",
                  "JWT_SECRETO=",
                  "",
                  "# .gitignore",
                  "node_modules/",
                  ".env",
                  "",
                  "# Docker Compose (produccion)",
                  "environment:",
                  "  - DATABASE_URL=${DATABASE_URL}",
                  "  - JWT_SECRETO=${JWT_SECRETO}",
                ],
                nota: "No generes claves con valores triviales: usa openssl rand -hex 32 para un JWT_SECRETO fuerte.",
                lang: "bash",
              },
              expl: [
                "dotenv lee .env solo en desarrollo; en prod inyecta la plataforma.",
                "process.env devuelve strings: tipa y convierte al usar.",
                ".env.example documenta el contrato del entorno.",
                "Validar al arrancar (fail fast) evita errores raros en runtime.",
                "Nunca comitees .env: está en tu .gitignore desde el día uno.",
              ],
              errors: [
                "Commitear .env con credenciales reales.",
                "Poner el secreto como default en el código.",
                "No validar y explotar con DATABASE_URL undefined en producción.",
              ],
              tips: [
                "Usa dotenv solo para desarrollo: en prod, inyecta por plataforma.",
                "Genera secretos con openssl rand -hex 32.",
                "Mantén nombres UPPER_SNAKE para las variables.",
              ],
              exercise: [
                "Mueve tus credenciales a un .env y añádelo al .gitignore.",
                "Crea .env.example con todos tus entornos documentados.",
                "Valida el entorno con Zod y falla rápido si falta algo.",
              ],
              summary: [
                "Los secretos viven en entorno, nunca en el código.",
                "dotenv carga .env en dev; la plataforma inyecta en prod.",
                "Validar al arrancar convierte config mala en error temprano.",
              ],
              lang: "js",
              exampleFile: "env-secretos",
              desc: "Gestiona secretos y configuración con variables de entorno.",
            },
          },
          {
            id: "helmet-cors-y-seguridad-http",
            title: "Helmet, CORS y seguridad HTTP",
            duration: "10 min",
            level: "intermedio",
            category: "seguridad",
            cardText: "Helmet fija cabeceras seguras y CORS decide quién puede llamarte.",
            desc: "Protege tu API con Helmet y configura CORS correctamente.",
            body: {
              intro: [
                "La seguridad HTTP empieza en las cabeceras: CSP, nosniff, referrer y demás dicen al navegador cómo comportarse ante tu contenido.",
                "Helmet configura esas cabeceras por ti, y CORS (Cross-Origin Resource Sharing) decide qué orígenes pueden consumir tu API desde el navegador.",
              ],
              def: {
                par: "Helmet es un conjunto de middlewares que fija cabeceras de seguridad (CSP, X-Frame-Options, HSTS, nosniff). cors() define la política CORS: qué orígenes, métodos y cabeceras se permiten.",
                porQue: "Las cabeceras seguras frenan clases de ataques en el navegador (XSS, clickjacking) y una política CORS estricta evita que cualquier web abuse de tu API.",
                cuando: "Helmet siempre, en toda API. CORS lo necesitas cuando consumes tu API desde un frontend con origen distinto; de lo contrario, hostname restringido o ausencia de CORS.",
              },
              concepts: [
                "npm install helmet cors añade ambos paquetes.",
                "app.use(helmet()) fija las cabeceras de seguridad por defecto.",
                "cors({ origin }) restringe los orígenes permitidos.",
                "Los métodos y cabeceras permitidos se configuran en CORS.",
                "Sin CORS, un frontend en otro origen verá bloqueada tu API.",
              ],
              basic: {
                p: "Activación de Helmet y una política CORS solo para tu frontend.",
                code: [
                  "const helmet = require('helmet');",
                  "const cors = require('cors');",
                  "",
                  "app.use(helmet());",
                  "",
                  "app.use(cors({",
                  "  origin: 'https://app.ejemplo.com',",
                  "  methods: ['GET', 'POST', 'PUT', 'DELETE'],",
                  "  allowedHeaders: ['Content-Type', 'Authorization'],",
                  "}));",
                ],
                out: "Las respuestas llevan cabeceras seguras y solo app.ejemplo.com puede consumir la API.",
                lang: "js",
              },
              intermediate: {
                p: "Lista de orígenes permitidos y manejo de preflight.",
                caption: "Lista blanca de orígenes",
                code: [
                  "const permitidos = [",
                  "  'https://app.ejemplo.com',",
                  "  'https://admin.ejemplo.com',",
                  "];",
                  "",
                  "app.use(cors({",
                  "  origin: (origin, cb) => {",
                  "    if (!origin || permitidos.includes(origin)) {",
                  "      return cb(null, true);",
                  "    }",
                  "    cb(new Error('origen no permitido'));",
                  "  },",
                  "  credentials: true,",
                  "}));",
                ],
                lang: "js",
              },
              real: {
                p: "Las API puras que sirven solo a clientes de confianza (apps propias, backend) pueden omitir CORS; los SPAs con frontend separado lo necesitan con un origen acotado.",
                ul: [
                  "Nunca uses cors() sin restringir orígenes en producción.",
                  "Combina con Helmet para cabeceras seguras de serie.",
                  "Usa credentials: true solo si de verdad necesitas cookies.",
                ],
                code: [
                  "const app = express();",
                  "",
                  "app.use(helmet({",
                  "  contentSecurityPolicy: {",
                  "    directives: {",
                  "      defaultSrc: [\"'self'\"],",
                  "      scriptSrc: [\"'self'\"],",
                  "    },",
                  "  },",
                  "}));",
                  "",
                  "// API de confianza: no hay CORS, solo mis apps",
                  "// app.use(cors({ origin: ['https://app.ejemplo.com'] }));",
                ],
                nota: "No necesitas CORS si tu frontend y API comparten origen o si la API solo se consume desde el servidor (sin navegador).",
                lang: "js",
              },
              expl: [
                "Helmet agrupa decenas de cabeceras de seguridad probadas.",
                "CSP limita desde dónde cargan los recursos del navegador.",
                "cors() predefine la política de origin/methods/headers.",
                "Un origin inválido se rechaza con un error CORS en el navegador.",
                "credentials: true obliga a orígenes explícitos (no '*').",
              ],
              errors: [
                "Dejar cors() abierto a cualquier origen en producción.",
                "confundir el error de preflight con un 500 de tu API.",
                "No montar Helmet y exponer la API sin cabeceras de seguridad.",
              ],
              tips: [
                "Configura Helmet primero: cobertura de serie.",
                "Escribe la lista blanca de orígenes en configuración.",
                "Prueba CORS desde un navegador en otro puerto.",
              ],
              exercise: [
                "Añade Helmet y comprueba las cabeceras con curl -I.",
                "Configura una lista blanca de dos orígenes válidos.",
                "Comprueba el preflight OPTIONS de una petición CORS.",
              ],
              summary: [
                "Helmet fija cabeceras de seguridad al instante.",
                "CORS con lista blanca protege quién puede llamarte.",
                "Sin navegador de por medio, CORS puede no hacer falta.",
              ],
              lang: "js",
              exampleFile: "helmet-cors",
              desc: "Protege tu API con Helmet y configura CORS correctamente.",
            },
          },
          {
            id: "rate-limiting-y-proteccion",
            title: "Rate limiting y protección",
            duration: "10 min",
            level: "intermedio",
            category: "seguridad",
            cardText: "express-rate-limit frena abusos y fuerza bruta con límites por IP.",
            desc: "Limita el uso de tu API con express-rate-limit.",
            body: {
              intro: [
                "Una API sin límites es un juguete roto: un cliente insiste en /login con miles de intentos o un script descarga toda la base con peticiones sin fin.",
                "El rate limiting fija cuántas peticiones aceptas por ventana de tiempo y por cliente, frenando fuerza bruta, scraping y abusos de coste.",
              ],
              def: {
                par: "El rate limiting limita las peticiones de un cliente (IP o identificador) en una ventana temporal (p.ej. 100 peticiones / 15 min), devolviendo 429 cuando se supera.",
                porQue: "Protege recursos (CPU, base de datos, coste) y flancos de abuso: login por fuerza bruta, scraping, spam de endpoints caros.",
                cuando: "Aplica un límite general en toda la API y límites más estrictos en endpoints sensibles (login, registro, envío de emails).",
              },
              concepts: [
                "express-rate-limit configura ventana y máximo.",
                "El contador se clavea por IP (o por usuario).",
                "Superar el límite responde 429 Too Many Requests.",
                "Retry-After informa cuándo reintentar.",
                "Los límites se almacenan en memoria por defecto (Redis si se escala).",
              ],
              basic: {
                p: "Un límite global para toda la API.",
                code: [
                  "const rateLimit = require('express-rate-limit');",
                  "",
                  "const limiteGeneral = rateLimit({",
                  "  windowMs: 15 * 60 * 1000,",
                  "  max: 100,",
                  "  standardHeaders: true,",
                  "  legacyHeaders: false,",
                  "  message: { error: 'demasiadas peticiones' },",
                  "});",
                  "",
                  "app.use(limiteGeneral);",
                ],
                out: "A partir de la petición 101 en 15 minutos, la API responde 429.",
                lang: "js",
              },
              intermediate: {
                p: "Un límite quirúrgico para el endpoint de login.",
                caption: "Rate limit específico por endpoint",
                code: [
                  "const loginLimit = rateLimit({",
                  "  windowMs: 60 * 1000,",
                  "  max: 5,",
                  "  skipSuccessfulRequests: true,",
                  "  message: { error: 'demasiados intentos, espera un minuto' },",
                  "});",
                  "",
                  "app.post('/login', loginLimit, controladores.login);",
                ],
                lang: "js",
              },
              real: {
                p: "En producción detrás de un proxy, Express necesita saber la IP real; con Nginx on-trust-proxy y app.set('trust proxy', true) el rate limit cuenta bien.",
                ul: [
                  "Configura trust proxy si usas Nginx/Cloudflare.",
                  "Clavea por usuario autenticado cuando puedas, no solo por IP.",
                  "Sube límites para orígenes internos y monitoriza 429s.",
                ],
                code: [
                  "const app = express();",
                  "app.set('trust proxy', 1);",
                  "",
                  "const apiLimit = rateLimit({",
                  "  windowMs: 60_000,",
                  "  max: 30,",
                  "  keyGenerator: (req) => req.ip,",
                  "});",
                  "",
                  "app.use('/api', apiLimit);",
                  "",
                  "// En Redis si hay varias instancias:",
                  "// const RedisStore = require('rate-limit-redis').RedisStore;",
                  "// store = new RedisStore({ sendCommand: (...a) => client.sendCommand(a) })",
                ],
                nota: "Con varias instancias, el límite en memoria deja de ser global: comparte el contador con Redis.",
                lang: "js",
              },
              expl: [
                "windowMs y max definen la ventana y el tope.",
                "El 429 'demasiadas peticiones' es la respuesta por exceso.",
                "skipSuccessfulRequests no cuenta los logines correctos.",
                "trust proxy evita contar peticiones desde 127.0.0.1.",
                "Redis hace el límite global entre instancias.",
              ],
              errors: [
                "Olvidar trust proxy detrás de Nginx y limitar a tu propio proxy.",
                "Un límite tan alto que no frenará nada o tan bajo que molesta.",
                "Contar solo por IP y dejar pasar abusos por usuarios compartidos.",
              ],
              tips: [
                "Empieza con un límite global conservador y afina con métricas.",
                "Endpoints caros (export, login, envío) con límites estrictos.",
                "Devuelve Retry-After para que los clientes se reorganicen.",
              ],
              exercise: [
                "Aplica un límite global y prueba rebasar con un bucle.",
                "Protege /login con 5 intentos por minuto.",
                "Habilita trust proxy y comprueba las IPs reales.",
              ],
              summary: [
                "El rate limiting pone un tope por ventana y por cliente.",
                "Los endpoints sensibles llevan límites más estrictos.",
                "trust proxy y Redis mantienen el contador correcto en producción.",
              ],
              lang: "js",
              exampleFile: "rate-limiting",
              desc: "Limita el uso de tu API con express-rate-limit.",
            },
          },
          {
            id: "despliegue-pm2-y-nginx",
            title: "Despliegue con PM2 y Nginx",
            duration: "12 min",
            level: "intermedio",
            category: "seguridad",
            cardText: "PM2 mantiene tu Node vivo y Nginx lo sirve al mundo.",
            desc: "Despliega tu API Node con PM2 detrás de Nginx.",
            body: {
              intro: [
                "Tu API en local funciona, pero producción es otro país: caídas, reinicios, logs y un proxy que reparta HTTPS. PM2 se encarga del proceso y Nginx del tráfico.",
                "PM2 mantiene el proceso vivo (reinicia en caídas, aguanta en cluster) y Nginx escucha en el puerto 80, sirve estáticos y reenvía a tu Node.",
              ],
              def: {
                par: "PM2 es el gestor de procesos de Node: arranca, monitorea y reinicia tu app en clúster. Nginx actúa de reverse proxy: termina HTTPS, sirve estáticos y redirige el resto a la app por un puerto interno.",
                porQue: "Un proceso Node sin supervisor muere con el primer crash. PM2 garantiza disponibilidad y Nginx aporta seguridad, HTTPS y un front robusto.",
                cuando: "Úsalo en la mayoría de despliegues de una app Node: VPS, bare-metal y contenedores (aunque Docker+PM2 se combinan según el caso).",
              },
              concepts: [
                "npx pm2 start app.js mantiene el proceso vivo.",
                "pm2 reload y pm2 restart aplican cambios sin perder uptime.",
                "pm2 monit y pm2 logs vigilan el proceso.",
                "Nginx hace un reverse proxy con proxy_pass a 127.0.0.1:3000.",
                "un cluster en PM2 reparte peticiones entre varias instancias.",
              ],
              basic: {
                p: "Arrancamos la API con PM2 y creamos un sistema de despliegue simple.",
                code: [
                  "# Instalar y arrancar",
                  "npm i -g pm2",
                  "pm2 start app.js --name tienda-api",
                  "",
                  "# Visión general y logs",
                  "pm2 status",
                  "pm2 logs tienda-api",
                  "",
                  "# Al desplegar nuevas versiones",
                  "pm2 reload tienda-api",
                ],
                out: "El proceso queda monitorizado, con logs y disponibilidad automática.",
                lang: "bash",
              },
              intermediate: {
                p: "Varias instancias en clúster usan todos los núcleos del servidor.",
                caption: "Cluster con PM2",
                code: [
                  "# Sube una instancia por nucleo",
                  "pm2 start app.js -i max --name tienda-api",
                  "",
                  "# Recargar sin downtime en cluster",
                  "pm2 reload tienda-api",
                  "",
                  "# Guardar y generar el proceso de arranque",
                  "pm2 save",
                  "pm2 startup",
                ],
                lang: "bash",
              },
              real: {
                p: "El despliegue completo: Nginx termina HTTPS y reenvía; PM2 aguanta el proceso; la app queda tras el proxy en un puerto interno.",
                ul: [
                  "HTTPS con certbot en Nginx, nunca sin TLS en producción.",
                  "proxy_pass a 127.0.0.1:PORT mantiene tu app fuera del mundo.",
                  "pm2 startup hace que la app sobreviva a reinicios del servidor.",
                ],
                code: [
                  "server {",
                  "    listen 443 ssl;",
                  "    server_name api.ejemplo.com;",
                  "",
                  "    ssl_certificate     /etc/letsencrypt/live/api.ejemplo.com/fullchain.pem;",
                  "    ssl_certificate_key /etc/letsencrypt/live/api.ejemplo.com/privkey.pem;",
                  "",
                  "    location / {",
                  "        proxy_pass http://127.0.0.1:3000;",
                  "        proxy_set_header Host $host;",
                  "        proxy_set_header X-Real-IP $remote_addr;",
                  "        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;",
                  "    }",
                  "}",
                  "",
                  "server {",
                  "    listen 80;",
                  "    server_name api.ejemplo.com;",
                  "    return 301 https://$host$request_uri;",
                  "}",
                ],
                nota: "tu app Express debe confiar en el proxy para ver IPs reales: app.set('trust proxy', true) (y el rate limit lo agradece).",
                lang: "nginx",
              },
              expl: [
                "PM2 supervisa y reinicia; el proceso no muere silenciosamente.",
                "El cluster reparte peticiones entre CPUs (Node multihilo de gestión).",
                "Nginx termina TLS y oculta el puerto interno de la app.",
                "proxy_set_header lleva host e IP real a la aplicación.",
                "pm2 startup registra el arranque en systemd del servidor.",
              ],
              errors: [
                "Abrir el puerto de la app (3000) al mundo en vez de usar solo Nginx.",
                "Depurar sin mirar pm2 logs: el error está registrado.",
                "Desplegar sobre un proceso sin pm2 reload (uptime y confusión).",
              ],
              tips: [
                "Cierra todos los puertos salvo 80/443 en el firewall.",
                "Usa pm2 reload (no restart) para cero downtime en cluster.",
                "Monitorea logs y memoria con pm2 monit.",
              ],
              exercise: [
                "Despliega tu API con PM2 y comprueba que sobrevive a un kill.",
                "Configura el reverse proxy en Nginx con tu dominio.",
                "Añade HTTPS con certbot y fuerza la redirección 301.",
              ],
              summary: [
                "PM2 supervisa el proceso y ofrece cluster y reload.",
                "Nginx hace de reverse proxy con TLS y oculta el puerto interno.",
                "pm2 startup y logs sostienen la operación del servidor.",
              ],
              lang: "nginx",
              exampleFile: "pm2-nginx",
              desc: "Despliega tu API Node con PM2 detrás de Nginx.",
            },
          },
        ],
      },
    ],
  },
];